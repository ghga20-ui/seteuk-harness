---
name: seteuk-harness
description: 수행평가 학생 원본(xlsx, hwpx, pdf, 사진 등 학생의 글이나 산출물 텍스트가 있는 활동 전제)을 입력으로 과목별 세부능력 및 특기사항(과세특) 초안을 생성하는 자기 성장형 하네스. 규칙 인터뷰, 구조 샘플 시연, 학생별 생성, 결정론+루브릭 2단 검증, 검증 통과 후에만 xlsx 저장, 검수 회고의 위키 축적까지 수행한다. 세특 작성, 과세특 생성, 수행평가 기반 생기부 초안 요청에 사용한다.
---

# 불변 원칙
- **질문으로 해결되는 애매함은 사다리를 내려가지 않는다.** 파일이 읽히고 질문 하나로 확정할 수 있으면(파일 후보 선택, 점수 열 선택 등) 그 질문을 하고 답을 기다린다 — 클립보드·수동 입력 같은 대체 입력을 성급히 권하지 않는다. 대체 입력 안내는 파일 읽기가 실제로 실패하고 파일 교정(재저장)까지 안 될 때만 한다
- **이 도구는 평가를 대신하지 않는다. 교사의 평가 자료(채점표, 루브릭 점수, 관찰 기록 등)가 확인되지 않은 활동은 세특을 생성하지 않는다** — 채점 완료를 먼저 안내하고 중단한다(검증기도 평가자료 미기록 시 저장을 차단한다)
- **톤(수준) 판정의 주권은 교사에게 있다.** 톤 등급은 교사의 평가 자료에서 기계적으로 파생하는 것을 기본으로 하고, 원문 품질에 따른 조정이 필요해 보이면 생성 전에 조정안을 질문으로 제시해 승인된 것만 반영한다. AI가 조정해 놓고 사후 통보하지 않는다
- **학생의 실명과 학번은 LLM에 전송하지 않는다.** 명렬 인식·가명화·재결합은 전부 `pseudonymize.py` CLI를 통해 로컬 파이썬이 수행하고(**예외: 손글씨 이미지 마스킹은 아직 CLI가 없어 `mask_images.py` 함수 직접 호출이다** — 마스킹 실적용을 확인하는 기계 게이트도 없으므로, 손글씨 활동은 전송 직전 교사 육안 확인이 필수이며 이 경로의 보증 수준이 텍스트 경로보다 낮다는 것을 시작 전에 교사에게 알린다), 에이전트(LLM)는 그 CLI의 stdout 요약(인원수·건수)만 읽는다 — **명렬 파일(명렬.json)·매핑표(매핑.json)·채점표를 에이전트가 직접 열어보지 않는다.** 오케스트레이터 자체가 클라우드 LLM이므로 대화창에 입력되는 순간 이미 전송이 끝난다는 점을 전제한다 — 그래서 교사 관찰 메모는 **채팅으로 받지 않고 파일로만** 받아 `memo` 명령으로 토큰화한 뒤에야 에이전트가 결과를 보고, 채점표의 점수도 **채점표 원본을 열지 않고** `score` 명령의 산출물(토큰↔점수)로만 받는다
- **매핑표는 로컬 전용이며 활동 종료 시 파기한다.** 매핑표와 민감 경고 상세는 확정본 발행 후 삭제하고, 위키에는 익명 건수만 남긴다
- 학생 원본에 실제로 있는 내용만 쓴다. 미화·창작 금지
- 검증 FAIL 상태에서는 어떤 산출물도 저장하지 않는다
- 위키(활동이력 포함)에 학생 실명·학번·원문을 기록하지 않는다
- 위키 개정은 사용자 승인 후에만 한다
- 초안은 교사 검수·승인 전에는 NEIS 입력 후보가 아니다. 이를 사용자에게 명확히 안내한다
- 모든 응답은 한국어로 한다

# 개입 지도 (사람이 결정하는 곳)
이 하네스는 Human in the Loop 원칙으로 설계되었다. 사람은 판단이 필요한 고판단·저빈도 지점에, 기계는 저판단·고빈도 지점에 배치한다. 학생별 개별 승인처럼 사람을 도장 찍는 기계로 만드는 설계는 하지 않는다.

| 단계 | 사람의 역할 | 방어하는 실패 |
|---|---|---|
| 채점 | **결정(In)** — 평가 없이는 시작 불가 | 평가 행위의 AI 위임 |
| 규칙·구조 인터뷰 | **결정(In)** — 선택지 비교로 확정 | 획일적 기본값 |
| 파일럿 배치 | **결정(In)** — 표본 승인 후 배치 생성 | 배치 전체의 문체·톤 오류 |
| 배치 생성·기계 검증 | 자동(Out) — 규칙은 사람이 정한 것 | 단순 반복의 사람 소모 |
| 검수 | **감독(On)** — 우선 검토 대상부터 | 균등 검토의 도장 찍기화 |
| 회고·확정 | **결정(In)** — 패턴별 승인, 확정 선언 | AI의 자가 학습 폭주 |

# 실행 전 점검 (환경 자동 준비)
사용자에게 설치를 미루지 말고 스킬이 직접 환경을 준비한다. 사용자는 권한 승인만 하면 된다.

0. **업데이트 확인**: 스킬 폴더에서 `git fetch` 후 로컬이 원격보다 뒤처져 있으면(`git rev-list HEAD..origin/master --count`가 0이 아니면) 사용자에게 한 줄로 알리고 받을지 묻는다: "스킬에 새 버전이 있습니다(수정 N건). 받을까요?" — 승인 시 `git pull`. 교사의 개인 축적물(`wiki/성장/`·활동이력)은 git 밖이라 업데이트로 덮이지 않는다. 네트워크가 없거나 fetch가 실패하면 조용히 건너뛴다(차단 금지). **묻지 않고 pull하지 않는다** — 작업 직전에 동작이 바뀌면 교사가 혼란스럽다. **이 확인은 오케스트레이터가 1회만 한다** — 서브에이전트가 각자 pull하면 부모와 자식이 서로 다른 버전의 절차를 따르게 된다
1. 파이썬 탐지: `python --version`이 실패하면 `py --version`, `python3 --version`을 차례로 시도한다. 성공한 명령을 이 세션의 파이썬 명령으로 삼는다(이후 모든 스크립트 실행에 사용)
2. 파이썬이 없으면 사용자에게 한 줄로 알리고 직접 설치한다:
   - Windows: `winget install -e --id Python.Python.3.12` (winget이 없으면 Microsoft Store의 Python 설치를 안내)
   - macOS: `brew install python3` (brew가 없으면 python.org 설치 파일 안내)
   - 설치 후 새 셸에서 다시 탐지해 경로 반영을 확인한다
3. openpyxl 확인: `<파이썬> -c "import openpyxl"` — 실패하면 `<파이썬> -m pip install openpyxl`을 직접 실행한다(pip가 없으면 `<파이썬> -m ensurepip --upgrade` 먼저)
4. Pillow 확인(손글씨 이미지가 있는 경우): `<파이썬> -c "import PIL"` — 실패하면 `<파이썬> -m pip install pillow`를 직접 실행한다
5. 잔존 산출물 감지: 작업 폴더에 이전 실행의 매핑표·명렬·관찰메모·점수·토큰본(`매핑*.json`/`명렬*.json`/`관찰메모*.json`/`점수*.json`/`토큰본*.json`/`확인*.html`/`검수*.html` 등)이 남아 있으면 크래시 잔존으로 보고 사용자에게 알린 뒤 파기한다(`pseudonymize.detect_stale_artifacts` / `destroy_artifacts`)
6. 자동 설치까지 실패한 경우에만(프록시 차단 등) 수동 해결 방법을 안내하고 중단한다. 환경이 준비되기 전에는 생성을 시작하지 않는다. **1~5는 병렬이 아니라 배치로 줄인다** — 탐지 명령들은 각 수 초라 워커 재적재가 이득을 통째로 먹으므로 한 Bash 호출에 `;`로 이어 붙인다. 5의 잔존 감지·파기는 같은 폴더를 훑으므로 동시 실행하면 파기 경합이 난다
7. 대상이 3개 학급 이상이면 컨텍스트 한도를 고려해 반 단위로 나눠 실행하는 흐름을 기본으로 제안한다
8. 이 스킬은 학생의 글·산출물 텍스트가 있는 활동 전용이다. 활동 성격을 시작 때 확인한다 — **팀 산출물**(같은 글이 여러 학생에 매핑)이면 표절 의심 보고를 끄고 팀 단위 개별성 기준으로 바꾼다(인터뷰에서 확인). **학생당 파일이 여러 개**면 전부 그 학생의 근거로 병합해 쓰되 어느 파일에서 온 근거인지 비고에 남긴다. **본문이 영어 등 로마자**면 이름 마스킹이 한글 이름만 잡으므로 로마자 표기 실명은 탐지하지 못한다는 한계를 시작 전에 알린다. **산출물이 학생 창작물(자작시 등)**이면 핵심소재 자동 추출(첫 작은따옴표)이 작품명이 아닐 수 있으니 생성 시 핵심소재를 명시 기입한다. 실기 영상·연주처럼 텍스트 산출물이 없는 활동이면 범위 밖임을 알리고 중단한다

# 위키 선독 (매 실행 시작)
다음을 전부 읽는다: [wiki/규칙정본.md](wiki/규칙정본.md), [wiki/구조패턴.md](wiki/구조패턴.md), [wiki/표현사전.md](wiki/표현사전.md), [wiki/루브릭.md](wiki/루브릭.md), [wiki/문체관찰.md](wiki/문체관찰.md), `wiki/활동이력/`의 최근 3개 파일.
씨앗을 읽은 다음 `wiki/성장/`의 대응 파일(사용실적·규칙개정·표현이력·문체관찰 — gitignore되는 개인 축적물)을 함께 읽는다. 없으면 배포 직후의 씨앗 상태이므로 그대로 진행한다.
활동이력에서 이번 활동과 유사한 회차를 찾으면 그 결정(구조, 톤, 예외 방침)을 이번 인터뷰의 1안으로 삼는다.
**선독은 서브에이전트에 위임하지 않는다(요약을 받지 않는다)** — 이 내용 전체가 §3·§5의 생성 컨텍스트 그 자체라 요약 손실이 곧 품질 손실이고 어디서 손실됐는지 추적도 안 된다. 대신 **한 메시지에서 Read를 전부 동시에 발행**한다(병렬 서브에이전트가 아니라 병렬 툴콜) — 벽시계 이득은 여기서 이미 다 나온다.

# 병렬 실행 공통 규율
**기본은 순차다.** 아래 파이프라인에서 「병렬」이라고 명시한 단계에서만 서브에이전트를 띄우고 나머지는 오케스트레이터가 순차로 한다. 벽시계를 줄이는 수단은 병렬 말고 **배치**도 있다 — 한 Bash 호출에 명령 여러 개, 한 메시지에 Read 여러 개. 초 단위로 끝나는 로컬 CLI 구간은 배치가 정답이다(워커는 기반을 새로 싣기 때문에 작업당 소요가 짧으면 이득이 재적재 비용에 먹힌다).

1. **계약 재주입(생략 불가).** 서브에이전트는 이 SKILL.md의 불변 원칙을 상속하지 않는다 — 계약이 도달하지 않으면 워커에게 계약은 0이다. 모든 워커 브리프 머리에 다음을 **그대로** 붙인다:
   > - 아래에 경로가 명시된 파일만 읽는다. 폴더 탐색·Glob·상위 폴더 접근을 하지 않는다.
   > - 다음은 어떤 이유로도 열지 않는다: `명렬*.json`, `매핑*.json`, 채점표 원본, `확인*.html`, `검수*.html`, `검수번들*.json`, `검수본*.json`, 미마스킹 원본 이미지.
   > - 산출물과 반환값에 학생 실명·학번을 쓰지 않는다. 학생은 토큰(또는 파일명)으로만 부른다.
   > - 지정된 파일이 없거나 읽히지 않으면 **대안 파일을 찾지 말고** 그 사실을 보고하고 멈춘다(폴백 금지).
   > - 오케스트레이터가 지정하지 않은 파일에 쓰지 않는다. `활동프로파일.json`에는 절대 쓰지 않는다.
   > - 이유: 이 작업의 오케스트레이터는 클라우드 LLM이라 네가 실명을 읽는 순간 이미 전송이 끝난다. 되돌릴 수 없다.
   마지막 줄(이유)을 빼지 않는다 — 이유를 모르는 워커는 막혔을 때 조용히 우회한다("토큰본이 없네 → 옆에 명렬이 있네").
2. **범위 제한이 재주입보다 앞선다.** 재주입은 확률적 준수라 워커 N개면 위반 경로가 N배로 열리고, 워커 채널을 사후에 훑어 잡아 줄 장치는 없다(파기 그물도 단일 폴더 비재귀라 워커 스크래치패드를 못 본다). 그래서 워커에 넘기는 입력은 **토큰 산출물(`토큰본.json`·`관찰메모.json`·`점수.json`·`검수diff(AI용).json`·`wiki/`)과 마스킹 사본 이미지로 한정**한다. 실명 파일을 열어야 풀리는 작업은 어떤 이득이 있어도 병렬하지 않는다.
3. **사람에게 묻는 지점은 워커에 넣지 않는다.** 개입 지도에서 결정(In)·감독(On)인 단계, CLI가 exit 1로 사람에게 되묻도록 설계된 지점(detect 후보 다중, score 점수 열 후보 다중, memo 헤더 불일치 등)을 워커에 넘기면 사람 개입이 사라지는 게 아니라 **워커가 대신 결정한다**. 서브에이전트에는 사용자에게 물을 채널이 없다.
4. **쓰기는 오케스트레이터가 한다.** 워커는 결과를 반환값으로만 준다. 파일을 써야 하면 워커 전용 경로(`토큰본_1.json`처럼 파기 그물 글롭에 걸리는 이름)에만 쓰고 활동 폴더 밖에는 쓰지 않는다. **`활동프로파일.json` 갱신은 워커 전원 회수 후 오케스트레이터가 단독으로** 한다 — 프로파일은 락 없이 통째로 덮어쓰이는 구조라 동시 쓰기가 키를 조용히 지우고, `금지어휘`·`금지문자패턴`·`상한바이트`가 지워지면 검증기가 경고 없이 씨앗 기본값으로 채점해 **다른 기준으로 PASS가 난다**.
5. **전역 순서에 의존하는 배정은 워커가 하지 않는다.** 워커는 비동기로 끝나 반환 순서가 입력 순서와 다르다. 순서가 결과를 바꾸는 배정(토큰↔학생)은 오케스트레이터가 전원 회수 후 결정론 키(파일명 정렬 등)로 재구성해 수행한다.
6. **실패 추적.** 워커마다 고유 ID를 주고 반환에 `{워커ID, 처리 건수, 실패 목록, 확인 필요 목록}`을 반드시 포함시킨다. 실패 원인을 좁히겠다고 명렬·매핑을 여는 것은 금지다 — CLI 오류는 계약상 집계만 출력하므로 원인이 안 좁혀지면 그 구간을 **순차로 1회 재실행해** 확인한다.
7. **슬림 브리프.** 워커에 SKILL.md 전체(≈2.8만 토큰)를 싣지 않는다 — N배로 실으면 병렬 이득이 토큰 비용에 뒤집힌다. 해당 단계에 필요한 위키 파일·프로파일 값·입력 경로·계약 6줄만 싣는다.
8. **병렬 전 게이트 확인.** 워커를 띄우기 전에 그 단계의 선행 게이트(마스킹 실적용, 개수 대사, 결정론 검증 등)가 오케스트레이터에서 통과됐는지 확인한다. 게이트 자체를 워커에 위임하지 않는다.

# 파이프라인 (순서를 바꾸지 않는다)

## 1. 입력 파악 (입력 정규화)
학생 원본이 어떤 포맷으로 오든 이후 단계가 포맷을 몰라도 되도록 학생별 텍스트로 정규화한다. 특정 MCP나 외부 도구 설치를 전제하지 않는다.

1. 인벤토리: 원본 파일을 나열하고 확장자가 아니라 파일 헤더로 실제 포맷을 판정한다(번들 추출기가 판정과 추출을 함께 수행)
2. 포맷 라우팅 — 4단 폴백을 순서대로 시도한다:
   1) 세션에 한글 문서 파싱 도구(MCP·스킬)가 있으면 활용한다(선택 가속기일 뿐 필수가 아님)
   2) 번들 추출기를 사용한다: `python <스킬경로>/scripts/extract_sources.py <파일>` — hwpx, xlsx, 확장자 위장 HTML, 일반 텍스트 지원
      **조건부 병렬 — 파일 20개 이상일 때만 서브에이전트 2~3개.** 추출 자체는 밀리초라 벽시계 이득은 0이고, 병렬의 목적은 **오케스트레이터 컨텍스트 격리**다(48명 × 400~1500자를 오케스트레이터가 직접 읽으면 3~6만 토큰이 그냥 나간다). 분할은 파일명 정렬 후 균등. 각 워커는 추출 → 학생별 `{"학번","본문"}` 정규화 → **워커 전용 경로(`추출_1.json` 등)에 저장까지만** 하고, **본문을 반환하지 않는다** — 반환은 `{워커ID, 처리 건수, 실패 파일 목록, 확인 필요 목록}`뿐이다. 계약 6줄 재주입 필수(학번은 원본 파일 안의 값이므로 워커 반환 요약에 학번을 적지 않게 못 박는다). 파일이 20개 미만이면 순차 — 재적재 비용만 남는다
   3) 사진(jpg, png)과 스캔 pdf는 파일을 직접 읽어 비전으로 전사한다(외부 OCR 불필요). 손글씨 전사는 학생별 신뢰도를 기록하고, 판독이 안 되는 구간은 지어내지 않고 [판독불가]로 남기며 그 구간을 세특 근거로 쓰지 않는다
      **병렬 — 이미지 16장 이상이면 서브에이전트 4개.** 전 파이프라인에서 벽시계를 가장 많이 돌려주는 구간이다(장당 20~40초 × 48장 = 순차 20~30분 → 6~9분). 전사는 이미지 1장 안에서 닫히므로 상호 의존이 없다. **다만 아래를 전부 지켜야 병렬이 성립한다 — 하나라도 못 지키면 순차로 한다:**
      - **선행 게이트**: 손글씨면 8-2 개수 대사와 8-4 미마스킹 제외 게이트를 **오케스트레이터가 먼저** 통과시킨다. 마스킹 실적용 여부는 워커에 위임할 수 있는 판단이 아니다(기계 게이트가 없어 이 경로의 보증 수준이 이미 낮은데, 병렬은 그 낮은 보증을 4배로 곱한다)
      - **경로 명시**: 원본과 마스킹 사본이 같은 폴더에 공존하므로 워커는 둘을 구분할 근거가 없다. 브리프에 **마스킹 사본의 파일 경로를 하나씩 열거**한다. 폴더 경로를 주지 않고 Glob을 금지한다
      - **반환 규약**: 워커는 토큰을 모른다. `{"파일명","전사","신뢰도","판독불가구간":[...]}`만 반환한다. 토큰 배정은 8-5대로 오케스트레이터가 전원 회수 후 파일명 정렬로 촬영 순서를 재구성한 뒤 수행한다(공통 규율 5)
      - **재주입 항목**: 계약 6줄 + "판독 불가 구간은 지어내지 않고 `[판독불가]`로 남긴다" + "학생별 신뢰도를 기록한다" + "전사문에 이름·학번으로 보이는 글자가 보이면 옮겨 적지 말고 `[식별정보]`로 대체하고 그 파일명을 확인 필요 목록에 올린다"(마스킹이 빠진 장을 워커가 그대로 옮겨 적는 사고를 막는 마지막 그물)
   4) 위 방법이 모두 실패한 포맷(hwp 바이너리 등)은 사용자에게 한글에서 hwpx나 pdf로 다시 저장해 달라고 안내한다
3. 고유명사 즉시 대조: 작품명 등 흔들리기 쉬운 고유명사는 전사 직후 활동 참조 목록과 대조하고 불일치는 확인 목록에 올린다. **단독 병렬은 하지 않는다**(작업이 짧아 재적재 비용에 먹힌다) — 대신 2-2·2-3을 병렬로 돌릴 때 **참조 목록을 그 워커 브리프에 함께 실어 전사와 동시에 수행**한다(추가 비용 0). 불일치는 워커의 확인 필요 목록으로 올라온다
4. 명렬 확보: 전체 수강생의 학번·이름 목록을 확보한다(명렬 파일이 있으면 파싱, 없으면 사용자에게 확인). `명렬.json`으로 저장한다: `{"students": [{"학번": "10101", "이름": "김가상"}]}` — 명렬 없이는 미제출자를 감지할 수 없음을 사용자에게 알린다
5. 학생 매핑: 파일명·시트·표 구조에서 학번과 이름을 추출하고 **학번과 이름을 함께 명렬과 대조한다**(크로스체크). 하나라도 불일치하면 오전사·오매핑 의심으로 확인 목록에 올린다. 잘못된 학생에게 세특이 귀속되는 것이 이 도구의 최악의 실패이므로 추정 배정을 절대 하지 않는다. **4~6은 순차(병렬 금지)** — 4는 후보 다중 시 사용자가 확정해야 하고 `--profile`이 활동프로파일을 덮어쓰며, 5는 명렬 대조 그 자체라 실명 파일을 열지 않고는 성립하지 않는다. 6은 파일 1개라 병렬 대상이 아니다
6. 활동 안내문이 있으면 같은 방식으로 파싱해 활동명·평가 요소·참조 목록을 확보한다
7. 가명화(로컬 전용, 이 단계를 마치기 전에는 어떤 학생 데이터도 LLM에 노출하지 않는다). **모든 하위 항목은 함수를 직접 호출하는 것이 아니라 `pseudonymize.py` CLI를 실행하고 그 요약(인원수·건수)만 받는다 — 명렬 파일과 매핑표를 에이전트가 직접 읽지 않는다. CLI 출력(인원수·경고 건수)만 확인한다:**
   **이 절 전체는 순차다(병렬 금지).** 이유 셋: (1) 입력이 실명 파일(명렬·매핑·채점표)이라 공통 규율 2의 범위 밖이다, (2) `detect` 후보 다중·`score` 점수 열 다중·`memo` 헤더 불일치는 CLI가 일부러 exit 1로 **사람에게 되묻는** 지점이라 워커에 넣으면 워커가 대신 고른다, (3) 각 명령이 초 단위라 줄일 벽시계가 없다. 대신 **배치**를 쓴다 — 사람 확인이 끝나 되물을 것이 없다고 확정된 뒤에는 `mask`·`memo`·`score`(서로 다른 `--out`이라 경합 없음)를 한 Bash 호출에 이어 붙여 실행한다.
   1) 명렬 인식 — **파일 직접이 기본이다.** 교사는 명단·채점표를 활동 폴더에 넣고 말만 하면 된다. 아래 사다리(①→④)를 순서대로 밟고, 질문 하나로 해결되는 애매함 때문에 사다리를 내려가지 않는다.

      **① 후보 확인(detect).**
      ```
      python <스킬경로>/scripts/pseudonymize.py detect --dir <활동폴더>
      ```
      파일명·수정시각·시트 수만 출력된다(셀 내용은 열지도 출력하지도 않는다). **후보가 2개 이상이면 목록을 그대로 사용자에게 보여 어느 파일인지 확정받는다 — 자동 선택 금지.** 파일 오선택(백업본·작년 파일·미저장 엑셀)은 CLI의 fail-closed가 작동하지 않는 유일한 계층이라 자동 선택이 곧 사고다. 후보가 1개여도 파일명·수정시각을 알리고 그 파일이 맞는지 확인받는다. 구형 .xls는 목록에 "구형 .xls — 쓰려면 엑셀에서 .xlsx로 다시 저장해 주세요"로 표시된다.

      **② 명렬 인식 + 구조 지문(roster --profile).** 사람이 확정한 파일로 실행한다.
      ```
      python <스킬경로>/scripts/pseudonymize.py roster <명단원본파일> --out 명렬.json --profile 활동프로파일.json
      ```
      `--profile`을 반드시 붙인다. 첫 회차에는 명단 파일명과 구조 지문(시트명 목록·헤더행 해시·인원수 — 실명·학번이 들어가지 않는 값)이 활동프로파일에 기록되고("활동프로파일에 명단 파일과 구조 지문을 기록했습니다"), 다음 회차부터 자동 대조해 불일치면 exit 1로 멈춘다("중단: 지난번에 확정한 명단 파일과 다릅니다" + 파일명·인원수·시트 구성·머리글 차이 목록). 일부러 바꾼 것이면(전입·전출, 파일 교체) 활동프로파일의 `구조지문` 키를 지우고 재실행하게 안내한다. 파일이 엑셀에 열려 있으면(`~$` 잠금 파일) "이 파일이 엑셀에 열려 있습니다. 저장하셨는지 확인해 주세요."가 출력된다 — 그대로 전달하고 저장 확인 후 재실행한다.
      stdout에 "명렬 인식: N명 (방식: 표헤더|패턴)"만 나온다. 실패(0명)면 exit 1과 함께 원인(읽기오류 등)이 출력된다. 방식이 "패턴"이면 확인필요 경고가 함께 나오므로 원본 표를 사용자에게 확인받는다. 명렬에 성 열과 이름 열이 분리되어 있으면(흔한 형식) 자동으로 성+이름을 합쳐 전체 이름을 만들고 "성 열과 이름 열을 합쳐 전체 이름으로 사용합니다"를 알린다. 결합 후에도 1자 이름이 남아 있다는 경고가 나오면(예: '봄') 그 이름은 본문에서 안전하게 가릴 수 없어(일반명사 충돌) 이후 mask/memo 단계가 저장을 차단하므로, 명렬의 이름 열을 성이 포함된 전체 이름으로 고쳐 다시 이 명령을 실행해야 한다. **명렬 파일(명렬.json)을 에이전트가 직접 열어보지 않는다** — 인식 결과는 인원수와 방식만 CLI 출력으로 확인한다.

      **③ 가린 확인(대화).** stdout의 집계(인원수·방식·시트 합침·중복·건너뜀 경고)만으로 사용자와 확인한다. 실명·학번은 대화에 올리지 않는다 — "48명 읽었고, 시트 2개를 합쳤고, 중복 0건"처럼 집계로만 말한다.

      **④ 눈 대조(confirm-html).** 집계 확인만으로는 실명 전 행을 눈으로 대조하는 순간이 워크플로우 어디에도 없다 — 그 유일한 순간을 이 화면이 만든다.
      ```
      python <스킬경로>/scripts/pseudonymize.py confirm-html --roster 명렬.json --out 확인.html
      ```
      (토큰 발급 후라면 `--mapping 매핑.json`을 붙이면 제출 여부 열이 함께 표시된다.) 읽기 전용 명단 화면(순번·학번·이름)이 생성되니 사용자에게 브라우저로 열어 훑어보게 한다. 파일명은 '확인'으로 시작하는 .html이어야 저장된다 — 파기 그물(destroy)에 걸리게 하기 위한 강제다. 고칠 것이 있으면 이름을 대화에 치지 말고 "3번째 학생 이름이 달라요" 식으로 말하게 안내한다(화면 상단에 같은 안내가 박혀 있다). 이 파일은 실명을 담으므로 활동 폴더에만 두고, 확인이 끝나면 파기 대상이다.

      **교정 사다리 — 파일이 안 읽힐 때는 교정이 먼저다.** 구형 .xls 등으로 roster가 실패하면 클립보드로 넘어가지 말고 1회성 파일 교정을 안내한다(한 번 고치면 영구 해결). 문구 그대로:
      > 이 파일은 구형 .xls 형식이라 읽지 못했습니다. 내용 문제가 아니라 저장 형식 문제입니다.
      > 엑셀에서 한 번만 다시 저장하면 앞으로는 계속 그냥 됩니다:
      > 파일 열기 → F12 → 파일 형식에서 "Excel 통합 문서(*.xlsx)" 선택 → 저장.
      > 저장하셨으면 "다시"라고 말씀해 주세요.

      **비상구 — 클립보드(`--clipboard`).** 종이 사진·스캔 PDF처럼 **파일로 만들 수 없는 원본일 때만** 쓴다(파일이 있으면 언제나 파일 경로가 우선). 전환 문구 그대로 — 붙여넣기 선제 금지가 핵심이다:
      > 이 원본은 파일로 읽을 수 없습니다. 엑셀 표가 있다면 학번·이름·점수 열을 드래그해서
      > Ctrl+C만 눌러 주세요. ⚠ 이 창에는 붙여넣지 마세요 — 학생 실명이 대화에 들어갑니다.
      > 복사하셨으면 "복사했어"라고만 말씀해 주세요. 그때 한 번만 읽고 바로 지웁니다.
      > 복사 후: 전체 몇 명인가요? (반쪽 명단 검산)
      사용자가 "복사했어"라고 하면 실행한다:
      ```
      python <스킬경로>/scripts/pseudonymize.py roster --clipboard --out 명렬.json
      ```
      CLI가 클립보드를 한 번 읽고 즉시 비운다(Win+V 히스토리·대화창 Ctrl+V로 흘러갈 시간을 줄인다). `--profile`과는 함께 쓸 수 없다(클립보드에는 대조할 파일 지문이 없다). 읽은 인원수를 사용자가 말한 전체 인원과 검산하고, 다르면 반쪽 명단(부분 선택)을 의심해 다시 복사를 안내한다. 클립보드가 2회 연속 비어 있으면 CLI가 학교 보안 프로그램 가능성을 안내한다 — 재시도 루프를 돌리지 않고 그 안내를 그대로 전달한다. 클립보드 입력이어도 ④ 눈 대조는 생략하지 않는다.
      **엑셀에 시트가 여러 개면 시트를 이어붙이지 않고 시트별로 학번 집합을 비교해 판단한다** — 반별로 시트가 나뉜 표(선택과목 등)는 시트 간 학번이 겹치지 않으므로 합집합을 쓰고 "N개 시트를 합쳐 M명 인식했습니다"를 출력한다(결과 JSON에 `시트합침` 키로 시트명 목록이 남는다). 반대로 같은 명렬이 여러 시트에 반복 등재돼(교집합이 작은 시트의 80% 이상) 있으면 가장 큰 시트만 쓰고 확인사유에 반복 사실을 남긴다. 부분적으로만 겹치면 합집합을 쓰되 확인사유로 겹친 건수와 함께 확인을 요구한다. 시트가 1개면 종전과 동일하게 동작한다
   2) 제출자 확정 및 토큰 발급: 원본에 답안이 있는 학번만 제출자로 하고,
      ```
      python <스킬경로>/scripts/pseudonymize.py issue --roster 명렬.json --submitted <학번,학번,...> --out 매핑.json
      ```
      (또는 `--submitted-from <학번이 한 줄에 하나씩 있는 텍스트 파일>`) 미제출자에게는 토큰이 발급되지 않는다. stdout의 "제출자 N명, 미발급(미제출) M명"만 확인한다
   3) 텍스트 치환: 모든 학생 텍스트를 담은 `{"items":[{"학번","본문"}]}` 형태의 JSON을 만들어
      ```
      python <스킬경로>/scripts/pseudonymize.py mask <입력.json> --roster 명렬.json --mapping 매핑.json --out 토큰본.json
      ```
      을 실행한다. 이 명령이 내부에서 학번 유출 검사(`scan_leak`)까지 수행해 학번이 하나라도 남으면 저장하지 않고 exit 1로 차단한다. stdout의 "가명화: N건 처리, 본문 이름 치환 경고 W건, 학번 유출 0건"만 확인하고, 이후 LLM에는 `토큰본.json`의 토큰·본문만 넘긴다. 이름은 '김 하윤'처럼 글자 사이에 공백을 넣은 표기도 잡는다(학번도 '301 01'처럼 띄어 써도 숫자 경계를 지키며 잡는다). 성을 빼고 부르는 표기('하윤이는', '하윤아' 등)는 **인명 전용 호칭 조사(이는/이가/이를/아/야/쌤/선배 등)가 뒤따를 때만** 잡는다 — 맨몸으로 부른 '하윤'만 있으면 의도적으로 건드리지 않는다. 실제 학생 답안 실측(69,034자)에서 성 제외 이름이 맨몸으로 등장한 사례는 전부 시어·일반어 충돌(예: 성 제외 이름이 '하늘'이면 '하늘을 우러러' 같은 구절)이었고 조사 동반 등장은 0건이었기 때문에, 맨몸까지 잡으면 얻는 것 없이 학생 글(작품명·시어)만 훼손한다. **이 정도의 표기 변형까지가 한계다** — mask는 실행할 때마다 경고 건수와 무관하게 stdout 끝에 "⚠ 탐지 한계: 한자·영문 표기, 별명, 3자 이상 띄어쓰기, 명렬에 없는 사람(교사·가족·친구)의 이름은 탐지하지 못합니다. 이 도구는 위험을 줄일 뿐 0으로 만들지 않습니다."를 항상 출력한다. **"경고 0건"을 "안전하다"로 읽지 않는다** — 이 문구가 나온다는 사실 자체가 탐지에 원리적 한계가 있다는 뜻이므로, 교사의 최종 육안 검수를 대체하지 않는다
   4) **관찰 메모 수집(있는 경우): 반드시 파일로 받는다, 대화로 받지 않는다.** 오케스트레이터가 곧 클라우드 LLM이므로 "관찰 메모 불러 주세요" 같은 대화 질문에 교사가 실명·학번을 타이핑하는 순간 이미 전송이 끝난다 — 가명화 이전이든 이후든 대화 채널 자체를 쓰지 않는다. 채점표의 '관찰 메모'/'관찰메모'/'메모'/'특기사항' 열(공백 무시 자동 탐지) 또는 `학번: 메모`/`학번 메모` 형식의 별도 텍스트 파일로 받아
      ```
      python <스킬경로>/scripts/pseudonymize.py memo <메모원본파일> --roster 명렬.json --mapping 매핑.json --out 관찰메모.json
      ```
      을 실행한다. 메모 열 헤더를 못 찾으면 exit 1과 함께 헤더를 '관찰 메모'로 지정해 달라는 안내가 나오니 그대로 전달한다. 매핑에 없는 학번(미제출자 등)의 메모는 건수만 보고되고 제외된다. stdout의 "관찰 메모: N건 수집(M건은 매핑 없음으로 제외). 본문 이름 치환 경고 W건, 학번 유출 0건"만 확인하고, 이후 LLM에는 `관찰메모.json`의 토큰·메모만 넘긴다. **메모 데이터가 있는 시트가 여럿이면 roster와 같은 논리로 자동 병합을 시도한다** — 열 인덱스를 시트 간에 재사용하는 것(시트1의 헤더 인덱스로 시트2를 읽는 것)이 진짜 위험이었고 그건 시트별 독립 해석으로 이미 해결됐으므로, 학번 교집합이 없고 메모 열의 헤더 텍스트가 같으면 자동으로 합쳐 진행하고 "N건 수집(M개 시트 합침)"을 알린다. 학번이 겹치면(같은 학생에 값이 둘) 또는 시트마다 메모 열의 헤더가 다르면 exit 1로 멈추고 각각 안내(`--sheet` 요구 또는 시트별 헤더 텍스트 병기)가 나오니 그대로 전달해 `--sheet <시트명>`으로 재실행하게 한다. 시트가 1개면 자동 진행된다
   5) **점수 수집(교사 평가 자료가 채점표인 경우): 채점표를 에이전트가 직접 열지 않는다 — 점수는 `score` 명령의 산출물(토큰↔점수)로만 받는다.** 채점표에는 학번·이름이 함께 있으므로 명렬·매핑표와 동일하게 취급한다. **파일 직접이 기본이다** — 채점표 파일도 위 ①(detect)의 목록에서 사람이 확정한 파일을 쓰고, 애매한 것(점수 열 후보 여럿 등)은 CLI가 멈춰서 물으니 그 질문을 그대로 전달해 사람이 답하게 한다:
      ```
      python <스킬경로>/scripts/pseudonymize.py score <채점표> --roster 명렬.json --mapping 매핑.json --out 점수.json [--column 점수] [--sheet 1반]
      ```
      점수 열 헤더는 '점수'/'총점'/'합계'/'평가점수'(공백 무시) 중 하나를 자동 탐지한다. **다중 헤더 채점표(예: 활동별로 각각 '점수' 열이 있고 '총점'도 따로 있는 표)에서 점수 열 후보가 2개 이상이면(한 시트 안에서) 조용히 하나를 고르지 않고 exit 1로 멈춘 뒤 후보 목록(엑셀 열 문자 + 상위 병합 라벨 결합, 예: "J열 — 소설 비평하기 > 점수")을 그대로 사용자에게 전달한다** — 오선택은 반 전체의 톤 등급이 조용히 틀어지는 최악의 실패이므로 반드시 교사가 확정하게 한다. 사용자가 확정하면 `--column`에 엑셀 열 문자(J, R, AA 등)나 헤더 이름 중 편한 쪽을 그대로 넣어 재실행한다. **엑셀에 시트가 여러 개면(반별로 나뉜 채점표 등) roster와 같은 논리로 자동 병합을 시도한다** — 진짜 위험은 "시트를 합치는 것"이 아니라 "시트1의 열 인덱스로 시트2를 읽는 것"이었고 그건 시트별 독립 해석으로 이미 해결됐다. 그래서 (1) 시트 간 학번 교집합이 없고 각 시트에서 해석된 점수 열의 라벨이 같으면(`--column`을 지정했든 자동 탐지했든 동일하게 적용) 자동으로 합쳐 진행하고 "N명(열: R — 시 비평하기 > 점수, 2개 시트 합침)"을 알린다. (2) 학번이 겹치면(같은 학생에 값이 둘이라 어느 쪽이 맞는지 알 수 없음) exit 1로 멈추고 `--sheet <시트명>`을 요구한다. (3) 학번은 겹치지 않아도 시트마다 해석된 열의 라벨이 다르면(같은 열 문자가 다른 의미를 가리키는 가장 위험한 경우) exit 1로 멈추고 시트별 라벨을 나란히 보여준다(예: "시트마다 R열의 의미가 다릅니다. --sheet 로 지정해 주세요. / 1반 R열 — 시 비평하기 > 점수 / 2반 R열 — 총점"). (4) 데이터가 있는 시트가 1개면 종전처럼 자동 진행한다(회귀 없음). `--sheet <시트명>`으로 특정 시트만 지정할 수도 있고, 잘못된 시트명을 주면 exit 1과 함께 유효한 시트명을 안내한다. `--grade-column`/`--sum-columns`도 같은 시트 병합 논리를 따른다. **확정한 열은 활동프로파일.json의 `점수열` 키에 에이전트가 기록해 다음 회차(같은 형식의 채점표)에 재사용한다** — 이 기록은 코드가 아니라 에이전트가 하므로, **프로파일 파일을 읽어 해당 키만 더한 뒤 통째로 다시 쓴다**(다른 키를 지우지 않게 주의). 여러 작업이 동시에 이 파일을 쓰면 키가 조용히 사라지고 검증기가 씨앗 기본값으로 되돌아가 경고 없이 통과하므로, **활동프로파일.json은 오케스트레이터만 쓴다**(서브에이전트 쓰기 금지) — 매 회차 같은 질문을 반복하지 않기 위해서다. 후보가 0개(점수 열 자체가 없는 채점표)면 exit 1과 함께 헤더를 '점수'로 지정하거나 `--column`으로 알려 달라는 안내가 나오는데, 점수 대신 등급(A/B/C, 상/중/하 등)만 있는 채점표는 `--grade-column <열>`로, 점수 열 없이 문항별 원점수만 있는 채점표는 `--sum-columns <범위>`(예: `--sum-columns E:J`)로 대안을 안내한다(`--column`/`--grade-column`/`--sum-columns`는 상호 배타 — 2개 이상 주면 exit 1). 매핑에 없는 학번(미제출자 등)과 점수가 비었거나 숫자가 아닌 행은 건수로만 보고되고 제외된다. stdout의 "점수 수집: N명(열: R — 시 비평하기 > 점수). 분포 — 15점 5명, 13점 2명, ... 제외: 매핑 없음 M건, 점수 없음 K건"만 확인한다 — 점수 분포는 집계값이라 식별정보가 아니므로 출력되지만, 학번·이름은 여기서도 절대 출력되지 않는다. 이후 톤 등급은 `점수.json`의 토큰별 점수(또는 등급)에 교사와 정한 구간표를 적용해 파생한다.

      **분포 에코 낭독 의무.** stdout의 분포("15점 5명, 13점 2명, …")를 사용자에게 그대로 읽어 주고 "만점 근처에 몰려 있는 게 맞나요? 점수 없는 N명은 미제출인가요?" 식으로 반드시 확인받는다. 실측 채점표 37개 중 20개(54.1%)가 점수 열 후보를 정확히 하나만 내놓았는데 그 하나가 틀린 활동의 열이었다 — 후보가 1개면 모호성 감지가 원리적으로 발동하지 않아 어떤 파싱 로직도 이 오염을 못 잡는다. 분포를 아는 사람의 눈이 마지막 그물이므로, 활동프로파일의 `점수열` 재사용 회차에도 이 확인은 생략하지 않는다.

      **비상구 — 클립보드(`--clipboard`).** 명렬과 같은 기준(파일로 만들 수 없는 원본일 때만)·같은 전환 문구를 쓴다. 사용자가 "복사했어"라고 하면:
      ```
      python <스킬경로>/scripts/pseudonymize.py score --clipboard --roster 명렬.json --mapping 매핑.json --out 점수.json
      ```
      한 번 읽고 즉시 비운다. 숫자 열이 여럿이면(엑셀 Ctrl 비인접 선택이 사이 열을 딸려 보내는 경우) 조용히 하나를 고르지 않고 멈추니, 학번·점수 두 열만 다시 복사하게 안내한다. 다른 입력 옵션과 함께 쓸 수 없다.

      `--paste`(도구.html 붙여넣기 산출물 `점수원본.json`) 경로는 비상용이다 — 파일이 전혀 읽히지 않고 클립보드도 막힌 환경에서만 안내하고, 기본 동선에서는 언급하지 않는다.
   6) 이미지 마스킹: 아래 이미지 절차를 따른다
   7) 매핑표(`매핑.json`)는 로컬 전용이며 위 `issue`/`mask`/`memo`/`score` 명령이 자동으로 다루므로 종료 시 파기 대상으로만 취급한다(내용을 열어보지 않는다)

8. 손글씨 이미지 절차(해당 시):
   1) 캘리브레이션: **미기입 빈 양식 1장**으로 인적사항 구역 위치를 파악하고(개인정보 0), 마스킹 미리보기를 사용자에게 보여 비율 좌표를 승인받는다. 승인된 좌표는 활동프로파일에 저장해 다음 회차에 재사용한다. 빈 양식이 없거나 자유 형식 사진이면 사용자와 대화로 영역을 지정한다. **조건부 병렬 — 빈 양식이 3종 이상일 때만 양식별 워커 1개**로 좌표 후보와 미리보기 생성까지만 시킨다(빈 양식은 개인정보 0이라 계약 밖이다). **승인은 언제나 오케스트레이터가 사용자에게 받고, 활동프로파일 기록도 오케스트레이터가 한다**(공통 규율 3·4). 2종 이하면 순차
   2) 미제출자 사전 신고를 받은 뒤 `check_count`로 개수 대사를 한다(이미지 수 = 학생당 장수 x 제출자 수). **불일치하면 차단하고 원인을 물어 재확인한다**. **순차 필수** — 전역 카운트 게이트라 분할하면 조각마다 기대값이 새로 생겨 게이트가 무의미해진다
   3) `mask_region`으로 마스킹 사본을 만들고(원본 보존, 여유 있게 크게 덮는다), `crop_region`으로 인적 크롭을 로컬에만 남긴다. **병렬 금지 — 한 파이썬 스크립트의 for 루프로 전 장을 처리한다**(장별 독립은 맞지만 Pillow 로컬 연산이 장당 수십 ms라 워커 재적재가 순손실이고, 워커에 미마스킹 원본 경로를 주는 것 자체가 계약 위반이다)
   4) 마스킹이 적용되지 않았거나 대사가 불일치하는 이미지는 **LLM 전송에서 제외하고 재지정을 요구한다**. 교사 확인만으로 미마스킹 전송을 허용하지 않는다. **이 게이트는 절대 병렬 대상이 아니며, 5의 병렬 전사보다 반드시 먼저 오케스트레이터에서 통과돼야 한다**(공통 규율 8)
   5) 마스킹 사본만 비전으로 전사하고, 촬영 순서 x 명렬 순서(미제출자 제외)로 토큰을 배정한다. 검수 보고에서 인적 크롭과 토큰을 나란히 제시해 교사가 순서 밀림을 표본 대조할 수 있게 한다.
      **전사는 병렬(2-3의 규약을 그대로 따른다), 토큰 배정은 병렬 금지 — 이 둘을 반드시 분리한다.** 토큰 배정은 **전역 순서**에 걸려 있어서, 워커 4개가 비동기로 끝난 결과를 반환 순서대로 이어 붙이면 토큰이 통째로 한 칸씩 밀린다. 앞 묶음의 미제출자 1명이 뒤 전원을 미는 것도 같은 사고다. 그 결과는 오귀속 — 이 도구의 최악의 실패인데, **§7의 명렬 대조로는 절대 잡히지 않고**(명렬을 명렬로 검증하는 항진명제) `--expected-count`도 못 잡는다(밀려도 행 수는 같다). 그래서:
      - 워커는 토큰·명렬·미제출자 목록을 **모른다**. 파일명을 키로 전사 결과만 반환한다
      - 오케스트레이터가 전원 회수 후 **파일명 정렬로 촬영 순서를 결정론 재구성**하고, 그다음에 미제출자를 제외한 명렬 순서와 맞춰 토큰을 배정한다
      - 워커 중 하나라도 반환이 없거나 파일 수가 안 맞으면 배정하지 말고 그 워커 몫을 순차로 재실행한다(부분 배정 금지)
      - 배정 후에도 인적 크롭↔토큰 표본 대조(위 문장)를 생략하지 않는다 — 순서 밀림을 잡는 그물은 이것 하나뿐이다

9. 표본 학생 2~3명(수준이 달라 보이는)을 골라 원문을 읽고 활동 성격을 파악한다

## 2. 규칙 인터뷰 (질문은 반드시 하나씩)
| # | 질문 | 기본값 |
|---|---|---|
| 1 | 활동명과 문두 형식(`<활동명>에서` / `<활동명>을 통해`) | 없음(필수) |
| 2 | **교사 평가 자료의 위치**(채점표, 루브릭 점수, 관찰 기록) | **없음(필수) — 없으면 채점 완료를 안내하고 여기서 중단.** 채점표인 경우 파일 위치만 받고 내용은 열지 않는다 — 점수는 이후 `score` 명령의 산출물로만 받는다 |
| 3 | 목표 분량 | 700바이트(상한 760) |
| 4 | 활동 특수 참조자료(소재 교정 기준 목록 등) | 안내문에서 추출 후 확인 |
| 5 | 톤 차등(단계 수·점수 구간 매핑) | 교사 평가 점수에서 파생. AI의 조정 의견은 생성 전 질문으로. 이 구간 매핑은 `score` 명령이 산출한 토큰별 점수(`점수.json`)에 적용해 학생별 톤을 파생한다 |
| 6 | 예외 처리(빈 답안, 짧은 답안, 목록 외 소재) | 빈=참여 사실만, 짧음=짧게, 목록 외=확인 요청 |
| 6-1 | **금지 어휘·특수문자 확인** — 씨앗 규칙(규칙.json의 금지어·금지문자패턴)을 보여주고 이 활동에 맞는지 확인. 과목 특성상 조정이 필요하면(수학의 부등호 등) **활동프로파일에 같은 키(`금지어휘`/`금지문자패턴`)로 기록** — 검증기가 그 활동에 한해 프로파일 값을 우선한다 | 씨앗 그대로. 씨앗은 원저자 취향이 기본값이므로 첫 회차엔 반드시 확인 |
| 7 | 학생별 교사 관찰 메모의 **파일 위치**(채점표의 메모 열 또는 별도 파일) | 없으면 없다고 답하고 진행. 메모를 대화로 불러 주시려 하면, 그 경우 실명이 전송된다는 점을 알리고 파일 작성을 먼저 권한다 |

- **7문항을 순서대로 빠짐없이 진행한다. 기본값이 있는 문항(분량, 톤, 예외)도 건너뛰지 않는다** — "기본값 700바이트로 갈까요?"처럼 반드시 확인을 받고, 사용자의 응답 없이 기본값을 조용히 적용하지 않는다(특히 분량 문항 누락 사례가 보고됨)
- 위키에 같은 답이 있으면 질문을 생략하는 것이 아니라 "지난번과 같이 갈까요?" 확인으로 축소한다
- **인터뷰 완료 점검**: 구조 인터뷰로 넘어가기 전에 7문항 각각의 확정값이 활동프로파일에 기록되었는지 확인한다. 비어 있는 문항이 있으면 해당 문항으로 돌아간다
- **이 절 전체는 순차·병렬 금지.** 절 제목이 곧 근거다 — 질문은 반드시 하나씩이고, 서브에이전트에는 사용자에게 물을 채널이 없다. 확정값의 프로파일 기록도 오케스트레이터가 단독으로 한다(공통 규율 4)
- 확정값을 수행평가 폴더의 `활동프로파일.json`에 기록한다. 최소 키: `활동명`, `문두`, `목표바이트`, `상한바이트`, `평가자료`(출처와 확인 일자), `톤단계`, `예외방침`, `채택구조` — `평가자료`가 비어 있으면 검증기가 저장을 차단한다

## 3. 구조 심층 인터뷰 (하네스의 심장)
1. 표본 분석과 활동 성격을 근거로 [wiki/구조패턴.md](wiki/구조패턴.md)에서 후보 2~3개를 고른다(유사 회차 채택 구조를 1안으로). **순차** — 후보 조합을 고르는 전역 판단이다
2. **같은 표본 학생 1명의 원문으로 후보별 완성 세특을 만들어 나란히 시연한다**
   **병렬 — 후보 1개 = 서브에이전트 1개(2~3개).** 여기서 병렬의 목적은 시간이 아니라 **문체 누출 제거**다. 한 에이전트가 세 안을 연달아 쓰면 첫 안의 문장·리듬이 둘째·셋째로 새어 구조 간 차이가 흐려진다 — §5에서 부수 이점이던 문체 수렴이 여기서는 오염원이다. 이 단계는 "학생도 같고 쓴 주체도 같으니 남는 차이는 구조뿐"이어야 하는 통제 실험이므로, **각 워커에게 문체 관련 입력을 완전히 동일하게 준다**(같은 표본 원문, 같은 `wiki/표현사전.md`·`wiki/문체관찰.md`, 같은 분량·문두·금지어, 같은 톤 지시). 다른 것은 배정된 구조패턴 항목 하나뿐이어야 한다 — 입력이 워커마다 다르면 교사는 구조가 아니라 에이전트를 고르게 되고, 하네스의 심장이 거기서 멈춘다.
   워커는 파일을 쓰지 않고 세특 본문 1편만 반환한다(입력은 표본 학생 1명의 **토큰 본문**뿐 — 실명 파일이 브리프에 등장할 일이 없다). 계약 6줄 재주입. 회수 후 **후보 번호 순으로 정렬해** 나란히 제시한다(반환 순서로 늘어놓지 않는다)
3. 사용자가 선택하면 "고칠 요소가 있나요?" 조정 질문을 1회 한다. **순차** — 사용자 결정
4. 확정 구조(패턴 ID + 조정 내역)를 활동프로파일.json에 기록한다. **오케스트레이터가 단독으로 쓴다**(공통 규율 4)

## 4. 파일럿 배치 (생성 구간의 사람 개입 지점)
전체 생성 전에 소량으로 실전 문체를 확정받는다. 구조 시연(구조 고르기)과 목적이 다르다 — 여기서는 확정된 구조로 쓴 실제 초안의 문체·톤 수위·개요 방식을 확인받는다.

1. 톤 등급별 표본 3~4명(예: 상·중·중하 각 1명)의 세특을 먼저 완성한다
2. 완성본을 사용자에게 나란히 보여 주고 고칠 점을 확인받는다
3. 지적이 나오면 파일럿에 반영해 재확인받고, 확정된 방식을 나머지 전원에게 적용한다
4. **파일럿 승인 없이 배치 생성을 시작하지 않는다** — 배치 후 발견된 문체 오류는 전원 재작성 비용이 된다
5. **이 절 전체는 순차·병렬 금지(§3-2와 겉보기만 같고 반대다).** §3-2는 학생을 고정하고 구조를 바꾸므로 항목 간 오염이 해롭지만, §4는 구조를 고정하고 **톤 등급**을 바꾸므로 항목 간 **대비가 필수**다. 산출물이 세특 3~4편이 아니라 "상은 이만큼, 중은 이만큼"이라는 **눈금**이고 눈금은 나란히 놓아야만 생긴다 — 워커가 서로를 못 보면 각자 자기 등급의 절대 해석으로 최선을 다해 써서 간격이 무너지고, 교사는 "톤 차이"와 "에이전트 문체 차이"를 분리하지 못한 채 승인하게 된다. 그렇게 승인된 어긋난 기준선은 3-4에 따라 **나머지 전원에게 전파**되므로 손상이 표본 4명이 아니라 전원에게 간다(톤 판정의 주권은 교사에게 있다는 불변 원칙 침해). 게다가 표본 3~4명은 §5 묶음(4~6명)보다 작아 애초 1턴이라 줄일 벽시계도 없다

## 5. 생성 루프
**파일럿 승인 후의 본 생성은 학생 1명=1턴이 아니라 4~6명 묶음=1턴으로 진행한다.** 묶음화의 이유는 토큰 재소비 절감이다 — 학생 1명=1턴 구조에서는 기반(SKILL.md+지시 ≈2.8만 토큰)이 학생 수만큼 재소비되므로, 묶음으로 턴 수를 줄이면 품질 손실 없이 명목 토큰을 크게 낮춘다(이 근거를 아는 개정자는 이 절을 학생 1명=1턴으로 되돌리지 않는다).
- 묶음 안에서도 학생별 절차는 각각 그대로 지킨다: 원문 읽기 → 개별 강점 추출 → 톤 판정 → 확정 구조와 [wiki/표현사전.md](wiki/표현사전.md)·[wiki/문체관찰.md](wiki/문체관찰.md)를 적용해 작성 → 짝 기록. 묶음이라고 학생별 원문 대조나 짝 데이터를 건너뛰지 않는다
- 묶음 생성의 부수 이점: 한 턴 안에서 직전 학생들의 세특이 함께 보이므로 문장·표현 중복을 즉시 피하고 학생 간 개별성을 대조하며 쓰기 쉽다. 이 대조를 살리려면 **소재(핵심 작품·소재)가 같은 학생들을 같은 묶음에 배치한다** — 같은 소재를 다루는 학생끼리 나란히 놓아야 "무엇이 이 학생만의 것인가"가 드러나 개별성 확보가 쉬워진다
- **본 생성은 병렬 금지 — 묶음 순차가 확정 설계다.** 같은 작품을 고른 학생끼리 문장이 겹치지 않으려면 **앞서 쓴 세특이 컨텍스트에 있어야** 하는데(바로 위의 개별성 요건), 워커는 서로의 산출을 볼 수 없다. 문체 수렴도 같은 연속성에 의존한다. **학급 단위 병렬(3학급을 3워커로)도 금지** — 같은 소재를 고른 학생은 학급 경계를 넘어 존재하므로 학급으로 잘라도 개별성 요건은 잘리지 않는다. 실행 전 점검 7의 반 단위 분할은 컨텍스트 한도 대책이지 병렬 설계가 아니다(**배치와 병렬은 다른 축이다** — 묶음 크기를 키우는 것과 워커를 늘리는 것을 혼동하지 않는다)
- 표절 의심 보고도 **병렬 금지** — 전 학생 교차 비교라 분할하면 다른 조각의 학생과 겹치는 답안을 영원히 못 잡는다. 짝 기록은 생성과 같은 턴에서만 정확하다
- 학생별로: 원문 읽기 → 개별 강점 추출 → 톤 판정 → 확정 구조와 표현사전·문체관찰을 적용해 작성.
- `관찰메모.json`에 해당 학생 토큰의 메모가 있으면 산출물에는 없는 교사만의 관찰로서 그 학생 세특에 결합한다 — 없으면 원문만으로 작성한다
- 소재가 참조 목록과 불일치하면: 내용 근거로 교정을 제안하고 사용자 확인 후 비고에 기록
- 예외 학생은 인터뷰에서 확정한 방침대로 처리하고 `"예외": true`로 표시
- 서로 다른 학생의 원본이 실질적으로 동일하면(표절 의심) 세특 작성과 별개의 평가 사안이므로 교사에게 보고한다
- **근거 짝을 초안에 싣는다**: 세특의 핵심 서술마다 원문 근거 구절을 짝지어 학생 항목에 `"짝": [{"n":1, "세특":"<세특 구절>", "원문":"<원문 구절>"}]`로, 원문에서 근거를 찾지 못한 서술은 `"짝없음": ["<세특 구절>"]`로 기록한다(구절은 양쪽 본문에 실제로 부분일치해야 화면에 그려진다). 이 데이터가 검수 화면의 짝 표시(①②·근거 못 찾음)가 되므로 생략하면 검수 도구의 핵심 장치가 꺼진다
- 민감정보(건강·가족사 등)가 담긴 서술이 보이면 생성 전에 대상 학생(토큰)과 사유를 로컬 화면에 목록으로 알린다. **전송을 차단하지는 않으며**, 경고 상세는 매핑표와 함께 파기 대상이다

## 6. 검증 루프 (2단)
1. 초안을 `초안.json`으로 만들고 결정론 채점기를 실행한다. 명렬이 있으면 `--roster 명렬.json`을 반드시 붙인다(학번·이름 대조와 미제출자 감지):

초안.json 계약(키 이름을 그대로 사용):
```json
{"classes": [{"name": "1반", "students": [{"학번": "10101", "이름": "김가상", "핵심소재": "가상의 책(작가)", "톤등급": "중", "세특": "...", "비고": "", "예외": false}]}]}
```
예외 학생(빈 답안 등)은 "예외": true로 표시한다.
   ```
   python <스킬경로>/scripts/verify_seteuk.py 초안.json --profile 활동프로파일.json --roster 명렬.json
   ```
   **1·2는 병렬 금지(전체 1회 실행).** `verify_seteuk.py`는 전역 게이트를 갖는다 — 미제출자 감지(`--roster`), `--expected-count` 대조, 제출 대조. **분할 실행하면 이 셋이 전부 무력화된다**(조각마다 인원이 달라 카운트 대조가 성립하지 않는다). 게다가 순수 파이썬이라 48명이 수 초다
2. FAIL이 있으면 해당 학생만 수정하고 재실행한다(수정 시 원문 근거는 삭제하지 않고 수식어부터 줄인다)
3. 결정론 통과 후 [wiki/루브릭.md](wiki/루브릭.md) 기준으로 LLM 채점을 한다(생성과 분리된 검증 관점으로, 원본과 세특을 대조). 이 대조도 §5와 같은 크기(4~6명) 묶음=1턴으로 진행해 기반 재소비를 줄인다 — 단 **검증 묶음의 경계를 생성 묶음과 어긋나게 짠다**. 같은 묶음을 같은 눈으로 두 번 보면 검증 독립성이 약해지므로, 생성이 1–5번 묶음이었으면 검증은 3–7번, 다음은 8–12번 식으로 어긋나게(overlap-shifted) 나눠 어떤 학생도 자기 생성 묶음과 동일한 이웃 구성으로 검증되지 않게 한다.
   **병렬 — 서브에이전트 3~4개(학생 24명 이상일 때).** 이 절의 설계가 이미 병렬을 전제한다: 묶음끼리 서로 볼 필요가 없고, "같은 눈으로 두 번 보지 말라"는 요구는 별개 워커가 순차보다 오히려 잘 만족시킨다(워커는 서로의 채점을 물리적으로 볼 수 없다). 채점은 학생 1명 안에서 닫히므로 상호 의존이 없고, 이 시점은 `finalize` 이전이라 초안이 **토큰 상태**다.
   - **분할**: 어긋난 묶음 단위를 **깨지 않고** 배분한다(묶음 경계를 워커 경계에 맞춰 다시 자르면 overlap-shift가 무너진다). 워커당 묶음 2~3개 = 8~15명
   - **입력**: `wiki/루브릭.md` + `wiki/규칙.json` + 프로파일의 분량·문두·금지어 + 해당 학생들의 **토큰 본문·토큰 세특**. 실명 파일 경로가 브리프에 등장하지 않는다. 계약 6줄 재주입
   - **채점 기준 고정(필수)**: 워커마다 루브릭을 새로 해석하면 같은 세특이 워커 A에선 PASS, B에선 FAIL이 되고, 그 노이즈가 §8-1 우선 검토 목록(=교사의 주의 배분)을 오염시킨다. **오케스트레이터가 판정 기준을 몇 줄로 압축해 전 워커에게 동일 문구로** 준다 — 항목별 PASS/FAIL 경계, FAIL 사유 표기 어휘, "애매하면 FAIL이 아니라 `판정보류`로 반환하고 사유를 적는다"까지. 판정보류는 오케스트레이터가 모아 한 눈으로 다시 본다
   - **반환**: `{워커ID, 토큰별 PASS/FAIL/판정보류 + 사유}`만. 워커는 파일을 쓰지 않고 세특을 고치지도 않는다(재작성은 4의 일이다). 토큰을 키로 병합하므로 반환 순서는 결과를 바꾸지 않는다
4. 루브릭 FAIL은 **해당 학생만 1명씩** 사유를 피드백으로 재작성한다 — 묶음 단위 재생성은 금지한다(같은 묶음의 통과한 학생을 다시 건드리면 안 된다). **학생당 재작성 상한 3회**, 초과 시 비고에 '검토 필요'를 기록하고 다음 학생으로 진행한다.
   **조건부 병렬 — 소재가 서로 다른 FAIL 학생끼리만 동시 2~3개.** 재작성은 곧 재생성이라 §5의 개별성 요건이 되살아나지만, 그 요건은 **같은 소재를 고른 학생 사이**에서만 실효가 있다. 같은 소재 FAIL이 둘 이상이면 그 그룹은 **순차**로 한다. FAIL이 5건 미만이면 그냥 순차(이득이 재적재 비용에 먹힌다).
   **재작성 횟수는 오케스트레이터가 센다.** 이 상한은 코드에 카운터가 없고 순차 컨텍스트의 기억으로만 유지돼 왔다 — 병렬에서 학생이 워커를 옮기면 카운터가 0으로 리셋되어 무한 재작성이 되거나, 더 나쁘게는 3회를 넘겼는데 '검토 필요'가 안 찍혀 **교사가 봐야 할 학생이 §8-1 우선 검토 목록에서 빠진다.** 그래서 오케스트레이터가 `{토큰: 재작성횟수}`를 자기 컨텍스트에 명시적으로 들고 있다가 매 배분 전에 확인하고, 워커에는 "이번이 N회차"를 알려 준다. 워커는 카운터를 관리하지 않는다.
   **재작성 후에는 반드시 1의 결정론 검사를 전체 재실행한다**(부분 재검증 금지)

## 7. 저장 게이트
**이 절 전체는 순차·병렬 금지.** 입력이 명렬·매핑이고(공통 규율 2의 범위 밖), `finalize`의 1:1 복원 검사와 `--expected-count`·`--submitted` 대조가 전부 **전역 게이트**라 분할하면 무력화된다 — 특히 `--expected-count`는 "산출물을 보기 전에 미리 정해 둔 값"이라는 외부성이 생명인데, 조각별 기대 인원을 새로 만들어 내는 순간 그 외부성이 붕괴한다. 프로파일의 `인원` 기록도 여기서 오케스트레이터가 한다.
전원 처리 후 **재결합을 먼저 한다**. 확정된 토큰 초안(`{"classes":[{"name","students":[{"토큰",...}]}]}`)을 대상으로 CLI를 실행한다:
```
python <스킬경로>/scripts/pseudonymize.py finalize 토큰초안.json --roster 명렬.json --mapping 매핑.json --out 초안.json
```
CLI가 내부에서 토큰을 학번으로 되돌리고(`reidentify`) 명렬에서 학번에 대응하는 이름을 붙여 초안.json을 실명 상태로 만든다. 매핑에 없는 토큰이 있으면(1:1 복원 실패) 저장하지 않고 exit 1로 차단한다. stdout의 "재결합: N명 복원 완료"만 확인한다 — **명렬 파일과 매핑표를 에이전트가 직접 읽지 않는다.**
전원 처리 후 `--save`로 재검증과 저장을 한 번에 실행한다. **`--roster`를 반드시 함께 붙인다** — 없으면 본문에 남은 학번을 잡는 최후의 게이트(ID_IN_BODY)가 실행되지 않는다.

**명렬↔이름 대조는 오귀속을 증명하지 못한다.** 초안의 이름은 바로 위 `finalize`가 이 명렬에서 가져다 붙인 것이므로, 그 이름을 다시 같은 명렬과 비교하면 명렬을 명렬로 검증하는 항진명제가 된다 — 명렬 자체가 잘못됐을 때(이름 열이 한 칸 밀리는 등) 반 전체가 남의 세특을 받아도 이 대조는 100% 통과한다. 그래서 `--save`에는 명렬·초안 어느 쪽에서도 파생되지 않은 외부 정보가 **반드시** 하나 있어야 한다:
- **`--expected-count N`(핵심)**: 교사가 아는 이번 활동의 실제 대상 인원. 학급 정원에서 전출·자퇴를 뺀 수 등, **산출물(초안·명렬)을 보기 전에** 미리 정해 둔 값이어야 한다 — 산출물을 본 뒤에 인원을 맞춰 넣으면 이 게이트가 무력화된다. 저장될 행 수와 다르면 저장을 거부하고 차이만 보여준다(이름·학번은 출력하지 않는다). 한 번 넣으면 활동프로파일의 `인원`에 기록되어 다음 회차부터는 생략할 수 있다 — 재사용 시에도 그 값이 stdout에 표시되니 여전히 맞는 인원인지 확인한다.
- **`--submitted 제출자.json`(선택, 권장)**: 명렬과 독립된 경로로 만들어진 제출자 학번 목록(또는 매핑.json처럼 학번이 키인 파일). 초안에는 있는데 여기 없는 학번은 FAIL(제출 안 한 학생에게 세특이 생성된 것), 반대로 여기 있는데 초안에 없는 학번은 WARN(누락 후보)이다. 생략하면 저장은 진행되지만 "제출 대조를 건너뛰었다"는 문구가 stdout에 반드시 남는다.
```
python <스킬경로>/scripts/verify_seteuk.py 초안.json --profile 활동프로파일.json --roster 명렬.json --submitted 제출자.json --save "세특(초안).xlsx" --expected-count 48
```
FAIL이 하나라도 있으면 스크립트가 저장을 차단한다. 우회 저장을 하지 않는다.
본문에 남은 급우 식별자로 ID_IN_BODY나 이름 혼입 FAIL이 나면, 학번이나 이름으로 되돌리지 말고 식별자 없는 표현(예: 급우, 동료 학생)으로 그 문장을 재서술한 뒤 재검증한다.
핵심소재가 빈 학생은 저장 직전 세특 본문의 첫 작품명(작은따옴표 안 내용)으로 자동 기입된다. 교사가 이미 채운 값은 절대 덮어쓰지 않으며, 작은따옴표가 없는 예외 답안은 빈칸으로 남는다. stdout의 "핵심소재 자동 기입: N명(빈칸 M명은 그대로)." 로 확인한다.
저장되는 파일의 첫 시트에는 `안내` 시트(AI 초안 — 교사 검수 전, NEIS 입력 금지)가 자동 포함된다. 이 표시는 회고(검수 완료) 전까지 제거하지 않는다.
검수는 브라우저(검수.html)가 기본이므로 검수용 xlsx 사본은 만들지 않는다. 교사가 엑셀 검수를 원할 때만 `(초안)`을 `(검수용)`으로 바꾼 복사본을 만들어 주고, 초안 파일은 회고 대조의 기준선이므로 수정하지 않고 보존한다고 안내한다.
사본의 각 시트 마지막 열에 '검수 메모' 열을 추가한다. 선택 입력 채널이다 — 사용자가 수정 이유를 한마디 적으면 회고가 의도를 추론하지 않고 그대로 학습하고, 비워 두면 diff에서 추론한다.
바이트수 열은 정적 값이 아니라 엑셀 수식 `=(LENB(세특셀)-LEN(세특셀))*2+LEN(세특셀)`로 저장된다(검증기가 자동 삽입) — 교사가 검수 중 문장을 고치면 바이트가 실시간 재계산되어 분량 조절이 수월하다.

## 8. 검수 보고
**이 절 전체는 순차·병렬 금지.** 이유 셋: (1) 우선 검토 대상 분류는 **이미 결정론 코드가 한다**(`review-bundle`이 예외 학생·비고 키워드·바이트 경계·미매핑을 기계로 붙인다) — 여기에 워커를 붙이는 것은 있는 결정론 게이트를 비결정론으로 갈아 끼우는 순손해다, (2) `검수번들.json`·`검수.html`·`검수본(실명·보관용).json`이 전부 실명 산출물이라 공통 규율 2의 범위 밖이다, (3) 확인 필요 목록·미제출자 명단은 전역 목록이라 분할하면 누락이 곧 실패다. 학급별 검수를 첫 학급부터 차례로 하는 것도 **사람의 주의를 직렬화하려는 설계 의도**이므로 학급 병렬로 바꾸지 않는다.
- **우선 검토 대상을 이유와 함께 명시한다**: 전사 신뢰도 낮음, 톤 조정 학생, 루브릭 재작성 발생, 소재 교정, 답안 끊김·짧음 등 판단이 필요한 학생을 먼저 제시하고, 나머지는 표본 검토를 안내한다. 전건 균등 검토는 전건 대충 검토가 되기 쉬우므로 교사의 주의를 판단이 필요한 곳에 배분한다
- 확인 필요 목록(빈 답안, 소재 교정, 추정, 검토 필요)을 절대 생략하지 않고 보고한다
- **미제출자 명단을 반드시 포함한다** — 원본이 없어 세특이 생성되지 않은 학생을 조용히 누락시키지 않는다
- 초안은 교사 검수·승인 전에는 NEIS에 입력하면 안 된다는 점을 보고에 명시한다(허위·과장·기재요령 준수의 최종 확인 책임은 교사에게 있음)
- 사용자에게 수정은 반드시 검수용 사본에서만 하도록 안내한다(초안은 비교 기준선)
- 학급이 여러 개면 첫 학급 검수 후 나머지를 진행하는 흐름을 기본으로 제안한다

**브라우저 검수 흐름**: 위 보고 직후 검수 화면을 만든다. 순서는 review-bundle → make_review_html → 교사가 검수.html에서 작업 → `검수diff(AI용).json`만 회수, 네 걸음이다.

1. 검수번들 생성:
```
python <스킬경로>/scripts/pseudonymize.py review-bundle --drafts 초안.json --mapping 매핑.json --sources 토큰본.json --profile 활동프로파일.json --out 검수번들.json
```
`--sources`(토큰본.json, 있으면 학생 원문을 함께 실어 나란히 검수 가능)와 `--profile`(없으면 700/760 기본값)은 선택이다. stdout의 "검수번들: N명(우선 검토 M명, 원문 포함 K명)"만 확인한다 — **산출물인 검수번들.json 자체는 실명을 담으므로 로컬 전용**이며, 마지막 줄의 민감 산출물 경고를 사용자에게 그대로 전달한다.

2. 검수 화면 생성(번들을 HTML에 심는다 — file:// 페이지는 옆 파일을 못 읽으므로 데이터를 심은 단일 파일이 유일한 길이다):
```
python <스킬경로>/scripts/make_review_html.py 검수번들.json
```
번들 옆에 `검수.html`이 생긴다(`--out`으로 경로 지정 가능). stdout의 "검수 화면: N명(우선 검토 M명, 원문 포함 K명, 짝 표시 P명)"만 확인하고, 이어지는 두 안내("이 파일에는 실명이 들어 있습니다 — 활동 폴더에만 두세요.", "창을 닫으면 고치던 내용이 사라집니다. 저장 단추를 눌러야 파일로 남습니다.")를 사용자에게 그대로 전달한다. 검수.html은 실명 산출물이라 파기 그물(검수*.html)에 걸린다.

3. 사용자가 `검수.html`을 더블클릭으로 열어 검수한다. 파일 불러오기 없이 바로 뜨고, 우선 검토 대상이 위로 정렬되며, 학생마다 원문·세특이 나란히 나오고, 고치는 대로 바이트·금지어가 표시된다. 검수 메모 한 줄을 적으면 회고가 의도를 추론하지 않고 그대로 학습한다.

4. 검수 완료 후 저장 단추는 하나다 — 누르면 화면이 `검수diff(AI용).json`(이름·학번 없음)을 먼저, `검수본(실명·보관용).json`(로컬 보관)을 이어서 저장한다(순서는 코드가 지키므로 중간에 취소돼도 실명 파일만 남는 일이 없다). **하네스가 회수하는 것은 `검수diff(AI용).json` 하나뿐이며, 교사가 올리는 것이 아니라 에이전트가 활동 폴더에서 직접 읽는다** — 이 파일은 토큰뿐이라 열어도 계약 위반이 아니다. 교사가 「검수 끝났어」라고 말하면 활동 폴더에서 찾고, 없으면 **내려받기 폴더(`%USERPROFILE%\Downloads`)에서 직접 찾아 읽는다** — diff는 실명이 없으므로 어느 위치든 에이전트가 읽어도 된다. 교사에게 파일을 올리라고 하지 않는다(어떤 경우에도 첨부를 청하지 않는다 — 두 폴더 모두에 없으면 저장이 실행됐는지부터 확인한다). **검수본(실명·보관용).json은 같은 폴더에 있어도 절대 열지 않는다.** 회고(§9)는 이 diff를 입력으로 진행한다.

`tools/도구.html`의 검수 탭은 비상용이다 — 검수.html 생성이 불가능하고(파일이 전혀 읽히지 않는 환경) 클립보드도 막힌 환경에서만 안내하며, 기본 동선에서는 언급하지 않는다.

## 9. 회고 (사용자가 검수 수정본을 주면)
1. 초안 파일의 수정 시각을 확인한다. 저장 게이트 실행 이후 초안이 수정된 흔적이 있으면 기준선 오염 가능성을 사용자에게 경고하고 어느 파일이 기준인지 확인한다
2. 초안과 검수용을 행 순서가 아니라 학번 키로 매칭해 diff를 만든다(사용자가 행을 정렬·삭제해도 비교가 깨지지 않게)
3. **무수정 감지**: diff가 0건이면 초안이 완벽했을 가능성과 검수가 충분하지 않았을 가능성을 함께 짚고, 우선 검토 대상을 확인하셨는지 물은 뒤 진행한다(비난이 아니라 확인 — 도장 찍기 방지 장치)
4. 검수 메모가 적힌 행은 메모를 수정 의도로 그대로 학습하고, 메모가 없는 행만 diff에서 반복 패턴을 추론한다.
   - **메모 행 학습은 조건부 병렬 — 메모 행이 20건 이상일 때만 서브에이전트 2개.** 메모는 "그대로 학습"이라 추론이 아니라 **전사**이고 행 간 의존이 없다. 입력은 `검수diff(AI용).json`(토큰뿐이라 계약 안). 20건 미만이면 순차
   - **메모 없는 행의 패턴 추론은 병렬 금지.** 반복 탐지는 **빈도 기반**이라 diff 전체를 한 눈이 봐야 한다. 4조각으로 나누면 전체에서 4회 나타난 패턴이 조각마다 1회씩 흩어져 어느 워커도 패턴으로 승격하지 못하고, **워커 전원이 "패턴 없음"을 성공적으로 보고한다** — 손실이 조용해서 더 나쁘다
   - 패턴이 **이미 확정된 뒤의** 근거 수집·문안화는 패턴별로 닫히므로 패턴이 6개 이상이면 2~3개로 나눠도 된다(그 미만은 순차)
5. 패턴별로 "규칙으로 반영할까요?"를 묻는다. **순차** — 사용자 승인이라 워커에 넣으면 워커가 대신 결정한다(공통 규율 3)
6. **성장은 씨앗과 분리해 기록한다.** 승인된 학습은 tracked 씨앗 파일(구조패턴·규칙정본·표현사전·문체관찰)에 덮어쓰지 않고, gitignore되는 `wiki/성장/`의 대응 파일(사용실적·규칙개정·표현이력·문체관찰)에 회차별로 덧붙인다. 씨앗에는 특정 회차·활동명·집계 건수를 적지 않는다. 규칙 개정 시 기계 정본 `wiki/규칙.json`은 예외로 씨앗과 함께 갱신하되 개정 이력(일자·사유·회차)은 `wiki/성장/규칙개정.md`에 남긴다. 씨앗을 직접 손대는 경우는 오직 하나 — 특정 학급이 아니라 모든 교사의 기본값을 바꿀 보편 개선일 때이며, 이때도 사용자 승인 후에만 한다. 승인된 것만 기록한다: 문체 경향→[wiki/문체관찰.md](wiki/문체관찰.md), 표현 승격·강등→[wiki/표현사전.md](wiki/표현사전.md), 규칙 개정→[wiki/규칙정본.md](wiki/규칙정본.md)(개정 이력 포함), 검수 지적 유형→[wiki/루브릭.md](wiki/루브릭.md). **금지 어휘·특수문자·분량 규칙을 개정할 때는 기계 정본인 `wiki/규칙.json`을 반드시 함께 갱신한다**(검증기는 이 파일을 읽는다). **6~8은 병렬 금지** — 승인된 학습 하나가 사용실적·규칙개정·표현이력·문체관찰에 걸쳐 기록되고 규칙 개정 시 `wiki/규칙.json`까지 함께 갱신해야 하는 **파일 4~5개짜리 한 트랜잭션**이라, 워커별로 쪼개면 부분 커밋이 남는다. 특히 `규칙정본.md`만 고쳐지고 `규칙.json`이 안 고쳐지면 **검증기가 다음 회차를 옛 규칙으로 채점한다**
7. 이번 회차를 `wiki/활동이력/YYYY-MM-활동명.md`로 기록한다(`_템플릿.md` 형식, 익명 통계만)
8. [wiki/구조패턴.md](wiki/구조패턴.md)의 채택 패턴에 사용 실적을 추가한다
9. 회고를 마친 활동에 한해, 사용자가 요청하면 확정본을 만든다: 검수 반영본에서 `안내` 시트를 제거하고 파일명을 `(확정)`으로 저장. **회고를 거치지 않은 활동의 확정본은 만들지 않는다** — 확정은 교사의 검수가 완료되었다는 선언이다
10. 확정본 발행 후 파기한다. **함수를 직접 호출하지 말고 CLI를 쓴다** — `python <스킬경로>/scripts/pseudonymize.py destroy --dir <활동폴더>`로 먼저 대상 목록을 확인해 사용자에게 보이고, 승인받은 뒤 `--yes`를 붙여 실행한다. 매핑표뿐 아니라 명렬·제출자·관찰메모·점수·점수원본·토큰본을 함께 파기하며, 파기 후 남은 파일이 있으면 exit 1로 알린다. **destroy는 가명처리 중간 산출물만 지운다** — 실명이 든 초안.json·학생 답안 원본·확정본 xlsx는 교사의 기록물이라 지우지 않으므로, 파기 보고 때 "답안 원본과 초안은 선생님 파일이라 그대로 두었습니다"를 함께 알린다. 위키 활동이력에는 '민감 경고 N건' 같은 익명 통계만 남긴다(파기 = 지정 경로 파일 부존재 확인 수준). **이번 회차에 병렬을 썼다면 파기 전에 워커 전용 산출물(`추출_1.json`·`토큰본_1.json` 등)이 전부 활동 폴더 안에, 파기 그물 글롭에 걸리는 이름으로 있는지 확인한다** — `destroy`는 지정 폴더 비재귀라 워커가 다른 곳에 쓴 파일은 목록에도 오르지 않는다(공통 규율 4)

# 질문 규율
- 묻지 않는다: 파일에서 파생되는 값, 규칙정본에 있는 것, 위키로 답할 수 있는 것(확인만), 학생별 개별 승인(도장 찍기 유발)
- 묻는다: 인터뷰 7문항, 구조 시연 선택, 파일럿 배치 승인, 톤 조정 승인, 원본 모호성, 3회 검증 실패 처리, 무수정 검수 확인, 회고 반영 승인

# 완료 조건
- 전원 결정론 검증 PASS(예외·검토 필요는 비고에 명시)
- xlsx 저장 완료
- 확인 필요 목록 보고 완료
