# B · 단일 세로 흐름

> 경로 주의: 과제문의 `undefined/`는 치환 실패다. 실제 대상은
> `C:\Users\admin\.claude\skills\seteuk-harness\` 로 확인해 진행했다.
> 이 회차는 설계 문서만 만든다. 코드는 건드리지 않았다.

---

## 0. 한 문장

탭을 없앤다. 페이지는 위에서 아래로 한 번 내려가는 하나의 길이고,
**지금 어디인지는 스크롤 위치가 말한다.**

---

## 1. 이 방향의 논거

### 1.1 탭이 숨기는 것은 화면이 아니라 경계다

`도구.html`이 탭으로 나눈 두 화면은 서로 다른 기능이 아니다. **같은 하나의
파이프라인의 앞과 뒤**다. 탭 B는 탭 A가 만든 것의 결과를 받는다. 그런데 탭은
그 인과를 화면에서 지운다. 교사는 탭 A만 보고 "이게 전부"라고 믿고,
30분 뒤 누가 "검수 탭으로 가세요"라고 말해 줘야 나머지 절반을 안다.

진단이 F14로 잡은 것이 정확히 이것이다. 이탈처가 하필 PRODUCT.md가 지목한
반참조(붙여넣고 쏟아지는 웹 서비스)다. **탭은 이 도구가 한 번에 끝나지
않는다는 사실을 숨기는 장치로 작동하고 있다.**

### 1.2 가장 강한 논거 — 첫 페인트에 도착지가 보인다

이 도구의 존재 이유 중 하나는 "어떤 파일에 실명이 들었고 무엇을 AI에게
주는가"다(설계 원칙 3). 탭 구조에서 그 경계는 **탭 B의 저장 버튼을 누른
뒤에야** 처음 등장한다. 이미 만들어진 다음이다.

단일 흐름에서는 페이지를 여는 순간, 아무것도 붙여넣기 전에, 화면 아래쪽에
두 개의 빈 상자가 보인다.

```
이 컴퓨터에만 두는 것        AI에게 주는 것
(아직 없음)                  (아직 없음)
```

**경계를 파일이 생기기 전에 배운다.** 이건 탭으로는 구조적으로 불가능하다.
탭 A에 저 상자를 놓으면 탭 A의 이야기가 아니고, 탭 B에 놓으면 30분 늦다.

### 1.3 "화면 자체가 순서"(설계 원칙 5)의 유일한 문자적 구현

원칙 5는 은유가 아니라 지시문으로 읽어야 한다. 세로 순서가 곧 시간 순서인
화면에서, 다음 칸은 "아래"다. 설명이 필요 없다. 탭은 순서가 아니라 **선택지**를
제시하는 컨트롤이고, 순서를 표현하려면 별도의 안내 문구가 더 필요해진다.

### 1.4 브라우저를 두 번 여는 사실과 충돌하지 않는다

워크플로 진단은 "브라우저 2회는 옳다, 합치지 않는다"고 결론했다. **이 안은
그 결론을 지킨다.** 두 번 여는 것은 브라우저이지 화면이 아니다. 두 번째로
열었을 때 페이지는 비어 있고, 앞부분은 3~4줄 높이로 접혀 있으며, 검수는
파일 하나만 열면 즉시 시작된다(§5.1). 탭을 없애는 것과 세션을 합치는 것은
다른 일이다.

### 1.5 카드 41장이 사라진다

UI 진단의 P1 "카드 남용(탭 A 4개·탭 B 40개 동일 형태)"은 탭 구조의 부작용이
아니라 **한 화면 안에 무엇이든 담으려면 상자가 필요하다**는 압력의 결과다.
세로 흐름은 그 압력을 없앤다. 구역은 상자가 아니라 **가로선과 여백**으로
나뉘고(§7.4), 검수는 목록 한 줄 + 펼쳐진 하나(§5)다. 상자 44개 → 2개.

---

## 2. 화면 구조

### 2.1 1280px

```
┌──────────────────────────────────────────────────────────────────────────────┐
│  세특 작업 도구                                     ○ 저장 안 됨            │  ← 상단 줄(sticky, 44px)
├─────────────┬────────────────────────────────────────────────────────────────┤
│             │                                                                │
│  인원        │   ─────────────────────────────────────────────────────────    │
│  명단 ●     │   이 활동 대상은 몇 명입니까                                    │
│  점수        │   명단을 보기 전에 아시는 수를 적어 주세요. 정원에서 전출·      │
│  저장        │   자퇴를 뺀 수입니다. 이 수와 명단이 다르면 화면이 말합니다.    │
│             │                                                                │
│  ‥‥‥‥‥   │        [  48  ] 명                                             │
│             │                                                                │
│  검수        │   ─────────────────────────────────────────────────────────    │
│  검수 저장   │   명단 붙여넣기                                                │
│  정리        │   엑셀에서 학번 열과 이름 열을 함께 긁어(Ctrl+C) 붙여넣으세요.  │
│             │   따로 두 번 붙이면 행이 어긋나고, 어긋난 명단은 어떤 검사로도  │
│  (sticky,   │   잡히지 않습니다.                                             │
│   200px,    │   ┌────────────────────────────────────────────────────────┐   │
│   글자만)    │   │ 30101   김ㅇㅇ                                          │   │
│             │   │ 30102   이ㅇㅇ                                          │   │
│             │   └────────────────────────────────────────────────────────┘   │
│             │                                                                │
│             │   ─────────────────────────────────────────────────────────    │
│             │   확인                              ← 붙여넣은 뒤에만 존재     │
│             │   …(§4)                                                        │
│             │                                                                │
│             │   ══════════════════════════════════════════════════════════   │
│             │   여기서 대화로 돌아갑니다                                      │
│             │   규칙·구조·표본을 정하는 20~60분이 지난 뒤, 이 페이지의        │
│             │   아래쪽으로 돌아오시면 됩니다. 창을 닫으셔도 됩니다.           │
│             │   ══════════════════════════════════════════════════════════   │
│             │                                                                │
│             │   ─────────────────────────────────────────────────────────    │
│             │   검수                                                         │
│             │   …(§5)                                                        │
│             │                                                                │
└─────────────┴────────────────────────────────────────────────────────────────┘
      200px                        본문 기둥 max 880px
```

본문 기둥 880px, 산문은 그 안에서 `--measure: 34rem`(≈40 한글자)로 더 좁게
간다. 검수만 880px 전폭을 두 열로 쓴다.

좌측은 **색인이지 스테퍼가 아니다.** 숫자 없음, 원 없음, 연결선 없음,
체크마크 없음. 글자 목록이고 현재 위치만 굵기로 표시한다. 마친 구역은
색이 아니라 위치로 안다(위에 있다).

### 2.2 좁은 창 (≤900px)

```
┌──────────────────────────────────────────────┐
│ 세특 작업 도구            명단 ▾   ○ 저장 안 됨│  ← 색인이 상단 한 줄로 접힘
├──────────────────────────────────────────────┤   (▾ 누르면 목록 펼침)
│                                              │
│  ────────────────────────────────────────    │
│  이 활동 대상은 몇 명입니까                   │
│  명단을 보기 전에 아시는 수를 적어 주세요.     │
│        [  48  ] 명                           │
│                                              │
│  ────────────────────────────────────────    │
│  명단 붙여넣기                                │
│  ┌────────────────────────────────────────┐  │
│  │ 30101   김ㅇㅇ                          │  │
│  └────────────────────────────────────────┘  │
│                                              │
│  ────────────────────────────────────────    │
│  확인                                        │
│  …                                           │
│                                              │
│  ══════════════════════════════════════════  │
│  여기서 대화로 돌아갑니다                     │
│  ══════════════════════════════════════════  │
│                                              │
│  검수 — 원문과 세특이 세로로 적층 (§5.3)      │
└──────────────────────────────────────────────┘
```

전환은 1120px에서 일어난다(rail 200 + 본문 880 + 여백). **구조적 전환만
하고 활자 크기는 건드리지 않는다**(product 레지스터: fluid typography 금지).

---

## 3. 첫 화면 — 아무것도 안 한 상태

교사가 더블클릭해서 처음 보는 것.

```
─────────────────────────────────────────────────────────────────
이 활동 대상은 몇 명입니까

명단을 보기 전에 아시는 수를 적어 주세요. 정원에서 전출·자퇴를 뺀
수입니다. 이 수와 붙여넣은 명단이 다르면 화면이 그 자리에서 말합니다.

        [      ] 명                    ← 로드 시 여기에 커서

─────────────────────────────────────────────────────────────────
명단 붙여넣기

엑셀에서 학번 열과 이름 열을 함께 긁어(Ctrl+C) 붙여넣으세요.
따로 두 번 붙이면 행이 어긋나고, 어긋난 명단은 어떤 검사로도
잡히지 않습니다.

┌───────────────────────────────────────────────────────────┐
│ 위에 인원을 먼저 적어 주세요.                              │  ← 비활성
│                                                           │     placeholder가
│ 30101   김ㅇㅇ                                             │     곧 안내
│ 30102   이ㅇㅇ                                             │
└───────────────────────────────────────────────────────────┘

─────────────────────────────────────────────────────────────────
점수 (선택)
채점표에서 학번 열과 점수 열을 함께 긁어 붙여넣으세요.
비워 두면 점수 없이 진행합니다.
(명단이 있어야 어느 열이 점수인지 알 수 있습니다.)

─────────────────────────────────────────────────────────────────
저장

  이 컴퓨터에만 두는 것            AI에게 주는 것
  아직 없음                        아직 없음
  실명과 학번이 들어갑니다.        이름도 학번도 들어가지 않습니다.

═════════════════════════════════════════════════════════════════
여기서 대화로 돌아갑니다
저장을 마치면 규칙·구조·표본을 정하는 대화가 20~60분 이어집니다.
그동안 이 창은 닫으셔도 됩니다. 끝나면 다시 열어 아래로 오세요.
═════════════════════════════════════════════════════════════════

─────────────────────────────────────────────────────────────────
검수

원문과 세특을 나란히 놓고 고치는 곳입니다.
대화에서 전체 생성이 끝나면 `검수번들.json`이 생깁니다. 그 파일이
있으면 여기가 채워집니다.

        [ 검수번들.json 열기 ]        ← 항상 누를 수 있다

─────────────────────────────────────────────────────────────────
정리

검수까지 마치면 실명이 든 중간 파일을 지웁니다.
대화창에 "중간 파일 파기해 줘"라고 말씀하시면 목록을 먼저 보여 드립니다.
```

### 3.1 이 화면이 가르치는 것 — 다섯

1. **한 번 내려가는 길이다.** 스크롤 한 번에 처음부터 끝까지가 다 있다.
   "이게 전부인가"를 물을 필요가 없다.
2. **먼저 묻는 것은 이미 아시는 수다.** 화면을 보기 전에 답하는 순서 자체가
   왜 그래야 하는지의 설명이다(워크플로 진단 §2 — 지금 `--expected-count`
   게이트는 교사가 화면에서 48을 본 뒤 48을 답하기 때문에 죽어 있다).
3. **두 열은 함께 긁는다.** 빈 상자의 placeholder가 두 열짜리 예시다.
4. **중간에 대화로 돌아간다.** 굵은 이중선이 길을 끊는다 — 이 도구가
   한 번에 끝나지 않는다는 사실이 화면의 물리적 사실로 존재한다.
5. **AI에게 줄 것은 아직 없다.** 빈 상자 두 개가 경계를 미리 가르친다.

### 3.2 넣지 않는 것

"처음이신가요?" 배너, 단계 투어, 첫실행 감지, localStorage.
전부 제품 슬롭이고, 마지막 것은 "아무것도 기억하지 않는다"는 약속을 깬다.
**비활성은 딱 하나 — 인원 입력 전의 붙여넣기 상자.** 그 외에는 어떤 것도
잠그지 않는다. 비어 있는 구역은 잠긴 게 아니라 **무엇이 있어야 채워지는지
한 문장으로 말하는 상태**다.

---

## 4. 붙여넣기 직후

Ctrl+V 순간 `확인` 구역이 180ms에 걸쳐 한 번에 나타난다. 위에서부터.

```
─────────────────────────────────────────────────────────────────
확인

▍ 48명을 읽었습니다. 적어 두신 48명과 같습니다.            ← ①  (본문 크기)

첫 줄을 이렇게 읽었습니다.                                  ← ②

        학번  30101
        이름  김ㅇㅇ

  [ 맞습니다 ]   [ 열이 바뀌었습니다 ]

────────────────────────────────────────────────
버린 행                                                    ← ③
학번이나 이름이 빈 행 2개를 건너뛰었습니다. (3행, 51행)
학번이 겹치는 행 1개가 있습니다. 여러 반을 함께 붙여넣었거나
출석번호를 학번으로 쓴 표일 수 있습니다.

학번 앞자리가 301·302 두 가지입니다. 두 반을 함께 붙여넣으셨습니까?

이 검사가 못 잡는 것 — 이름끼리 자기들끼리 뒤바뀐 명단.       ← ④
────────────────────────────────────────────────

  순번  학번     이름                                       ← ⑤
   1    30101   김ㅇㅇ
   2    30102   이ㅇㅇ
   3    30102   박ㅇㅇ   중복
   …
```

### 4.1 무엇이 먼저 눈에 들어오는가

**①의 대조 한 줄이다.** 본문 크기(17px), 본문 색, 굵기 600.
불일치면 같은 자리에서 문장이 바뀐다.

> **적어 두신 48명과 붙여넣은 47명이 다릅니다.**
> 명단에서 다시 세지 마시고, 어느 쪽이 맞는지 원본 표를 확인해 주세요.

배경 `--stop-ground`, 240ms 페이드 인, **사라지지 않는다.** 이 줄은
`aria-live="polite"`로도 읽힌다.

여기서 인원은 `명렬.json`의 `인원` 키로 저장되고, 에이전트는 나중에 묻지
않는다. 대화 왕복 1회가 사라지고 죽어 있던 게이트가 산다.

### 4.2 ② — F8을 여기서 죽인다

지금 화면은 `열 판별: 헤더 — 2번째 열을 학번으로 읽었습니다`라고 말한다.
교사는 자기 엑셀의 열 번호를 세지 않는다. 그래서 나이스 명렬
(`반│번호│학번│성명`)에서 **출석번호 열을 학번으로 잡아도** 그 문장은
읽히지 않고, 화면에는 `48명 · 중복 0 · 빈칸 0`이 초록으로 뜬다.

바꾼다. **열 번호를 말하지 않고, 그 열의 첫 행 실제 값을 보여 준다.**

```
        학번  1
        이름  김ㅇㅇ
```

`학번 1`은 아무 설명 없이도 틀렸다는 걸 안다. `[열이 바뀌었습니다]`를 누르면
붙여넣은 표의 첫 두 행을 그대로 보여 주고 열 머리를 눌러 지정하게 한다.

이 확인 블록은 **`via`가 헤더든 값 판별이든 항상 나온다.** 지금 1~2자리
경고가 `via === "값 판별"`일 때만 뜨는 헤더 경로 무경고 구멍이 닫힌다.

### 4.3 ③④ — 조용한 실패를 시각화

- **버린 행은 항상 보인다.** 0이어도 "버린 행 없습니다"라고 문장으로 쓴다.
- **0은 절대 초록으로 그리지 않는다.** 지금 `[dup ? "bad" : "", '중복 ${dup}']`은
  0일 때 클래스가 빈 문자열이 되어 `.chip` 기본 배경 `--okbg`(연녹)로
  **0을 성공색으로 렌더**한다(P0 #3). 새 규칙: **수치 0은 `--ink-3` 평문.
  성공색은 이 화면에 존재하지 않는다.**
- **중복 행은 글자로 표시한다.** 지금은 `tr.dup td{background:var(--badbg)}`
  뿐인데 대비가 라이트 1.129 : 다크 1.006이다. 다크에서는 **완전히 같은 색**이다.
  63%의 무성 실패가 바로 이 행이다. 새 규칙: 중복 행은 마지막 칸에 `중복`
  이라는 글자를 넣고, 배경은 보조로만 쓴다.
- **여러 반 섞임을 새로 잡는다.** 학번 앞 3자리로 묶어 종류가 2개 이상이면
  묻는다. 붙여넣기 경로의 "시트 여럿"에 해당하는 조용한 실패다.
- **"경고 0건"을 안전으로 읽히게 두지 않는다.** ④는 경고가 하나도 없어도
  항상 같은 자리에 있다. 이 도구가 무엇을 못 잡는지가 결과 화면의 상설 항목이다.

### 4.4 제출 여부 — F9의 순서 역전을 뒤집는다

지금은 미제출 체크박스가 답안·점수 파싱 **전에** 나오고 기본값이 전원 제출이다.
**기본값이 곧 오답**이고, 교사는 48개 체크박스를 훑어야 한다.

바꾼다. **명단 표에 제출 열이 처음부터 없다.** 점수를 붙여넣은 뒤에야 나타나고,
나타날 때 물음은 3명에 대한 것이다.

```
점수가 없는 학생 3명입니다. 미제출입니까?

   30117  최ㅇㅇ     [ 미제출 ]  [ 제출했음 ]
   30124  정ㅇㅇ     [ 미제출 ]  [ 제출했음 ]
   30131  한ㅇㅇ     [ 미제출 ]  [ 제출했음 ]

미제출로 표시하면 활동 참여 사실만 담은 예외 세특으로 처리됩니다.
```

기본값 없음. 판단 48회 → 3회. 점수를 안 붙이면 이 물음 대신
"점수를 붙여넣지 않으셨습니다. 전원 제출로 봅니다."가 한 줄 뜬다 —
가정을 침묵으로 두지 않는다.

---

## 5. 검수 화면

### 5.1 잠기지 않는다 — 비어 있고, 무엇이 필요한지 말한다

```
─────────────────────────────────────────────────────────────────
검수

대화에서 전체 생성이 끝나면 `검수번들.json`이 생깁니다.
그 파일이 있으면 여기가 채워집니다.

        [ 검수번들.json 열기 ]
```

버튼은 **언제나 활성**이다. 두 번째 세션(검수만 하러 온 방문)에서 위쪽
구역들이 미완이어도 상관없다. 파일을 열면 그 자리가 채워지고 화면이
그리로 스크롤한다. 이게 단일 흐름의 가장 약한 이음매이고, 이 버튼이
그 이음매의 값이다(§10.5에 정직하게 적는다).

### 5.2 40~50명의 피로 — 목록 한 줄 + 펼쳐진 하나

지금은 카드 40장이 동시에 펼쳐져 있다. 전건이 균등하게 보이면 **전건을
균등하게 대충** 보게 된다.

```
─────────────────────────────────────────────────────────────────
검수  ·  남은 12명 / 48명 중 36명 마침 · 수정 9건

먼저 볼 12명                                    ← 진짜 제목. 태그 아님.
─────────────────────────────────────────────────────────────────
 예외 학생          30117  최ㅇㅇ         704바이트
 분량 상한 근접     30104  강ㅇㅇ         758바이트
 비고에 '확인' 포함  30122  오ㅇㅇ         689바이트
 매핑에 토큰 없음   30131  한ㅇㅇ         —
 …

┌─ 열려 있는 한 명 ────────────────────────────────────────────┐
│  30104  강ㅇㅇ      분량 상한 근접 · 톤 상 · 소재 만복사저포기  │
│                                                              │
│  학생 원문                     │  세특                        │
│  ────────────                  │  ────                        │
│  「만복사저포기」에서 양생이     │  「만복사저포기」의 서사 구조를 │
│  죽은 여인과 사랑을 나누는       │  분석하며 산 자와 죽은 자의   │
│  장면을 읽으며 나는 …           │  경계가 …                    │
│  (17px / 행간 1.75)             │  (17px / 행간 1.75, 편집 가능)│
│                                │                              │
│                                │  758바이트 (NEIS 기준)        │
│                                │  상한 760 · 남은 2바이트      │
│                                │  금지: 이는                   │
│                                │  ────────────────────────    │
│                                │  왜 고쳤는지 한 줄            │
│                                ├──────────────────────────    │
│                                │  [ 마치고 다음 ⌃⏎ ]          │
└──────────────────────────────────────────────────────────────┘

 분량 목표 미달     30109  윤ㅇㅇ         402바이트
 …

나머지 36명                                     ← 접힌 목록
─────────────────────────────────────────────────────────────────
 30101  김ㅇㅇ   712바이트                수정됨
 30102  이ㅇㅇ   698바이트
 …

마친 12명 ▾                                     ← 기본 접힘
```

**한 번에 하나만 펼쳐진다.** 나머지는 한 줄짜리 색인이다. 카드 40장 → 1장.
읽기 피로는 "동시에 열려 있는 긴 글의 수"에 비례하므로, 이게 가장 직접적인
감소다.

### 5.3 원문과 세특

- **1280px: 좌 원문 / 우 세특, 각 424px.** 원문이 왼쪽인 이유는 세로쓰기가
  아닌 한글 가로 읽기의 기본 방향이고, 판단의 근거가 결론보다 먼저 오기 때문이다.
- **≤900px: 원문 위, 세특 아래.** 둘 다 전폭. **원문을 절대 세특보다 작은
  스크롤 상자로 만들지 않는다.** 지금은 `.source`가 13.92px이고
  `.tabs button`이 14.88px이다 — 학생이 오래 써 온 글이 UI 라벨보다 작다.
  PRODUCT.md "본문 크기·행간을 UI 라벨보다 우선한다"의 정면 역전이다.
- 둘 다 **같은 크기·같은 행간**(17px / 1.75). 대조가 목적이므로 활자가 달라지면
  안 된다. 다른 것은 배경뿐 — 원문은 `--surface-sunken`(읽기만), 세특은
  `--surface`에 아래쪽 실선 하나(편집 가능).
- 세특 입력칸은 상자가 아니다. **밑줄 한 줄**이다. 상자를 그리면 "폼"이 되고,
  이 화면은 폼이 아니라 원고다.

### 5.4 우선 검토를 어떻게 표시하는가

지금은 `.tag.flag`가 라이트 4.35:1 / 12.16px다 — **웹 세특 생성 서비스 결과
화면과 이 도구를 가르는 유일한 장치가 화면에서 가장 안 보이는 요소다.**

바꾼다. 세 겹으로.

1. **목록을 두 개로 쪼개고 진짜 제목을 붙인다.** `먼저 볼 12명` / `나머지 36명`.
   `<h2>`이고 본문 크기이며 굵다. 태그가 아니라 구조다.
2. **사유를 줄의 맨 왼쪽에 문장으로 놓는다.** `예외 학생`, `분량 상한 근접`.
   색이 아니라 위치와 낱말이 신호다. `--flag` 색은 보조로만 얹는다
   (라이트 6.23:1 / 다크 10.20:1).
3. **키보드가 우선순위를 따른다.** ⌃⏎(마치고 다음)는 `먼저 볼` 목록을
   먼저 소진한 뒤 나머지로 넘어간다. 그 전환에서 한 줄이 뜬다 —
   "먼저 볼 12명을 마치셨습니다. 나머지 36명으로 넘어갑니다."

### 5.5 키보드

| 키 | 동작 |
|---|---|
| `↑` `↓` / `J` `K` | 목록 이동(roving tabindex) |
| `⏎` | 펼치기 / 접기 |
| `⌃⏎` | 마치고 다음 |
| `Tab` | 열린 항목 안에서 세특 → 메모 → 마치고 다음 |
| `Esc` | 접고 목록으로 |

지금은 `role=` 0건, `tabindex` 0건, `role="tab"` 없는 `aria-selected`
(스크린리더가 무시하는 무효 ARIA), 40개 항목 사이 이동 수단은 순차 Tab뿐이다.
명시 요구사항 미충족이다. 목록은 `role="listbox"`+`option`이 아니라
**`<ul>` + roving tabindex**로 간다 — 표준 어휘를 재발명하지 않는다.

### 5.6 바이트는 하나만 보여 준다

지금은 `712 바이트(UTF-8)`와 `700 (2바이트 기준)`을 나란히 놓고,
헤더에 `(기준 확정 전)`이라고 적는다. 교사에게 이 괄호는 **"못 믿을 도구"**로
읽힌다. NEIS도 엑셀 수식도 2바이트 기준이다.

화면은 **한 숫자만** 쓴다. `758바이트 (NEIS 기준) · 상한 760 · 남은 2바이트`.
UTF-8은 화면에서 사라지고 `verify_seteuk`의 내부 검사로만 남는다.
(기준을 확정하는 것은 하네스의 일이다. 화면이 할 수 있는 기여는 **둘을
보여 주기를 거부하는 것**이다.)

---

## 6. 파일의 정체를 화면이 어떻게 설명하는가

### 6.1 현재의 문제

파일 이름이 나오는 곳은 저장 중 스치는 토스트와 **페이지 최하단 회색 단락**
뿐이다. 그 목록조차 `제출자.json`(학번 있음)이 빠지고 `점수원본.json`(이름
없음)은 "실명"으로 적혀, 경계가 임의로 보인다. 실제로 교사가 만질 파일은
2종인데 6종을 동등하게 나열한다.

### 6.2 역할 상자 — 저장 자리에 있고, 처음부터 보인다

```
─────────────────────────────────────────────────────────────────
저장

┌ 이 컴퓨터에만 두는 것 ────────┐ ┌ AI에게 주는 것 ────────────┐
│                              │ │                            │
│  명렬.json                    │ │  아직 없음                  │
│  48명 · 실명과 학번 있음       │ │                            │
│                              │ │  검수를 마치면 여기에        │
│  이 파일은 활동 폴더에만       │ │  검수diff(AI용).json 이     │
│  둡니다. 대화창에 열어서       │ │  생깁니다. 이름도 학번도    │
│  붙여넣지 마세요.             │ │  들어가지 않습니다.         │
└──────────────────────────────┘ └────────────────────────────┘

        [ 저장 ]
```

- 상자는 **두 개뿐이고 영원히 두 개다.** 파일 수가 늘어도 상자는 안 늘어난다.
- 왼쪽 상자에는 **실명이 있는 것만** 들어간다. `점수원본.json`처럼 이름이
  없는 파일은 애초에 나열하지 않는다 — 교사가 판단할 필요가 없기 때문이다.
- 오른쪽 상자는 **처음부터 존재하고 비어 있다**(§3). 채워지는 순간이 §6.4다.

### 6.3 저장은 대화상자 한 번 (F11 · F17)

`file://`에서는 `showSaveFilePicker`가 막혀 `<a download>` 폴백이 되고,
파일이 2~3개면 크롬이 "여러 파일 다운로드"를 **차단**한다. 차단당해도 화면은
`저장했습니다(내려받기 폴더) — 명렬 48명, 제출자 48명, 점수 30명`이라고 말한다.
**거짓말이다.**

- **나가는 파일을 `명렬.json` 하나로 합친다.**
  `{students, 학번목록, 항목, 인원}`. 파일명이 유지되므로 `--roster`,
  `score --paste`, `destroy` 글롭은 변경 0이다.
  (단, `verify_seteuk.load_submitted_ids`가 `학번목록 ∪ students ∪ …` **합집합**
  이라 한 파일이 되면 명렬 48 + 제출자 45가 48로 뭉개져 "미제출자에게 세특
  생성" FAIL이 영원히 안 뜬다. `학번목록`이 있으면 그것만 쓰고 반환하는
  4줄이 이 안의 전제 조건이다. 이건 화면이 아니라 코드의 일이다.)
- 파일이 1개면 **차단 배너가 뜨지 않는다.**
- 저장 후 문구는 **확인한 것만** 말한다.
  성공: `명렬.json 을 저장했습니다 — 48명, 인원 확인됨.`
  실패/미확인: `저장이 브라우저에 막혔을 수 있습니다. 내려받기 폴더에
  명렬.json 이 있는지 확인해 주세요.`

### 6.4 검수 저장 — 두 번의 클릭으로 두 파일 (F17)

한 번의 클릭으로 두 파일을 내리면 두 번째(= AI에게 줄 유일한 파일)가
조용히 사라진다. **AI에게 줄 것을 먼저, 그리고 클릭을 나눈다.**

```
[ AI에게 줄 파일 저장 ]   ← 1단계. 검수diff(AI용).json

  ↓ 저장 확인 후 이 자리가 바뀐다

✓ 검수diff(AI용).json 저장됨 — 수정 9건. 이 파일만 대화창에 주세요.

[ 실명 검수본도 저장 ]    ← 2단계. 검수본.json. 로컬 보관용.
```

동시에 오른쪽 역할 상자가 채워진다.

```
┌ AI에게 주는 것 ────────────┐
│  검수diff(AI용).json        │
│  수정 9건 · 이름·학번 없음   │
│  이 파일을 대화창에 주세요.  │
└────────────────────────────┘
```

파일명에 `(AI용)`을 박는 것이 이 도구에서 가장 값싼 안전장치다. 30분 뒤
내려받기 폴더에서 파일 이름만 보고도 구분된다.

**부수 사항**(코드): `SENSITIVE_GLOBS`에 `검수본*.json`이 없어 실명 파일
하나가 지금 파기 대상에서 빠져 있다.

### 6.5 CLI를 화면에서 지운다

교사가 직접 터미널에 쳐야 할 정당한 지점은 **하나도 없다.** 그런데
`도구.html` 하단과 `사용안내.md`가 `destroy --dir <활동폴더> --yes`를 보여 준다.
`<>` 치환을 모르고 `--yes`가 무섭기만 하니 **아무것도 안 하고**, 실명 중간
파일이 바탕화면에 영구 잔존한다. 개인정보 설계가 마지막 걸음에서 무너진다.

```
─────────────────────────────────────────────────────────────────
정리

검수를 마치면 실명이 든 중간 파일을 지웁니다.
대화창에 이렇게 말씀하세요.

        중간 파일 파기해 줘

지울 목록을 먼저 보여 드리고, 확인하신 뒤에 지웁니다.
```

명령줄 문자열이 화면에서 사라진다. 남는 것은 교사가 그대로 복사해 채팅에
칠 수 있는 **한국어 한 줄**이다.

---

## 7. 색

### 7.1 원칙

- 종이 느낌은 **배경색이 아니라 조판**에서 낸다(PRODUCT.md). 크림·베이지 없음.
- **금지 대역**: warm-neutral near-white — OKLCH `L 0.84–0.97` ∧ `C < 0.06`
  ∧ `hue 40–100`. 아래 배경 토큰은 **전부 hue 230(냉중립) 또는 hue 25(로즈)**
  로 이 대역 밖이다.
- 다크 중립의 hue를 **271 → 230**으로 옮긴다. 271은 보라 기울기이고,
  반참조 "AI 챗봇"의 색이다.
- 토큰명에 `paper/cream/sand/ivory` 없음. 표면은 **높이**로 이름 짓는다.
- **레이어는 두 겹뿐.** 페이지(`--surface`)와 우묵한 자리(`--surface-sunken`).
  "떠 있는 카드" 층을 만들지 않는다 — 지금 라이트의 카드 레이어가 사실상
  부재(1.035)인데 다크는 1.089라 **두 테마가 다른 구조로 읽힌다.**
  두 겹만 쓰고 양쪽 비를 맞추면 그 문제가 원천 소멸한다.

### 7.2 라이트

| 토큰 | hex | OKLCH | 용도 |
|---|---|---|---|
| `--surface` | `#fdfdfe` | `0.995 0.001 230` | 페이지 |
| `--surface-sunken` | `#eef2f3` | `0.958 0.004 230` | 학생 원문, 파일명 |
| `--ink` | `#1a1f21` | `0.235 0.008 230` | 본문 |
| `--ink-2` | `#51585b` | `0.455 0.010 230` | 설명문 |
| `--ink-3` | `#6a7175` | `0.545 0.010 230` | 수치·라벨·0 |
| `--rule` | `#dde0e2` | `0.905 0.004 230` | 구역 가로선(장식) |
| `--rule-strong` | `#81878a` | `0.620 0.008 230` | 입력·버튼 경계 |
| `--accent` | `#1f6543` | `0.455 0.090 158` | 주 버튼, 포커스, 현재 |
| `--accent-ink` | `#fdfdfe` | `0.995 0.001 230` | 주 버튼 글자 |
| `--accent-quiet` | `#ddf3e5` | `0.945 0.030 158` | 현재 검수 중인 줄 |
| `--flag` | `#8b5214` | `0.495 0.105 62` | 우선 검토, 주의 |
| `--stop` | `#aa2f2f` | `0.495 0.160 25` | 불일치, 초과, 중복 |
| `--stop-ground` | `#ffeeec` | `0.962 0.020 25` | 불일치 줄 배경 |

### 7.3 다크

| 토큰 | hex | OKLCH | 용도 |
|---|---|---|---|
| `--surface` | `#111517` | `0.192 0.007 230` | 페이지 |
| `--surface-sunken` | `#06090a` | `0.135 0.007 230` | 학생 원문, 파일명 |
| `--ink` | `#e5e8ea` | `0.930 0.004 230` | 본문 |
| `--ink-2` | `#aaafb2` | `0.752 0.007 230` | 설명문 |
| `--ink-3` | `#8f9598` | `0.665 0.008 230` | 수치·라벨·0 |
| `--rule` | `#2b3032` | `0.305 0.008 230` | 구역 가로선 |
| `--rule-strong` | `#6a7175` | `0.545 0.010 230` | 입력·버튼 경계 |
| `--accent` | `#72c496` | `0.755 0.105 158` | 주 버튼, 포커스, 현재 |
| `--accent-ink` | `#06170d` | `0.185 0.030 158` | 주 버튼 글자 ← **뒤집힘** |
| `--accent-quiet` | `#13281c` | `0.255 0.035 158` | 현재 검수 중인 줄 |
| `--flag` | `#ebb878` | `0.815 0.100 72` | 우선 검토, 주의 |
| `--stop` | `#f28881` | `0.740 0.130 25` | 불일치, 초과, 중복 |
| `--stop-ground` | `#391b18` | `0.262 0.048 25` | 불일치 줄 배경 |

### 7.4 대비비 (계산값)

| 쌍 | 라이트 | 다크 | 기준 |
|---|---|---|---|
| `--ink` / `--surface` | **16.37** | **14.92** | 4.5 |
| `--ink-2` / `--surface` | **7.13** | **8.29** | 4.5 |
| `--ink-3` / `--surface` | **4.88** | **6.05** | 4.5 |
| `--ink` / `--surface-sunken` | **14.76** | **15.84** | 4.5 |
| `--ink-2` / `--surface-sunken` | **6.43** | — | 4.5 |
| `--ink-3` / `--surface-sunken` | 4.40 → 이 조합은 쓰지 않는다 | 6.58 | 4.5 |
| `--accent` / `--surface` | **6.88** | **8.79** | 4.5 |
| `--flag` / `--surface` | **6.23** | **10.20** | 4.5 |
| `--stop` / `--surface` | **6.54** | **7.53** | 4.5 |
| **`--accent-ink` / `--accent`** (주 버튼) | **6.88** | **8.85** | 4.5 |
| `--ink` / `--stop-ground` | **14.81** | **12.70** | 4.5 |
| `--stop` / `--stop-ground` | **5.92** | **6.41** | 4.5 |
| `--ink` / `--accent-quiet` | **14.29** | **12.66** | 4.5 |
| `--rule-strong` / `--surface` | **3.58** | **3.70** | 3.0 (1.4.11) |
| `--rule-strong` / `--surface-sunken` | **3.23** | 3.9+ | 3.0 |
| `--surface-sunken` / `--surface` (층 분리) | 1.109 | 1.088 | 양 테마 근사 |

**핵심 수정 셋.**
지금 다크 주 버튼은 `#fff` on `#7fb99a` = **2.26:1**이고, 해당 요소가
`#save`·`#rv-save` — 파이프라인의 **두 종결 동작 전부**다. `--accent-ink`가
테마에 따라 뒤집히면(라이트=흰 글자 / 다크=검은 글자) 6.88과 8.85가 된다.
`tr.dup` 배경은 라이트 1.129 / 다크 1.006(= 완전 동일)이었고,
`--warn`(라이트) 4.35는 미달이었다. 셋 다 위 표에서 해소된다.

### 7.5 색을 쓰지 않는 규칙

- **성공색이 없다.** 초록은 오직 `--accent`(주 버튼·포커스·현재 위치)뿐이다.
  "다 잘됐다"를 색으로 말하지 않는다. 설계 원칙 2가 그렇게 시킨다.
- **수치 0은 언제나 `--ink-3` 평문.**
- **모든 상태는 낱말을 동반한다.** `중복`, `초과`, `미제출`, `수정됨`,
  `우선 검토 사유`. 색은 그 낱말 위에 얹히는 두 번째 신호다.
- **경고에 큰 면적의 배경 채움을 쓰지 않는다.** `--stop-ground`는 한 줄짜리
  대조 문장에만. 라이트에서 노란 채움을 쓰려면 hue 40–100 / L 0.84–0.97 /
  C<0.06 이 되어 **크림 금지 대역에 정확히 들어간다** — 그래서 `--flag`는
  글자색과 선색으로만 존재하고 배경이 없다. 양 테마 동일 규칙이므로
  구조가 갈라지지 않는다.
- **비활성에 `opacity`를 쓰지 않는다.** 지금 `.card.done{opacity:.55}`는
  본문을 3.90, muted를 2.23으로 떨어뜨리면서 textarea는 여전히 편집 가능하고,
  `tr.absent{opacity:.45}`는 2.89다. 대신 **색 토큰을 `--ink-3`로 교체**하고
  실제로 `disabled`를 건다.

### 7.6 상태 어휘 (모든 인터랙티브 요소가 전부 갖는다)

지금 `:hover` 0건 / `:active` 0건 / `:focus-visible` 0건이고, 포커스 스타일은
`textarea:focus` 한 줄인데 `outline-offset:-1px`라 글자를 덮는다.

| 상태 | 규칙 |
|---|---|
| default | `--rule-strong` 1px 경계 |
| hover | 배경 `--surface-sunken`, 120ms |
| focus-visible | `outline: 2px solid var(--accent); outline-offset: 2px` (**바깥으로**) |
| active | 배경 `--accent-quiet`, transform 없음 |
| disabled | 글자 `--ink-3`, 경계 `--rule`, `cursor: default`, 실제 `disabled` |
| selected(검수 현재) | 배경 `--accent-quiet` + 학번 굵기 600 |

---

## 8. 타이포

### 8.1 스택 (외부 폰트 로드 없음)

```css
--font: "Apple SD Gothic Neo", "Malgun Gothic", "맑은 고딕",
        system-ui, -apple-system, "Segoe UI", sans-serif;
--font-mono: "D2Coding", Consolas, ui-monospace, monospace;
```

한글을 먼저 놓는다. `system-ui`를 앞에 두면 한국어 윈도우에서 Segoe UI가
잡히고 한글만 글자 단위로 맑은 고딕에 폴백되어 **한 줄 안에서 라틴과 한글의
무게가 어긋난다.** 학번(라틴 숫자)과 이름(한글)이 같은 줄에 붙어 있는
화면에서 이건 눈에 띈다.

모노는 **파일명과 토큰(`S-08F4`)에만.** 학생 원문·세특에는 절대 쓰지 않는다.

### 8.2 스케일 — 고정 rem, 5단

지금은 12종이 0.19rem 안에 9단계로 몰려 있고 인접 비가 1.01~1.03이다.
스케일이 아니라 흩어짐이다.

| 토큰 | 크기 | 행간 | 용도 |
|---|---|---|---|
| `--t-title` | `1.5rem` / 24px | 1.35 | 페이지 제목(문서 전체에 `h1` 하나) |
| `--t-section` | `1.1875rem` / 19px | 1.45 | 구역 제목. **대문자·자간 없음, muted 아님** |
| `--t-body` | `1.0625rem` / 17px | **1.75** | 학생 원문, 세특, 설명문 |
| `--t-ui` | `0.9375rem` / 15px | 1.5 | 버튼, 라벨, 표 |
| `--t-meta` | `0.8125rem` / 13px | 1.5 | 바이트, 순번, 파일 부기 |

비율 1.26 / 1.12 / 1.13 / 1.15. **`--t-body`가 `--t-ui`보다 크다** —
PRODUCT.md의 우선순위가 스케일에 박혀 있다.

`h2/h3`의 `text-transform: uppercase` + `letter-spacing: .08em` + `--muted`를
전부 없앤다. 금지 목록의 "모든 섹션 위 작은 대문자 eyebrow" 직격이었고,
게다가 **`uppercase`는 "붙여넣기"에 아무 효과가 없어** 실효는 구역 제목을
12px 최저 대비로 만든 것뿐이었다.

`1 · 붙여넣기` 같은 번호도 없앤다 — 세로 순서가 곧 번호이므로 중복이다.

### 8.3 한글 가독

```css
line-height: 1.75;          /* 한글은 낱자가 네모칸을 꽉 채워 라틴보다 더 필요 */
letter-spacing: 0;          /* 한글에 자간을 주지 않는다. 어절 경계가 흐려진다 */
word-break: keep-all;       /* 어절 중간에서 끊지 않는다 */
overflow-wrap: break-word;  /* 단, 긴 토큰은 넘치지 않게 */
font-variant-numeric: tabular-nums;   /* 학번·바이트가 세로로 정렬된다 */
```

`text-align: justify` 금지 — 한글 양끝맞춤은 어절 간격이 널뛴다.

### 8.4 줄 길이

`--measure: 34rem` (≈ 578px, 한글 약 34자).
지금 산문 줄 길이는 약 83자이고 **하필 가장 긴 것이 실명 경고문**이다.
가장 중요한 문장이 가장 읽기 어렵다.

- 설명문·경고문: `--measure`
- 학생 원문 / 세특(1280px 2열): 각 424px ≈ 한글 25자 — 짧지만 두 글을 **같은
  높이로 나란히** 두는 값이 더 크다
- 표: 제한 없음(880px 전폭)

### 8.5 세로 리듬

4px 기준, 8단만 쓴다(지금 15종, 공배수 없음).

```
--s1: 4px   --s2: 8px   --s3: 12px  --s4: 16px
--s5: 24px  --s6: 32px  --s7: 48px  --s8: 64px
```

구역 사이 `--s8`, 구역 안 블록 사이 `--s5`, 라벨과 값 사이 `--s2`.
**종이의 느낌은 여기서 나온다.** 배경색이 아니라 이 눈금에서.

---

## 9. 모션

### 9.1 원칙

상태 변화에만. 150~250ms. 페이지 로드 연출 없음. 스태거 없음.
지금은 `transition/animation/keyframes` **전부 0건**인데, 이건 장식을 배제한
게 아니라 **상태 전달 수단이 없는 것**이다. 붙여넣기 직후 카드 3개가 한
프레임에 튀어 오른다.

### 9.2 목록

| 무엇이 | 언제 | 어떻게 |
|---|---|---|
| 붙여넣기 상자 활성화 | 인원 입력 완료 | `opacity .5→1`, `border-color`, 160ms ease-out |
| `확인` 구역 등장 | 붙여넣기 직후 | `opacity 0→1` + `translateY 4px→0`, 180ms. **한 덩어리로, 스태거 없이** |
| 인원 불일치 줄 | 대조 실패 | 배경 `--stop-ground` 240ms 페이드 인. **사라지지 않는다** |
| 검수 항목 펼침 | ⏎ / 클릭 | `grid-template-rows: 0fr→1fr`, 200ms ease-out |
| 검수 항목 이동 | 마침 표시 | `먼저 볼` → `마친` 목록으로 200ms 위치 이동 |
| 버튼 hover/active | — | `background-color` 120ms |
| 색인 현재 위치 | 스크롤 | **transition 없음.** IntersectionObserver로 굵기만 바꾼다 (스크롤 중 깜빡임 방지) |
| 저장 진행 | 클릭 | **스피너 없음.** 버튼 라벨이 `저장` → `저장 중…` → `✓ 저장됨`으로 교체 |

### 9.3 존재하지 않는 것

로드 애니메이션, 스켈레톤(로컬 파싱은 즉각이다), 축하 효과,
숫자 카운트업, 스크롤 연동 페이드, 호버 시 확대·그림자.

### 9.4 `prefers-reduced-motion: reduce`

```css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: .01ms !important;
    transition-duration: .01ms !important;
    scroll-behavior: auto !important;
  }
}
```

**대안은 문구다.** 움직임이 전하던 정보를 낱말이 대신 전한다.

- 항목이 목록 사이를 이동하는 대신 즉시 재배치되고, 목록 위에
  `방금 1명을 '마친 12명'으로 옮겼습니다`가 남는다(자동 소멸 없음).
- `확인` 구역은 즉시 나타나고, `aria-live="polite"` 영역이
  `48명을 읽었습니다. 적어 두신 48명과 같습니다`를 읽는다.
- 자동 스크롤(`scrollIntoView`)은 `behavior: auto`가 되며, 화면이
  이미 목표를 담고 있으면 아예 스크롤하지 않는다.

`aria-live`는 지금 0건이다. ①인원 대조 줄 ②저장 상태 ③검수 진행 카운트,
셋에만 붙인다(`polite`). 그 이상은 소음이다.

### 9.5 네이티브 모달 제거

`alert()` / `confirm()`은 product 레지스터 금지("modal as first thought").
`검수 완료 표시가 없는 학생이 N명` 확인은 저장 버튼 **바로 위 인라인 블록**이
된다.

```
검수 완료 표시가 없는 학생이 12명 있습니다.

  [ 그래도 저장 ]   [ 12명 보러 가기 ]
```

`[12명 보러 가기]`는 목록을 미완료만으로 걸러 준다 — 확인 대화상자에는
없던 **세 번째 선택지**이고, 이게 인라인으로 옮기는 진짜 이유다.

---

## 10. 마찰 대응

### 10.1 first-run 진단

| 번호 | 마찰 | 이 안에서 | 해소 |
|---|---|---|---|
| **F8** | 열 판별 침묵 (헤더 경로 무경고) | §4.2 첫 행 **실제 값** 확인, `via` 무관하게 상설 | ✅ |
| **F9** | 미제출 체크가 파싱 전 / 기본값 전원 제출 | §4.4 제출 열은 점수 뒤에 생기고 기본값 없음, 판단 48→3 | ✅ |
| **F11** | 다중 다운로드 차단 + 거짓 성공 문구 | §6.3 파일 1개, 확인한 것만 말함 | ✅ |
| **F12** | 파일 6종 동등 나열, 경계 임의 | §6.2 역할 상자 2개, 실명 있는 것만 나열 | ✅ |
| **F14** | 시간 기대치 부재 | §3 이중선 "여기서 대화로 돌아갑니다" + 색인이 전 구간 노출 | ✅ |
| **F15** | 바이트 두 개 + `(기준 확정 전)` | §5.6 화면은 한 숫자만 | 🟡 화면 몫만 |
| **F17** | `검수diff.json`이 조용히 사라짐 | §6.4 AI용 먼저, 클릭 분리, 파일명에 `(AI용)` | ✅ |
| **CLI** | `destroy --yes` 노출 → 아무것도 안 함 | §6.5 한국어 한 줄로 교체 | ✅ |
| **F3** | 파이썬 PATH | **해결 못 한다.** 브라우저 화면이 닿지 않는 영역 | ❌ |

### 10.2 UI 진단 P0

| # | 지적 | 해소 |
|---|---|---|
| 1 | 다크 주 버튼 2.26:1 (`#save`·`#rv-save`) | §7.3 `--accent-ink` 테마별 반전 → 8.85 |
| 2 | `tr.dup` 1.129 / **1.006** + 색 단독 | §4.3 `중복` 글자 + §7.4 대비 |
| 3 | `중복 0`을 초록으로 렌더 | §7.5 0은 `--ink-3` 평문, 성공색 부재 |
| 4 | `.source` 13.92px < `.tabs button` 14.88px | §8.2 `--t-body` 17px > `--t-ui` 15px |
| 5 | 전 구역 uppercase eyebrow + muted 12px | §8.2 제거, 구역 제목 19px 본문색 |
| 6 | hover/active/focus-visible 0건 | §7.6 6상태 전부 정의, 포커스 바깥 offset |
| 7 | `role=` 0건, 항목 간 이동 수단 없음 | §5.5 roving tabindex + ⌃⏎ |
| 8 | 4단계 중 3단계가 DOM에서 사라짐 / 검수 완전 공백 | §3 전 구역 상설, 빈 자리가 무엇이 필요한지 말함 |

### 10.3 UI 진단 P1 (주요)

`--warn` 4.35:1 → §7.4 `--flag` 6.23 · `--warnbg` 크림 표면 → §7.5 배경 채움
제거 · 라이트 카드 레이어 부재(테마 구조 분기) → §7.1 2겹 통일 · 경계 3:1
미달 → §7.2 `--rule-strong` 3.58/3.70 · `opacity` 상태 붕괴 → §7.5 토큰 교체 ·
타입 12종 → §8.2 5단 · 간격 15종 → §8.5 8단 · 줄 길이 83자 → §8.4 34자 ·
카드 44개 → §1.5 2개 · `aria-live` 0건 → §9.4 3곳 · 네이티브 모달 → §9.5 인라인 ·
`input` 매 타건 재파싱 → paste/change + 400ms 디바운스 · 모션 0건 → §9.2

### 10.4 workflow-surgery

| 항목 | 이 안에서 |
|---|---|
| §1 브라우저 2회 유지 | §1.4 — 탭을 없애도 세션은 둘로 유지 |
| §2 인원을 붙여넣기보다 위로 | §3, §4.1 — 이 안의 첫 화면 그 자체 |
| §3 JSON 6종 → 4종 | §6.3 — 단, `load_submitted_ids` 4줄이 전제 |
| §4 검수본/diff 분리 유지 + 파일명 구분 | §6.4 |
| §5 손 이동 0 | §6.3 — 화면은 정직한 문구까지, 회수는 에이전트 |
| §8 온보딩 배치 3가지만 | §3.1 — 두 열 함께 / 인원 먼저 / diff 하나 |
| §9 번호 스캐폴딩·eyebrow 자기 위반 | §8.2 — 둘 다 제거 |

---

## 11. 이 안이 무너지는 지점

정직하게, 여섯 개.

### 11.1 실명이 한 문서 안에 공존한다 — 가장 아픈 곳

지금 `showTab()`의 주석은 "탭 사이에 자료를 저장하지 않는다(실명이 브라우저에
눌러앉지 않게)"라고 적혀 있다. 사실 `showTab`은 `hidden`만 토글하므로 실제
격리는 없었고, 이 안이 실질을 더 나쁘게 만들지는 않는다. **그러나 의도의
표명은 사라진다.** 명렬(실명)과 검수 번들(실명)이 하나의 DOM·하나의 스코프에
공존하면, 앞으로 어떤 기능(인쇄 스타일, 전역 검색, 자동완성, 실수로 넓힌
셀렉터)이 둘을 가로질러도 구조가 막아 주지 않는다.

완화: 저장 성공 직후 명단 구역이 접히고 `[ 화면에서 명단 지우기 ]`가 나타나
누르면 실제로 상태를 비운다. 자동으로 지우지는 않는다(교사가 아직 확인 중일
수 있으므로). **완화이지 해결이 아니다.** 탭을 유지하면서 두 스코프를 진짜로
분리하는 안이 이 항목만큼은 더 강하다.

### 11.2 색인 레일이 스테퍼로 미끄러지기 쉽다

숫자를 붙이는 순간 금지 목록의 "01/02/03 번호 스캐폴딩"이고, 동그라미와
연결선을 붙이면 SaaS 온보딩이다. 체크마크를 붙이면 장난스러운 교육앱이다.
**글자 목록 + 굵기 하나**라는 제약을 구현 내내 지켜야 하는데, 이건 설계가
아니라 규율의 문제이고 규율은 새기 쉽다. 이 안에서 가장 실패 확률이 높은
단일 요소다.

### 11.3 새로고침 한 번에 검수 48명이 날아간다

이건 탭 안도 같지만, 단일 흐름은 **페이지가 길어서** 새로고침 유혹이
더 크다(스크롤이 꼬였을 때 사람은 F5를 누른다). 그리고 `beforeunload` 경고는
브라우저마다 문구를 못 바꿔서 "변경 사항이 저장되지 않을 수 있습니다"라는
일반 문구만 나온다 — 이 도구가 지향하는 "원인까지 말하는 경고"의 정반대다.
localStorage 자동 저장은 "아무것도 기억하지 않는다"는 약속과 정면 충돌하므로
쓰지 않는다. **남는 방어는 잦은 부분 저장 유도뿐이고, 그건 약하다.**

### 11.4 검수만 하러 온 두 번째 방문이 한 클릭 더 든다

탭 안에서는 `[검수]` 클릭 한 번이다. 이 안에서는 스크롤하거나 색인을
클릭해야 한다. §5.1의 상시 활성 파일 열기 버튼과 자동 스크롤이 이를 거의
상쇄하지만 **정확히 상쇄하지는 못한다.** 두 번째 세션이 첫 세션보다 잦아지는
사용 양상(같은 활동을 여러 반에 반복)이라면 이 비용은 누적된다.

### 11.5 가로 200px을 색인에 내준다

1280px에서 검수 2열이 각 424px이 된다. 탭 안이라면 각 520px을 쓸 수 있다.
한글 25자 vs 30자 — 세특 700바이트(≈350자)를 읽을 때 14줄 vs 12줄이다.
크지 않지만 **이 도구에서 가장 오래 보는 화면에서 지불하는 값**이다.
1120px 미만에서 레일이 접히므로 노트북에서는 문제가 아니고, 교무실
데스크톱(1920)에서도 문제가 아니다. **정확히 1280px 근처에서만 손해다.**

### 11.6 여러 반 검수의 필터를 놓을 자리가 없다

워크플로 진단은 다반 검수용 반 필터로 "열기 N회 → 1회"를 제안했다.
단일 흐름에서 그 컨트롤을 놓을 자리는 레일뿐인데, 레일에 컨트롤이 들어가는
순간 레일은 색인이 아니라 **사이드바**가 되고 §11.2의 규율이 무너진다.
본문 안에 두면 "먼저 볼 12명" 제목 위에 필터 바가 생겨 §5.4가 약해진다.
**이 안은 이 요구를 아직 수용하지 못한다.** 반이 하나일 때 가장 강하고,
셋을 넘으면 다른 구조가 필요해질 것이다.

---

## 12. 요약

| | 지금 | 이 안 |
|---|---|---|
| 상자(카드) | 44 | 2 |
| 활자 크기 | 12종 | 5종 |
| 간격 값 | 15종 | 8종 |
| 저장 대화상자 (명단) | 2~3 (차단됨) | 1 |
| 제출 여부 판단 | 48회 | 3회 |
| 동시에 펼쳐진 세특 | 40 | 1 |
| 화면의 CLI 문자열 | 1 | 0 |
| 최저 대비(주 버튼, 다크) | 2.26 | 8.85 |
| 최저 대비(중복 행, 다크) | 1.006 | 7.53 + 낱말 |
| `:focus-visible` | 0건 | 전 인터랙티브 |
| `aria-live` | 0건 | 3곳 |
| 첫 페인트에 보이는 개인정보 경계 | 없음 | 빈 상자 2개 |
