# 설계 브리프 — 세특 작업 도구 화면

> 경로 주의: 과제문의 `undefined/`는 치환 실패다. 실제 대상은
> `C:\Users\admin\.claude\skills\seteuk-harness\`로 해석했다.
> 이 회차는 설계 문서만 만든다. 코드는 수정하지 않았고 git 명령도 쓰지 않았다.
> 등장하는 학생 이름·학번은 전부 가상이다.
>
> 채택안: **B-one-page-flow(단일 세로 흐름)**.
> 여기에 판정단이 지목한 이식(graft) 13건과 적대적 검증이 짚은 6개 약점의
> 대응책을 반영했다. 원안과 달라진 곳은 각 절에 **[변경]**으로 표시한다.
> 구현자는 이 문서만 읽으면 된다. 원안 문서(`B-one-page-flow.md`)와
> 충돌하면 **이 문서가 이긴다.**

---

## 1. 채택 구조와 근거

1. **탭을 없애고 위에서 아래로 한 번 내려가는 단일 페이지로 간다.** 탭 A와
   탭 B는 다른 기능이 아니라 같은 파이프라인의 앞뒤인데, 탭은 그 인과를 지워
   "이 도구가 한 번에 끝나지 않는다"는 사실을 숨긴다. 그 자리가 정확히 교사가
   이탈하는 곳이고, 이탈처가 하필 PRODUCT.md의 다섯 번째 반참조(붙여넣고
   버튼 누르면 쏟아지는 웹 세특 생성 서비스)다.
2. **개인정보 경계가 첫 페인트에 보이는 유일한 구조다.** 「이 컴퓨터에만
   두는 것 / AI에게 주는 것」 두 상자가 아무것도 붙여넣기 전에 빈 채로 존재한다.
   탭 구조에서는 이 경계가 탭 B의 저장 버튼을 누른 뒤에야, 즉 파일이 이미
   만들어진 다음에 처음 등장한다. 설계 원칙 3을 화면이 지키는 방법이 이것뿐이다.
3. **세로 순서가 곧 시간 순서라서 설계 원칙 5("화면 자체가 순서")가 은유가
   아니라 배치가 된다.** 브라우저를 두 번 여는 사실은 유지한다 — 탭을 없애는
   것과 세션을 합치는 것은 다른 일이다. 중간의 굵은 이중선이 두 세션의 경계를
   화면의 물리적 사실로 만든다.

### 1.1 원안에서 바꾼 것 (요약)

| # | 원안 | 이 브리프 | 근거 |
|---|---|---|---|
| A1 | 로드 시 인원 칸에 자동 포커스 | **자동 포커스 없음** + 인원 칸 붙여넣기 감지 | 적대적 §1 |
| A2 | 인원 미입력 시 붙여넣기 상자 `disabled` | **잠금 폐기.** 상자는 늘 편집 가능, 결과 표시만 보류 + 탈출구 제공 | 적대적 §2 |
| A3 | 역할 상자 초기값 `아직 없음` | **`이 화면에서는 아직 만들지 않았습니다`** (3상태) | 적대적 §3 |
| A4 | 검수본은 맨 이름 | **`검수본(실명·보관용).json`** + recency 함정을 문장으로 명시 | 적대적 §4 · novice graft |
| A5 | `--ink-3` 미달 조합 존재, 이중선 토큰 미지정 | `--ink-3` 재계산, `--rule-double` 신설, `--focus`/`--panel` 신설 | 적대적 §5 · slop graft |
| A6 | 마친 항목이 목록 사이를 200ms 이동 | **이동 없음.** 제자리 + 낱말. 포커스 이관 명시 | 적대적 §6 |
| A7 | 저장 버튼 상시 존재 | **열 확인 전에는 저장 버튼이 나타나지 않는다** | novice graft |
| A8 | 오류를 알려 주기만 함 | **정오표에 복구 동작 단추** | novice graft |
| A9 | `beforeunload` 없음 | **파일 저장 상태에 건다**(students.length 아님) | novice graft |
| A10 | 목록 행에 사유·학번·이름·바이트 | **둘째 줄에 핵심소재** | novice · long-review · slop 3표 일치 |
| A11 | 검수 빈 상태 "생깁니다"까지 | **어디에 있는지 + 없을 때 무엇을 할지** | novice graft |
| A12 | 레이어 2겹 | **패널 중립 1겹 추가**(`--panel`), 원문 inset / 세특 raised | slop graft |
| A13 | `✓ 저장됨` 단언 | **`내려받기 폴더 확인`** 상태 신설 | long-review · slop 2표 일치 |
| A14 | 검수 2열 각 424px | **검수 구역만 폭 제한을 푼다**(각 ~500px) | long-review(§11.5 자인) |
| A15 | 세특 입력 400ms 디바운스 | **300ms + blur/paste 즉시**, 원문·세특 기준선 일치, 칸 높이 고정 | long-review graft |
| A16 | 색인에 `세특` 항목 없음 | **`세특 ― 대화에서 만듭니다`가 색인에 상설** | slop graft |
| A17 | ≤900px 색인 접힘 기제 미지정 | **네이티브 `<details>/<summary>`** | slop graft |
| A18 | 포커스 링이 `--accent`(초록) | **`--focus` 독립 토큰(청)** | slop graft |

---

## 2. 화면별 명세

공통 골격: 상단 줄(sticky 44px) + 좌측 색인(200px, `--panel`, ≥1120px에서만) +
본문 기둥. 본문 기둥은 기본 `max-width: 880px`이고 **검수 구역만
`max-width: 1032px`**(A14). 산문은 그 안에서 `--measure: 34rem`으로 더 좁게 간다.

구역은 상자가 아니라 **가로선(`--rule`)과 여백(`--s8`)**으로 나뉜다.
페이지 전체에 상자(테두리 있는 블록)는 **역할 상자 2개뿐**이다.

---

### 2.1 첫 화면 — 아무것도 하지 않은 상태

#### 1280px

```
┌──────────────────────────────────────────────────────────────────────────────┐
│ 세특 작업 도구                          ○ 이 화면에서 저장한 것 없음         │ sticky 44px
├────────────┬─────────────────────────────────────────────────────────────────┤
│            │                                                                 │
│  인원 ●    │  ──────────────────────────────────────────────────────────     │
│  명단      │  이 활동 대상은 몇 명입니까                                     │
│  점수      │                                                                 │
│  저장      │  명단을 보기 전에 아시는 수를 적어 주세요. 정원에서 전출·자퇴를  │
│            │  뺀 수입니다. 이 수와 붙여넣은 명단이 다르면 화면이 그 자리에서 │
│  ‥‥‥‥  │  말합니다.                                                      │
│  세특 ―    │                                                                 │
│  대화에서  │      [        ]  명           ← 자동 포커스 없음 (A1)           │
│  만듭니다  │                                                                 │
│  ‥‥‥‥  │  ──────────────────────────────────────────────────────────     │
│            │  명단 붙여넣기                                                  │
│  검수      │                                                                 │
│  검수 저장 │  엑셀에서 학번 열과 이름 열을 함께 긁어(Ctrl+C) 붙여넣으세요.    │
│  정리      │  따로 두 번 붙이면 행이 어긋나고, 어긋난 명단은 어떤 검사로도   │
│            │  잡히지 않습니다.                                               │
│  (색인,    │  ┌──────────────────────────────────────────────────────────┐  │
│   글자만,  │  │ 30101   김하늘                                            │  │
│   200px)   │  │ 30102   이서준                          ← placeholder      │  │
│            │  └──────────────────────────────────────────────────────────┘  │
│            │       ↑ 잠기지 않는다 (A2). 늘 편집·붙여넣기 가능.              │
│            │                                                                 │
│            │  ──────────────────────────────────────────────────────────     │
│            │  점수 (선택)                                                    │
│            │  채점표에서 학번 열과 점수 열을 함께 긁어 붙여넣으세요.          │
│            │  비워 두면 점수 없이 진행합니다.                                │
│            │  명단을 먼저 붙여넣으시면 어느 열이 점수인지 대조할 수 있습니다. │
│            │                                                                 │
│            │  ──────────────────────────────────────────────────────────     │
│            │  저장                                                           │
│            │                                                                 │
│            │  ┌ 이 컴퓨터에만 두는 것 ─────┐ ┌ AI에게 주는 것 ───────────┐  │
│            │  │ 이 화면에서는 아직 만들지  │ │ 이 화면에서는 아직 만들지 │  │
│            │  │ 않았습니다.                │ │ 않았습니다.               │  │
│            │  │ 활동 폴더에 이미 있을 수   │ │ 검수를 마치면 여기에      │  │
│            │  │ 있습니다 — 화면은 폴더를   │ │ 검수diff(AI용).json이     │  │
│            │  │ 볼 수 없습니다.            │ │ 생깁니다. 이름도 학번도   │  │
│            │  │                            │ │ 들어가지 않습니다.        │  │
│            │  └────────────────────────────┘ └───────────────────────────┘  │
│            │                                                                 │
│            │       (저장 단추는 아직 없다 — A7)                              │
│            │                                                                 │
│            │  ═══════════════════════════════════════════════════════════   │
│            │  여기서 대화로 돌아갑니다                                        │
│            │  저장을 마치면 규칙·구조·표본을 정하는 대화가 20~60분 이어집니다.│
│            │  그동안 이 창은 닫으셔도 됩니다. 끝나면 다시 열어 아래로 오세요. │
│            │  ═══════════════════════════════════════════════════════════   │
│            │                                                                 │
│            │  ──────────────────────────────────────────────────────────     │
│            │  검수                                                           │
│            │  원문과 세특을 나란히 놓고 고치는 곳입니다.                      │
│            │  활동 폴더의 검수번들.json을 이 자리에 끌어다 놓거나 아래 단추로 │
│            │  고르세요. 파일이 아직 없다면 대화로 돌아가 전체 생성을 마치세요.│
│            │  생성이 끝나야 이 파일이 만들어집니다.                           │
│            │       [ 검수번들.json 고르기 ]     ← 언제나 활성                 │
│            │                                                                 │
│            │  ──────────────────────────────────────────────────────────     │
│            │  정리                                                           │
│            │  검수까지 마치면 실명이 든 중간 파일을 지웁니다.                 │
│            │  대화창에 이렇게 말씀하세요.                                     │
│            │        중간 파일 파기해 줘                                       │
│            │  지울 목록을 먼저 보여 드리고, 확인하신 뒤에 지웁니다.           │
└────────────┴─────────────────────────────────────────────────────────────────┘
```

#### ≤900px

```
┌────────────────────────────────────────────────┐
│ 세특 작업 도구      ▸ 차례   ○ 저장한 것 없음  │ sticky
├────────────────────────────────────────────────┤
│  ▸ 차례  ← <details>/<summary> (A17)           │
│    (펼치면 인원·명단·점수·저장 / 세특―대화에서 │
│     / 검수·검수 저장·정리 가 한 줄씩)          │
│                                                │
│  ────────────────────────────────────────────  │
│  이 활동 대상은 몇 명입니까                     │
│  명단을 보기 전에 아시는 수를 적어 주세요…      │
│      [        ] 명                             │
│                                                │
│  ────────────────────────────────────────────  │
│  명단 붙여넣기                                  │
│  엑셀에서 학번 열과 이름 열을 함께 긁어…        │
│  ┌────────────────────────────────────────┐    │
│  │ 30101   김하늘                          │    │
│  └────────────────────────────────────────┘    │
│                                                │
│  ────────────────────────────────────────────  │
│  점수 (선택) …                                  │
│                                                │
│  ────────────────────────────────────────────  │
│  저장                                           │
│  ┌ 이 컴퓨터에만 두는 것 ──────────────────┐   │
│  │ 이 화면에서는 아직 만들지 않았습니다.    │   │
│  └──────────────────────────────────────────┘   │
│  ┌ AI에게 주는 것 ─────────────────────────┐   │
│  │ 이 화면에서는 아직 만들지 않았습니다.    │   │
│  └──────────────────────────────────────────┘   │
│      (역할 상자는 세로로 쌓인다. 순서 유지)     │
│                                                │
│  ══════════════════════════════════════════    │
│  여기서 대화로 돌아갑니다                       │
│  ══════════════════════════════════════════    │
│                                                │
│  ────────────────────────────────────────────  │
│  검수 … [ 검수번들.json 고르기 ]                │
│  ────────────────────────────────────────────  │
│  정리 …                                         │
└────────────────────────────────────────────────┘
```

#### 요소 목록

| 요소 | 태그 | 상태 | 비고 |
|---|---|---|---|
| 페이지 제목 | `h1` | — | 문서 전체에 **하나만**. 현재 코드는 2개다 |
| 저장 상태 표시 | `output[aria-live=polite]` | 3상태(§6.7) | sticky 우측 |
| 색인 | `nav > ul > li > a[href="#…"]` | 현재 위치 굵기 600 | ≥1120px. 숫자·원·연결선·체크마크 **없음** |
| 색인 대화 구간 | `li` (링크 아님) | 상설 | `세특 ―` / `대화에서 만듭니다` (A16) |
| 인원 입력 | `input[type=text][inputmode=numeric][pattern="[0-9]{1,3}"]` | §5 | `type=number` 아님(A1) |
| 명단 textarea | `textarea` | 늘 편집 가능(A2) | `placeholder`가 두 열 예시 |
| 점수 textarea | `textarea` | 늘 편집 가능 | |
| 역할 상자 ×2 | `section > h3 + p` | 3상태(§7.2) | 테두리 있는 유일한 블록 |
| 이중선 | `hr.double` | — | `--rule-double`, 위아래 2px 두 줄 |
| 검수 파일 고르기 | `input[type=file]` + 드롭 영역 | 언제나 활성 | |
| 정리 안내 | `p` + `code`(한국어 문장) | — | CLI 문자열 0 |

---

### 2.2 붙여넣기 직후

Ctrl+V 순간 `확인` 구역이 180ms에 한 덩어리로 나타난다(스태거 없음).

#### 1280px

```
  ──────────────────────────────────────────────────────────────────
  확인

  ▍48명을 읽었습니다. 적어 두신 48명과 같습니다.        ① 17px, 굵기 600
                                                          aria-live=polite

  첫 줄을 이렇게 읽었습니다.                              ②

        학번   30101
        이름   김하늘

     [ 맞습니다 ]   [ 열이 바뀌었습니다 ]
     저장은 위 확인을 마치신 뒤에 나옵니다.               ← A7

  ─ 손봐야 할 곳 ──────────────────────────────────────────────     ③ 정오표
  학번이 겹치는 행이 1개 있습니다. (12행 30102 · 13행 30102)
  여러 반을 함께 붙여넣었거나 출석번호를 학번으로 쓴 표일 수 있습니다.
     [ 뒤엣것 지우기 ]  [ 둘 다 두기 ]                    ← A8 복구 동작

  학번 앞자리가 301·302 두 가지입니다. 두 반을 함께 붙여넣으셨습니까?
     [ 301반만 두기 ]  [ 302반만 두기 ]  [ 둘 다 두기 ]

  학번이나 이름이 빈 행 2개를 건너뛰었습니다. (3행, 51행)
  ──────────────────────────────────────────────────────────────

  이 검사가 못 잡는 것 — 이름끼리 자기들끼리 뒤바뀐 명단입니다.       ④
  표의 이름 몇 개를 눈으로 확인해 주세요.                    ← 늘 같은 자리

   순번   학번     이름                                             ⑤
     1   30101   김하늘
     2   30102   이서준
     3   30102   박도윤    중복        ← 낱말이 먼저, 색은 보조
     …
```

`점수`를 붙여넣은 뒤에만 아래가 추가로 나타난다.

```
  ──────────────────────────────────────────────────────────────
  제출 여부

  점수가 없는 학생 3명입니다. 미제출입니까?      ← 기본값 없음. 판단 3회

     30117  최유진     ( ) 미제출   ( ) 제출했음
     30124  정하람     ( ) 미제출   ( ) 제출했음
     30131  한서윤     ( ) 미제출   ( ) 제출했음

  미제출로 표시하면 활동 참여 사실만 담은 예외 세특으로 처리됩니다.
```

#### ≤900px

동일. 다만 ②의 `학번 / 이름` 두 줄이 라벨-값 세로 배치를 유지하고,
⑤ 표는 `overflow-x: auto` 컨테이너 안에서 가로 스크롤한다
(페이지 본문은 절대 가로 스크롤하지 않는다).
③의 복구 동작 단추는 문장 아래 줄바꿈되어 세로로 쌓인다.

#### 요소 목록

| 요소 | 규칙 |
|---|---|
| ① 대조 줄 | `--t-body` 17px, 굵기 600, `aria-live="polite"`. 불일치면 `--stop-ground` 배경 + `--stop` 좌측 2px, 240ms 페이드 인, **사라지지 않는다** |
| ② 열 확인 | **열 번호를 절대 말하지 않는다.** 그 열의 첫 행 실제 값만. `via`가 헤더든 값 판별이든 **항상** 나온다 |
| ②-단추 | `[맞습니다]`를 누르기 전에는 `저장` 구역에 단추가 존재하지 않는다(A7) |
| ③ 정오표 | 표 **위**에 있다. 각 항목 = 사실 문장 + 원인 가설 문장 + 복구 동작 단추(A8) |
| ③-0건 | `버린 행은 없습니다.` — 0이어도 **문장으로 항상 보인다** |
| ④ 한계 고지 | 경고가 하나도 없어도 상설. 설계 원칙 2의 문자적 구현 |
| ⑤ 표 | 중복 행 마지막 칸에 `중복` 낱말. 배경 틴트는 보조(대비 §3.3) |
| 수치 0 | 언제나 `--ink-3` 평문. **초록으로 그리지 않는다** |
| 재파싱 | `paste`·`change`·`blur` 즉시, `input`은 300ms 디바운스(A15) |

---

### 2.3 검수

한 번에 **하나만** 펼쳐진다. 나머지는 한 줄짜리 색인이다. 카드 40장 → 1장.

#### 1280px (검수 구역만 폭 1032px — A14)

```
  ──────────────────────────────────────────────────────────────────────────
  검수 · 48명 중 12명 마침 · 수정 9건                   aria-live=polite

  먼저 볼 12명                                    ← h2. 태그가 아니라 제목이다
  ──────────────────────────────────────────────────────────────────────────
   예외 학생          30117  최유진                              704바이트
     만복사저포기 · 산 자와 죽은 자의 경계          ← 둘째 줄 = 핵심소재(A10)

   분량 상한 근접     30104  강민서                              758바이트
     만복사저포기 · 서사 구조와 시점

  ┌── 펼쳐진 한 명 ───────────────────────────────────────────────────────┐
  │ 30104  강민서    분량 상한 근접 · 만복사저포기 · 서사 구조와 시점      │
  │                                                                        │
  │  학생 원문                        │  세특                              │
  │  ─────────                        │  ────                              │
  │  「만복사저포기」에서 양생이       │  「만복사저포기」의 서사 구조를    │
  │  죽은 여인과 사랑을 나누는 장면을 │  분석하며 산 자와 죽은 자의 경계가 │
  │  읽으며 나는 …                    │  …                                 │
  │                                   │                                    │
  │  (inset · 읽기만 · 17px/1.75)     │  (raised · 편집 가능 · 17px/1.75)  │
  │  ↑ 두 칸의 첫 줄 baseline이 같다  │  ↑ 밑줄 한 줄. 상자 아님           │
  │  ↑ 칸 높이 = max(원문,세특,14rem) │                                    │
  │    — 학생이 바뀌어도 눈이 같은     │  758바이트 (NEIS 기준)             │
  │      자리에 있다 (A15)            │  상한 760 · 남은 2바이트           │
  │                                   │  금지 낱말이 있습니다 — 이는       │
  │                                   │  ─────────────────────────         │
  │                                   │  왜 고쳤는지 한 줄 (선택)          │
  │                                   │  [ 마치고 다음  ⌃⏎ ]               │
  └────────────────────────────────────────────────────────────────────────┘

   비고에 '확인' 포함  30122  오지호                              689바이트
     모닥불 · 공동체의 온기
   …

  나머지 36명                                     ← h2. 기본 펼침
  ──────────────────────────────────────────────────────────────────────────
   30101  김하늘                                  712바이트      수정됨  마침
     모닥불 · 공동체의 온기
   30102  이서준                                  698바이트
     모닥불 · 공동체의 온기
   30103  박도윤                                  705바이트
     모닥불 · 공동체의 온기          ← 세로로 훑으면 반복이 눈에 잡힌다
   …
```

#### ≤900px

```
  ──────────────────────────────────────────────
  검수 · 48명 중 12명 마침 · 수정 9건

  먼저 볼 12명
  ──────────────────────────────────────────────
   예외 학생        30117  최유진      704바이트
     만복사저포기 · 산 자와 죽은 자의 경계

  ┌── 펼쳐진 한 명 ───────────────────────────┐
  │ 30104  강민서   분량 상한 근접             │
  │                                            │
  │  학생 원문   (inset, 전폭)                 │
  │  「만복사저포기」에서 양생이 …             │
  │  ───────────────────────────────────────   │
  │  세특        (raised, 전폭, 편집 가능)     │
  │  「만복사저포기」의 서사 구조를 …          │
  │                                            │
  │  758바이트 (NEIS 기준) · 상한 760          │
  │  [ 마치고 다음  ⌃⏎ ]                       │
  └────────────────────────────────────────────┘
```

원문이 **위**, 세특이 아래. 원문을 세특보다 작은 스크롤 상자로 만들지 않는다.
둘 다 전폭·같은 크기·같은 행간.

#### 요소 목록과 규칙

| 요소 | 규칙 |
|---|---|
| 목록 | `<ul>` + roving tabindex. `role="listbox"`/`option` **쓰지 않는다**(표준 어휘 재발명 금지) |
| 행 1줄 | 사유(맨 왼쪽) · 학번 · 이름 · 바이트 |
| 행 2줄 | **핵심소재**(A10). 데이터에 이미 `s.핵심소재`가 있다 — 새 필드 불필요 |
| 우선 검토 | 태그 아님. `먼저 볼 12명` / `나머지 36명` **두 개의 `h2`**로 구조화. 사유는 줄 맨 왼쪽 낱말 |
| 마침 표시 | **행이 이동하지 않는다**(A6). 제자리에 남고 `마침` 낱말 + 글자색 `--ink-3`. 재정렬은 파일을 다시 열 때만 |
| 원문 | `--surface-sunken` (inset), `readonly` 아님 — `<div>`. 선택·복사 가능 |
| 세특 | `--surface` + 아래쪽 `--rule-strong` 1px (raised). `<textarea>`, 자동 높이 |
| 두 칸 정렬 | 같은 `line-height: 1.75`, 같은 `padding-top`, 같은 baseline. 칸 높이 `min-height: max(내용, 14rem)` (A15) |
| 바이트 | **한 숫자만.** `758바이트 (NEIS 기준) · 상한 760 · 남은 2바이트`. UTF-8은 화면에서 삭제 |
| 재계산 | 300ms 디바운스. `blur`·`paste`는 즉시(A15) |
| 접힘 | `마친 N명 ▾` 별도 목록 **폐기**(A6로 불필요해짐) |

---

### 2.4 저장 완료

#### 명단 저장 (1280px)

```
  ──────────────────────────────────────────────────────────────────
  저장

  ┌ 이 컴퓨터에만 두는 것 ─────────┐ ┌ AI에게 주는 것 ──────────────┐
  │                                │ │                              │
  │  명렬.json                      │ │  이 화면에서는 아직 만들지    │
  │  48명 · 실명과 학번 있음         │ │  않았습니다.                 │
  │  내려받기 폴더 확인             │ │                              │
  │                                │ │  검수를 마치면 여기에        │
  │  이 파일은 활동 폴더에만 둡니다. │ │  검수diff(AI용).json이 생깁니다.│
  │  대화창에 열어서 붙여넣지 마세요.│ │  이름도 학번도 들어가지 않습니다.│
  └────────────────────────────────┘ └──────────────────────────────┘

  명렬.json을 내려받았습니다. 브라우저가 막았을 수 있으니 내려받기 폴더에
  파일이 있는지 확인해 주세요. 화면은 폴더를 볼 수 없어 여기까지만
  말씀드립니다.                                        ← A13. 단언하지 않는다

  [ 화면에서 명단 지우기 ]
  저장을 마치셨으면 눌러 주세요. 이 브라우저 창에서 이름과 학번이 사라집니다.
  파일은 그대로 남습니다.
```

#### 검수 저장 — 두 단계, 두 클릭

```
  ──────────────────────────────────────────────────────────────────
  검수 저장

  검수 완료 표시가 없는 학생이 12명 있습니다.        ← 인라인. alert 아님
     [ 그래도 저장 ]   [ 12명 보러 가기 ]            ← 세 번째 선택지

  [ AI에게 줄 파일 저장 ]                             ← 1단계

     ↓ 클릭 후 이 자리가 바뀐다

  검수diff(AI용).json을 내려받았습니다 — 수정 9건.
  대화창에는 이 파일만 주세요.

  [ 실명 검수본도 저장 ]                              ← 2단계

     ↓ 클릭 후

  검수본(실명·보관용).json을 내려받았습니다.
  방금 받은 두 파일 중 이름에 (실명·보관용)이 붙은 쪽이 더 최근입니다.
  대화창에 넣을 것은 (AI용)이 붙은 쪽입니다.          ← A4. recency 함정 명시
```

동시에 오른쪽 역할 상자가 채워진다.

```
  ┌ AI에게 주는 것 ────────────────┐
  │  검수diff(AI용).json            │
  │  수정 9건 · 이름·학번 없음       │
  │  내려받기 폴더 확인             │
  │  이 파일을 대화창에 주세요.      │
  └────────────────────────────────┘
```

#### ≤900px

역할 상자 2개가 세로로 쌓인다. 순서는 유지(**이 컴퓨터에만 두는 것**이 위).
두 단계 저장 단추는 각각 전폭. 그 외 동일.

---

## 3. 디자인 토큰

### 3.1 원칙

- 종이 느낌은 배경색이 아니라 **조판**(§4의 활자·행간·34자 줄·8px 눈금)에서 낸다.
- **크림 금지 대역 회피를 hue로 보장한다.** 금지 대역은
  `OKLCH L 0.84–0.97 ∧ C < 0.06 ∧ hue 40–100`. 아래 모든 중립 표면은
  **hue 214–240(냉중립)**이라 대역에서 최소 114° 떨어져 있다.
  유채색 표면(`--accent-quiet` H 158, `--stop-ground` H 25.6)도 대역 밖이다.
  **팔레트에 크림 대역 토큰은 0개다**(§3.4에 검증표).
- 토큰명에 `paper / cream / sand / ivory` 없음. 표면은 **높이와 역할**로 이름 짓는다.
- 다크 중립의 hue를 현재 코드의 **271 → 229~240**으로 옮긴다. 271은 보라 기울기이고
  반참조 "AI 챗봇"의 색이다.
- **성공색이 없다.** 초록은 `--accent`(주 버튼·현재 선택)뿐이고, 포커스는
  별도 청색 `--focus`다(A18). "다 잘됐다"를 색으로 말하지 않는다.
- 레이어는 **3겹**: 페이지(`--surface`) · 우묵한 자리(`--surface-sunken`) ·
  패널(`--panel`, 색인 전용). "raised"는 4번째 층이 아니라
  **sunken 옆에 있는 page 표면 + 밑줄 1px**로 표현한다. 그림자 없음.

### 3.2 라이트

| 토큰 | hex | OKLCH (L C H) | 용도 |
|---|---|---|---|
| `--surface` | `#fdfdfe` | 0.994 0.0013 286 | 페이지, 세특 칸(raised) |
| `--surface-sunken` | `#eef2f3` | 0.958 0.0045 214 | 학생 원문(inset), hover 배경, 파일명 |
| `--panel` | `#f2f5f6` | 0.968 0.0035 220 | 좌측 색인 (product 레지스터의 second neutral) |
| `--ink` | `#1a1f21` | 0.235 0.0083 224 | 본문, 학생 원문, 세특 |
| `--ink-2` | `#4c5356` | 0.437 0.0102 225 | 설명문 |
| `--ink-3` | `#5d6468` | 0.499 0.0108 233 | 수치·라벨·0·마친 항목 **[변경: #6a7175에서 어둡게]** |
| `--rule` | `#dde0e2` | 0.905 0.0043 237 | 구역 가로선(장식, 대비 요구 없음) |
| `--rule-strong` | `#81878a` | 0.619 0.0084 229 | 입력·단추 경계 (1.4.11) |
| `--rule-double` | `#6c7275` | 0.548 0.0087 229 | **이중선 전용 [신설]** |
| `--accent` | `#1f6543` | 0.454 0.0893 158 | 주 단추 배경, 현재 선택 |
| `--accent-ink` | `#fdfdfe` | 0.994 0.0013 286 | 주 단추 글자 |
| `--accent-quiet` | `#ddf3e5` | 0.944 0.0297 158 | 펼쳐진/선택된 검수 행 |
| `--focus` | `#1b4fd0` | 0.482 0.2063 264 | **포커스 링 전용 [신설]** |
| `--flag` | `#8b5214` | 0.494 0.1045 62 | 우선 검토 사유, 주의 — **글자·선색만, 배경 없음** |
| `--stop` | `#a72a2a` | 0.484 0.1617 26 | 불일치·초과·중복 |
| `--stop-ground` | `#ffeeec` | 0.962 0.0187 26 | 불일치 한 줄 배경(유일한 채운 경고면) |

### 3.3 다크

| 토큰 | hex | OKLCH (L C H) | 용도 |
|---|---|---|---|
| `--surface` | `#111517` | 0.192 0.0074 229 | 페이지, 세특 칸 |
| `--surface-sunken` | `#06090a` | 0.136 0.0064 217 | 학생 원문, hover 배경 |
| `--panel` | `#191d20` | 0.228 0.0084 240 | 좌측 색인 |
| `--ink` | `#e5e8ea` | 0.929 0.0042 237 | 본문 |
| `--ink-2` | `#aaafb2` | 0.751 0.0071 234 | 설명문 |
| `--ink-3` | `#949a9d` | 0.682 0.0082 229 | 수치·라벨·0 **[변경: #8f9598에서 밝게]** |
| `--rule` | `#2b3032` | 0.305 0.0079 224 | 구역 가로선 |
| `--rule-strong` | `#6a7175` | 0.544 0.0106 233 | 입력·단추 경계 |
| `--rule-double` | `#98a0a3` | 0.700 0.0102 222 | 이중선 전용 |
| `--accent` | `#72c496` | 0.755 0.1053 158 | 주 단추 배경 |
| `--accent-ink` | `#06170d` | 0.186 0.0314 157 | 주 단추 글자 **← 라이트와 반전** |
| `--accent-quiet` | `#13281c` | 0.255 0.0356 158 | 펼쳐진/선택된 검수 행 |
| `--focus` | `#8fb4ff` | 0.772 0.1154 264 | 포커스 링 |
| `--flag` | `#ebb878` | 0.815 0.1004 72 | 우선 검토 사유 |
| `--stop` | `#f28881` | 0.740 0.1305 25 | 불일치·초과·중복 |
| `--stop-ground` | `#391b18` | 0.264 0.0480 27 | 불일치 한 줄 배경 |

### 3.4 크림 금지 대역 검증

판정식 `(0.84 ≤ L ≤ 0.97) ∧ (C < 0.06) ∧ (40 ≤ H ≤ 100)`를 32개 토큰 전부에
적용한 결과 **해당 0개**. 아슬아슬한 것만 적는다.

| 토큰 | L | C | H | 왜 안전한가 |
|---|---|---|---|---|
| 라이트 `--surface-sunken` | 0.958 | 0.0045 | **214** | hue가 대역에서 114° 이탈 |
| 라이트 `--panel` | 0.968 | 0.0035 | **220** | 동일 |
| 라이트 `--rule` | 0.905 | 0.0043 | **237** | 동일 |
| 라이트 `--accent-quiet` | 0.944 | 0.0297 | **158** | 동일 |
| 라이트 `--stop-ground` | 0.962 | 0.0187 | **26** | hue가 대역 하한보다 14° 아래 |
| 다크 `--flag` | 0.815 | 0.1004 | 72 | **L이 하한 0.84 미만 + C가 0.06 초과** (이중 이탈) |

현재 코드의 `--warnbg #fdf6e3`(L 0.968 / C 0.026 / **H 90**)는 세 조건을 모두
만족하는 **팔레트 유일의 크림 표면이었다.** 이 브리프에서는 `--flag`에
배경이 존재하지 않으므로 그 토큰 자체가 소멸한다(§3.6).

### 3.5 대비비 (직접 계산, WCAG 2.x 상대휘도)

**본문 조합 (기준 4.5:1)**

| 조합 | 라이트 | 다크 |
|---|---|---|
| `--ink` / `--surface` | **16.37** | **14.92** |
| `--ink-2` / `--surface` | **7.71** | **8.29** |
| `--ink-3` / `--surface` | **5.92** | **6.44** |
| `--ink` / `--surface-sunken` (학생 원문) | **14.76** | **16.23** |
| `--ink-2` / `--surface-sunken` | **6.95** | **9.02** |
| `--ink-3` / `--surface-sunken` (hover 위 수치) | **5.34** | **7.01** |
| `--ink` / `--panel` (색인) | **15.19** | **13.78** |
| `--ink-2` / `--panel` | **7.15** | **7.66** |
| `--ink-3` / `--panel` | **5.49** | **5.95** |
| `--ink` / `--accent-quiet` (펼쳐진 행 본문) | **14.29** | **12.66** |
| `--ink-3` / `--accent-quiet` (펼쳐진 행 **바이트 수**) | **5.17** | **5.47** |
| `--accent` / `--accent-quiet` | **6.01** | **7.46** |
| `--accent` / `--surface` | **6.88** | **8.79** |
| `--accent` / `--panel` | **6.39** | **8.12** |
| **`--accent-ink` / `--accent` (주 단추)** | **6.88** | **8.85** |
| `--focus` / `--surface` | **6.72** | **8.87** |
| `--focus` / `--surface-sunken` | **6.06** | **9.65** |
| `--focus` / `--panel` | **6.23** | **8.19** |
| `--flag` / `--surface` | **6.23** | **10.20** |
| `--flag` / `--surface-sunken` | **5.62** | **11.09** |
| `--flag` / `--panel` | **5.78** | **9.42** |
| `--stop` / `--surface` | **6.87** | **7.53** |
| `--stop` / `--surface-sunken` | **6.19** | **8.19** |
| `--ink` / `--stop-ground` | **14.81** | **12.70** |
| `--stop` / `--stop-ground` | **6.22** | **6.41** |

**전 조합 4.5:1 이상. 미달 0건. "쓰지 않는 조합" 목록이 필요 없다** —
`--ink-3`를 어둡게(라이트) / 밝게(다크) 조정해 **모든 표면에서 통과**시켰기
때문이다. 원안의 봉인 규칙(`--ink-3`/`--surface-sunken` 4.40 = 쓰지 말 것)이
hover 배경 지정과 충돌했고, 규칙끼리 충돌하는 팔레트는 구현 중 반드시 샌다.

**비텍스트 대비 (기준 3:1, WCAG 1.4.11)**

| 조합 | 라이트 | 다크 |
|---|---|---|
| `--rule-strong` / `--surface` (입력·단추 경계) | **3.58** | **3.70** |
| `--rule-strong` / `--surface-sunken` | **3.23** | **4.03** |
| `--rule-strong` / `--panel` | **3.32** | **3.42** |
| `--rule-double` / `--surface` (**이중선**) | **4.80** | **6.90** |

`--rule`(구역 가로선)은 라이트 1.30 / 다크 1.29로 3:1 미달이지만 **의도적**이다.
순수 장식이고 어떤 정보도 나르지 않는다. **정보를 나르는 선은 전부
`--rule-strong` 이상을 쓴다.** 특히 이 안의 최대 교육 장치인
「여기서 대화로 돌아갑니다」 이중선은 `--rule-double`(4.80 / 6.90)로 긋는다 —
원안은 이 선의 토큰을 지정하지 않아 기본값 1.30으로 구현될 위험이 있었다(A5).

**층 분리 (양 테마 등가 확인)**

| 조합 | 라이트 | 다크 | 차 |
|---|---|---|---|
| `--surface-sunken` / `--surface` | 1.11 | 1.09 | 0.02 |
| `--panel` / `--surface` | 1.08 | 1.08 | 0.00 |

두 테마가 **같은 구조로 읽힌다.** 현재 코드는 라이트 카드 층이 1.035,
다크가 1.089로 사실상 층 하나가 라이트에만 없었다.

### 3.6 색을 쓰지 않는 규칙

- **성공색 없음.** 초록은 `--accent`뿐이고 주 단추·현재 선택에만 쓴다.
- **포커스는 초록이 아니라 청색**(`--focus`). 선택·성공과 혼동되면 안 된다(A18).
- **수치 0은 언제나 `--ink-3` 평문.** 현재 코드의
  `[dup ? "bad" : "", '중복 ' + dup]`은 0일 때 클래스가 빈 문자열이 되어
  `.chip` 기본 배경 `--okbg`(연녹)로 **0을 성공색으로 렌더**한다. 폐기한다.
- **모든 상태는 낱말을 동반한다.** `중복` `초과` `미제출` `수정됨` `마침`
  `우선 검토 사유`. 색은 낱말 위에 얹히는 **두 번째** 신호다.
- **`--flag`에 배경이 없다.** 라이트에서 노란 채움을 만들면 필연적으로
  크림 금지 대역에 들어간다. 글자색과 선색으로만 존재한다. 양 테마 동일 규칙.
- **채운 경고면은 `--stop-ground` 한 곳뿐**(인원 불일치 한 줄).
- **비활성에 `opacity`를 쓰지 않는다.** 현재 `.card.done{opacity:.55}`는 본문을
  3.90, muted를 2.23으로 떨어뜨리면서 textarea는 여전히 편집 가능하고,
  `tr.absent{opacity:.45}`는 2.89다. 색 토큰을 `--ink-3`로 교체하고
  필요한 곳에는 실제 `disabled`를 건다.

---

## 4. 타이포 스케일

### 4.1 폰트 스택 (외부 로드 없음 — 인터넷 없이 동작해야 한다)

```css
--font: "Apple SD Gothic Neo", "Malgun Gothic", "맑은 고딕",
        system-ui, -apple-system, "Segoe UI", sans-serif;
--font-mono: "D2Coding", Consolas, ui-monospace, monospace;
```

**한글 폰트를 반드시 먼저 놓는다.** `system-ui` 또는 `Segoe UI`를 앞에 두면
한국어 윈도우에서 라틴 글리프는 Segoe UI가, 한글만 글자 단위로 맑은 고딕이
잡혀 **한 줄 안에서 라틴과 한글의 무게가 어긋난다.** 이 화면은 학번(라틴 숫자)과
이름(한글)이 같은 줄에 붙어 있으므로 눈에 띈다.

모노는 **파일명과 토큰(`S-08F4`)에만.** 학생 원문·세특에는 절대 쓰지 않는다.
UI 라벨·단추·데이터에 디스플레이 서체를 쓰지 않는다(product 금지).

### 4.2 스케일 — 고정 rem, 5단

현재 코드는 12종이 0.19rem 안에 9단계로 몰려 있고 인접 비가 1.01~1.03이다.
스케일이 아니라 흩어짐이다. 5단으로 줄인다. `clamp()` 등 유동 타이포 금지
(product 레지스터: 사용자는 일정한 DPI로 본다).

| 토큰 | rem / px | 행간 | 자간 | 용도 |
|---|---|---|---|---|
| `--t-title` | `1.5rem` / 24px | 1.35 | 0 | 페이지 제목. 문서 전체에 `h1` **하나** |
| `--t-section` | `1.1875rem` / 19px | 1.45 | 0 | 구역 제목(`h2`). **대문자·자간·muted 없음** |
| `--t-body` | `1.0625rem` / **17px** | **1.75** | 0 | 학생 원문, 세특, 설명문, 대조 줄 |
| `--t-ui` | `0.9375rem` / 15px | 1.5 | 0 | 단추, 라벨, 표, 색인 |
| `--t-meta` | `0.8125rem` / 13px | 1.5 | 0 | 바이트, 순번, 파일 부기 |

인접 비 1.26 / 1.12 / 1.13 / 1.15.

**규칙: `--t-body`(17px) > `--t-ui`(15px).** PRODUCT.md의
"본문 크기·행간을 UI 라벨보다 우선한다"가 스케일에 박혀 있다. 현재 코드는
`.source`(학생 원문) 13.92px < `.tabs button` 14.88px으로 **정확히 반대**다.

**제거하는 것**: `h2/h3`의 `text-transform: uppercase` + `letter-spacing: .08em`
+ `--muted`. 금지 목록의 "모든 섹션 위 작은 대문자 eyebrow" 직격이고,
게다가 `uppercase`는 "붙여넣기"에 아무 효과가 없어 실효는 구역 제목을
12px 최저 대비로 만든 것뿐이었다. `1 · 붙여넣기` 같은 번호 스캐폴딩도 제거 —
세로 순서가 곧 번호이므로 중복이다.

### 4.3 한글 가독 규칙

```css
line-height: 1.75;                  /* 한글 낱자는 네모칸을 채워 라틴보다 더 필요 */
letter-spacing: 0;                  /* 한글 자간은 어절 경계를 흐린다 */
word-break: keep-all;               /* 어절 중간에서 끊지 않는다 */
overflow-wrap: break-word;          /* 단, 긴 토큰은 넘치지 않게 */
font-variant-numeric: tabular-nums; /* 학번·바이트가 세로로 정렬된다 */
```

`text-align: justify` **금지** — 한글 양끝맞춤은 어절 간격이 널뛴다.

### 4.4 줄 길이 상한

| 대상 | 상한 | 한글 기준 |
|---|---|---|
| 설명문·경고문·안내문 | `--measure: 34rem` (≈578px) | 약 34자 |
| 본문 기둥(표·구역) | 880px | — |
| **검수 구역** | **1032px** | — |
| 검수 2열(1280px) | 각 약 500px | 약 29자 |
| 검수 2열(1440px 이상) | 각 약 560px | 약 33자 |
| 표 | 제한 없음, `overflow-x: auto` 컨테이너 안 | — |

현재 산문 줄 길이는 약 83자이고 **하필 가장 긴 것이 실명 경고문**이다.
가장 중요한 문장이 가장 읽기 어렵다.

**[변경 A14]** 원안은 검수 구역도 880px로 묶어 2열이 각 424px(≈25자)이 됐고,
원안 자신이 §11.5에서 "이 도구에서 가장 오래 보는 화면에서 지불하는 값"이라
인정했다. 검수 구역만 1032px로 푼다. 1280px 화면에서
`색인 200 + 여백 24 + 검수 1032 = 1256`으로 들어가고, 열은 각 약 500px이 된다.
탭 구조의 520px에 거의 근접한다.

### 4.5 세로 리듬 — 4px 기준 8단

```css
--s1: 4px;  --s2: 8px;  --s3: 12px; --s4: 16px;
--s5: 24px; --s6: 32px; --s7: 48px; --s8: 64px;
```

| 자리 | 값 |
|---|---|
| 구역과 구역 사이 | `--s8` |
| 구역 안 블록 사이 | `--s5` |
| 라벨과 값 사이 | `--s2` |
| 목록 행 세로 패딩 | `--s3` |
| 단추 안쪽 여백 | `--s2` `--s4` |

현재 코드는 15종이고 공배수가 없다.
**종이의 느낌은 여기서 나온다 — 배경색이 아니라 이 눈금에서.**

---

## 5. 컴포넌트 상태표

현재 코드는 `:hover` 0건 / `:active` 0건 / `:focus-visible` 0건이고,
포커스 스타일은 `textarea:focus` 한 줄인데 `outline-offset: -1px`라 글자를 덮는다.

### 5.1 포커스 링 (전 컴포넌트 공통)

```css
:focus-visible {
  outline: 2px solid var(--focus);
  outline-offset: 2px;              /* 바깥으로. 글자를 덮지 않는다 */
  border-radius: inherit;
}
```

- 색은 `--focus`(청) — `--accent`(초록)와 분리한다. 포커스가 선택·성공과
  혼동되면 안 된다(A18).
- 대비: 라이트 6.72 / 다크 8.87 (기준 3:1의 두 배 이상).
- `transition` **없음**(0ms). 포커스는 즉시 보여야 한다.
- `outline: none`을 어디에도 쓰지 않는다.

### 5.2 주 단추 (`저장`, `AI에게 줄 파일 저장`)

| 상태 | 사양 |
|---|---|
| default | 배경 `--accent`, 글자 `--accent-ink`, 경계 없음, `--s2 --s4` |
| hover | 배경 `--accent` 밝기 ±6%(라이트 어둡게 / 다크 밝게), 120ms |
| focus-visible | §5.1 |
| active | 배경 hover와 동일 + `transform` 없음 |
| disabled | **쓰지 않는다.** 조건 미충족이면 단추가 아예 **존재하지 않는다**(A7) |
| loading | 스피너 없음. 라벨 교체 `저장` → `저장 중…` (§8) |
| error | 단추 아래 인라인 문장. 단추는 원상 복귀 |

### 5.3 보조 단추 (`맞습니다`, `뒤엣것 지우기`, `12명 보러 가기`)

| 상태 | 사양 |
|---|---|
| default | 배경 `--surface`, 글자 `--ink`, 경계 `--rule-strong` 1px |
| hover | 배경 `--surface-sunken`, 120ms |
| focus-visible | §5.1 |
| active | 배경 `--accent-quiet` |
| disabled | 글자 `--ink-3`, 경계 `--rule`, `cursor: default`, **실제 `disabled` 속성** |
| loading | 해당 없음 |
| error | 해당 없음 |

### 5.4 텍스트 입력 (`인원`) / 여러 줄 입력 (`명단`, `점수`, `세특`)

| 상태 | 사양 |
|---|---|
| default | 배경 `--surface`, 경계 `--rule-strong` 1px, 글자 `--ink` |
| hover | 경계 `--ink-3` |
| focus-visible | §5.1 + 경계 `--ink-3` |
| active(입력 중) | default와 동일 |
| disabled | **명단·점수 상자에는 절대 쓰지 않는다**(A2) |
| loading | 없음(로컬 파싱은 즉각이다. 스켈레톤도 없다) |
| error | 경계 `--stop` 1px + 아래 `--stop` 문장. **placeholder를 오류 문구로 바꾸지 않는다** |

**인원 입력 특칙 (A1)**
- `type="text"` + `inputmode="numeric"` + `pattern="[0-9]{1,3}"`.
  `type="number"`는 붙여넣은 표를 **조용히 버린다**.
- **자동 포커스 없음.** 교사는 엑셀에서 두 열을 복사한 상태로 이 창에 온다 —
  자동 포커스가 있으면 반사적 Ctrl+V가 인원 칸으로 들어간다.
- `paste` 핸들러: 붙여넣은 문자열에 탭·줄바꿈이 있거나 길이가 4자를 넘으면
  **인원 칸에 넣지 않고 명단 상자로 넘긴 뒤** 문장으로 알린다(§6.2).

**세특 입력 특칙 (A15)**
- 상자가 아니라 **밑줄**: `border: 0; border-bottom: 1px solid var(--rule-strong)`.
  상자를 그리면 "폼"이 되고, 이 화면은 폼이 아니라 원고다.
- 바이트·금지어 재계산은 `input`에서 **300ms 디바운스**, `blur`·`paste`는 즉시.
  현재 코드는 매 타건마다 재계산해 700바이트 근처를 손보는 내내 숫자와
  `금지:` 문구가 명멸한다.

### 5.5 체크박스 / 라디오 (`미제출` / `제출했음`)

네이티브 `<input type="radio">`를 쓴다. 커스텀 컨트롤을 만들지 않는다.

| 상태 | 사양 |
|---|---|
| default | `accent-color: var(--accent)`, 라벨 `--t-ui` `--ink` |
| hover | 라벨 배경 `--surface-sunken` (클릭 영역이 라벨 전체임을 알린다) |
| focus-visible | §5.1 (`input`에 걸린다) |
| active | 네이티브 |
| **checked 없음(초기)** | **어느 쪽도 선택돼 있지 않다.** 기본값이 곧 오답이 되는 것을 막는다 |
| disabled | 사용하지 않음 |

**`제출` 열은 점수를 붙여넣기 전에는 존재하지 않는다.** 현재 코드는 파싱 전에
전원 체크된 채로 48개를 보여 준다.

### 5.6 색인 (탭 대체 — 네비게이션)

| 상태 | 사양 |
|---|---|
| default | 글자 `--ink-2`, 배경 `--panel`, `--t-ui` |
| hover | 글자 `--ink`, 밑줄 없음, 120ms |
| focus-visible | §5.1 |
| active | 글자 `--ink` |
| current | 글자 `--ink`, `font-weight: 600`, **`aria-current="true"`**. transition 없음 |
| disabled | 없음 |

- 숫자·원·연결선·체크마크 **금지**. 붙이는 순간 스테퍼가 되고 금지 목록의
  "01/02/03 번호 스캐폴딩"이 된다. **글자 목록 + 굵기 하나**가 전부다.
- `세특 ― / 대화에서 만듭니다` 항목은 링크가 아닌 `li`이고 글자 `--ink-3`.
  이 줄이 다섯 번째 반참조(결과가 쏟아지는 화면)에 대한 기대를 **스크롤과
  무관하게** 꺾는다(A16).
- ≤900px에서는 `<details><summary>차례</summary>` 네이티브 요소로 접는다.
  **커스텀 드로어를 만들지 않는다**(A17, 표준 어포던스 재발명 금지).

### 5.7 검수 목록 항목

| 상태 | 사양 |
|---|---|
| default | 배경 `--surface`, 아래 `--rule` 1px |
| hover | 배경 `--surface-sunken`, 120ms |
| focus-visible | §5.1 (roving tabindex의 현재 `li`) |
| active | 배경 `--accent-quiet` |
| selected(펼쳐짐) | 배경 `--accent-quiet`, 학번 `font-weight: 600` |
| 마침 | 오른쪽 끝에 `마침` 낱말 + 행 글자 `--ink-3`. **위치는 그대로**(A6) |
| disabled | 없음 |

### 5.8 알림 (인라인 블록 — 모달 없음)

`alert()` / `confirm()`을 전부 제거한다(product 금지: modal as first thought).

| 종류 | 사양 |
|---|---|
| 대조 불일치 | 배경 `--stop-ground`, 좌측 `--stop` 2px, 글자 `--ink`, `aria-live="polite"`. **사라지지 않는다** |
| 주의(우선 검토·상한 근접) | 배경 없음. 글자 `--flag`, 낱말 동반 |
| 정오표 항목 | 배경 없음. 사실 문장(`--ink`) + 원인 가설(`--ink-2`) + 복구 단추(§5.3) |
| 저장 결과 | 배경 없음. 글자 `--ink`, `aria-live="polite"` |
| 확인 요청 | 저장 단추 **바로 위** 인라인 블록. 선택지 3개(§6.6) |

---

## 6. 한국어 마이크로카피 전문

말투 규칙: **무엇을 하라를 먼저, 왜인지를 나중에.** 전문 용어를 쓰지 않는다.
겁주지 않되 숨기지 않는다. 느낌표를 쓰지 않는다. 이모지를 쓰지 않는다.
"AI가 도와드릴게요" 투를 쓰지 않는다.
아래 문구는 **화면에 들어갈 실물 그대로**다. 구현자는 고쳐 쓰지 말 것.

### 6.1 첫 화면

| 자리 | 문구 |
|---|---|
| 페이지 제목 | `세특 작업 도구` |
| 상단 상태(초기) | `이 화면에서 저장한 것 없음` |
| 색인 항목 | `인원` `명단` `점수` `저장` / `세특 ―` `대화에서 만듭니다` / `검수` `검수 저장` `정리` |
| 인원 구역 제목 | `이 활동 대상은 몇 명입니까` |
| 인원 설명 | `명단을 보기 전에 아시는 수를 적어 주세요. 정원에서 전출·자퇴를 뺀 수입니다. 이 수와 붙여넣은 명단이 다르면 화면이 그 자리에서 말합니다.` |
| 인원 단위 | `명` |
| 명단 구역 제목 | `명단 붙여넣기` |
| 명단 설명 | `엑셀에서 학번 열과 이름 열을 함께 긁어(Ctrl+C) 붙여넣으세요. 따로 두 번 붙이면 행이 어긋나고, 어긋난 명단은 어떤 검사로도 잡히지 않습니다.` |
| 명단 placeholder | `30101\t김하늘`<br>`30102\t이서준` (두 열 예시가 곧 안내다) |
| 점수 구역 제목 | `점수 (선택)` |
| 점수 설명 | `채점표에서 학번 열과 점수 열을 함께 긁어 붙여넣으세요. 비워 두면 점수 없이 진행합니다.` |
| 점수 대기 | `명단을 먼저 붙여넣으시면 어느 열이 점수인지 대조할 수 있습니다.` |
| 저장 구역 제목 | `저장` |
| 이중선 제목 | `여기서 대화로 돌아갑니다` |
| 이중선 본문 | `저장을 마치면 규칙·구조·표본을 정하는 대화가 20~60분 이어집니다. 그동안 이 창은 닫으셔도 됩니다. 끝나면 다시 열어 아래로 오세요.` |
| 검수 구역 제목 | `검수` |
| 정리 구역 제목 | `정리` |
| 정리 본문 | `검수까지 마치면 실명이 든 중간 파일을 지웁니다. 대화창에 이렇게 말씀하세요.` |
| 정리 예문 | `중간 파일 파기해 줘` |
| 정리 부기 | `지울 목록을 먼저 보여 드리고, 확인하신 뒤에 지웁니다.` |

### 6.2 빈 상태 (teach the interface — "아무것도 없습니다"라고 쓰지 않는다)

| 자리 | 문구 |
|---|---|
| 역할 상자 — 왼쪽(초기) | `이 화면에서는 아직 만들지 않았습니다.`<br>`활동 폴더에 이미 있을 수 있습니다 — 화면은 폴더를 볼 수 없습니다.` |
| 역할 상자 — 오른쪽(초기) | `이 화면에서는 아직 만들지 않았습니다.`<br>`검수를 마치면 여기에 검수diff(AI용).json이 생깁니다. 이름도 학번도 들어가지 않습니다.` |
| 검수 빈 상태 | `원문과 세특을 나란히 놓고 고치는 곳입니다.`<br>`활동 폴더의 검수번들.json을 이 자리에 끌어다 놓거나 아래 단추로 고르세요.`<br>`파일이 아직 없다면 대화로 돌아가 전체 생성을 마치세요. 생성이 끝나야 이 파일이 만들어집니다.` |
| 검수 파일 단추 | `검수번들.json 고르기` |
| 제출 여부 빈 상태 | `점수를 붙여넣으시면 점수가 없는 학생을 여기에 모아 드립니다.` |

**[변경 A3]** 원안의 `아직 없음`은 **거짓말이 될 수 있다.** 검수만 하러 온
2회차 교사는 어제 저장한 `명렬.json`이 활동 폴더에 실재하는데도 한 시간 내내
`아직 없음`을 본다. 화면은 폴더를 볼 수 없다 — 그 사실을 문장이 말한다.

**[변경 A11]** 검수 빈 상태에 **파일이 있는 위치**와 **없을 때의 대처**가
들어간다. "생깁니다"까지만 말하면 파일이 없는 교사는 멈춘다.

### 6.3 확인 — 성공

| 조건 | 문구 |
|---|---|
| 인원 일치 | `48명을 읽었습니다. 적어 두신 48명과 같습니다.` |
| 열 확인 제목 | `첫 줄을 이렇게 읽었습니다.` |
| 열 확인 값 | `학번   30101` / `이름   김하늘` |
| 열 확인 단추 | `맞습니다` / `열이 바뀌었습니다` |
| 저장 단추 미출현 안내 | `저장은 위 확인을 마치신 뒤에 나옵니다.` |
| 버린 행 0 | `버린 행은 없습니다.` |
| 한계 고지(상설) | `이 검사가 못 잡는 것 — 이름끼리 자기들끼리 뒤바뀐 명단입니다. 표의 이름 몇 개를 눈으로 확인해 주세요.` |
| 점수 미입력 | `점수를 붙여넣지 않으셨습니다. 전원 제출로 봅니다.` |
| 제출 여부 물음 | `점수가 없는 학생 3명입니다. 미제출입니까?` |
| 제출 여부 선택지 | `미제출` / `제출했음` |
| 제출 여부 부기 | `미제출로 표시하면 활동 참여 사실만 담은 예외 세특으로 처리됩니다.` |

### 6.4 확인 — 경고와 오류

| 조건 | 문구 |
|---|---|
| **인원 불일치** | `적어 두신 48명과 붙여넣은 47명이 다릅니다.`<br>`명단에서 다시 세지 마시고, 어느 쪽이 맞는지 원본 표를 확인해 주세요.` |
| **인원 미입력 상태로 붙여넣음** | `명단을 받았습니다. 위 칸에 아시는 수를 먼저 적어 주세요 — 적으신 뒤라야 대조가 의미를 갖습니다.`<br>단추: `모르겠습니다. 명단을 보고 정하겠습니다` |
| 위 단추를 누른 뒤 | `명단에서 센 48명으로 진행합니다. 이 수는 따로 대조하지 않았습니다.` |
| **인원 칸에 표를 붙여넣음** | `붙여넣으신 것이 명단으로 보여 아래 명단 칸에 넣었습니다. 인원 칸에는 숫자만 적어 주세요.` |
| **0명 인식** | `학번으로 읽을 수 있는 값이 한 줄도 없습니다.`<br>`엑셀에서 셀 범위를 끌어 선택한 뒤 Ctrl+C로 복사하셨는지 확인해 주세요. 셀 하나만 복사하면 이렇게 됩니다.` |
| **열을 못 찾음** | `어느 열이 학번인지 정하지 못했습니다.`<br>`아래 표에서 학번이 있는 칸의 머리를 눌러 주세요.` |
| 열 바꾸기 화면 | `붙여넣으신 표의 첫 두 줄입니다. 학번이 있는 칸의 머리를 눌러 주세요.` |
| **중복** | `학번이 겹치는 행이 1개 있습니다. (12행 30102 · 13행 30102)`<br>`여러 반을 함께 붙여넣었거나 출석번호를 학번으로 쓴 표일 수 있습니다.`<br>단추: `뒤엣것 지우기` / `둘 다 두기` |
| **여러 반 혼재** | `학번 앞자리가 301·302 두 가지입니다. 두 반을 함께 붙여넣으셨습니까?`<br>단추: `301반만 두기` / `302반만 두기` / `둘 다 두기` |
| **시트 여럿(머리글 재등장)** | `27행에서 머리글로 보이는 줄(학번 이름)이 다시 나옵니다. 표 두 개를 이어 붙이신 것 같습니다.`<br>단추: `27행부터 잘라내기` / `그대로 두기` |
| **빈 행** | `학번이나 이름이 빈 행 2개를 건너뛰었습니다. (3행, 51행)` |
| **학번이 1~2자리** | `학번으로 읽은 값이 1, 2, 3 …처럼 짧습니다. 출석번호 열을 학번으로 읽었을 수 있습니다.`<br>단추: `열이 바뀌었습니다` |
| 정오표 구역 제목 | `손봐야 할 곳` |

**[변경 A8]** 모든 정오표 항목은 **사실 문장 + 원인 가설 문장 + 복구 동작 단추**
세 부분을 갖는다. 알려 주기만 하고 손댈 수단을 주지 않으면, 교사는 엑셀로
돌아가 처음부터 다시 한다.

**말투의 모범**: 현재 코드의 `학번이 겹치는 행` 경고는 원인 가설
("여러 반을 함께 붙여넣었거나 출석번호를 학번으로 쓴 표일 수 있습니다")까지
준다. **이 어법이 도구 전체의 표준이다.** 나머지 문구를 여기에 맞췄다.

### 6.5 저장 안내

| 조건 | 문구 |
|---|---|
| 명단 저장 단추 | `저장` |
| 저장 중 | `저장 중…` |
| 저장 성공 (파일 선택창을 쓴 경우) | `명렬.json을 저장했습니다 — 48명, 인원 확인됨.` |
| **저장 완료를 확인할 수 없는 경우(내려받기 폴백)** | `명렬.json을 내려받았습니다. 브라우저가 막았을 수 있으니 내려받기 폴더에 파일이 있는지 확인해 주세요. 화면은 폴더를 볼 수 없어 여기까지만 말씀드립니다.` |
| 상단 상태(저장 후) | `내려받기 폴더 확인` |
| 상단 상태(저장 후 수정함) | `저장한 뒤에 고치신 것이 있습니다` |
| 명단 지우기 단추 | `화면에서 명단 지우기` |
| 명단 지우기 부기 | `저장을 마치셨으면 눌러 주세요. 이 브라우저 창에서 이름과 학번이 사라집니다. 파일은 그대로 남습니다.` |
| 검수 저장 1단계 단추 | `AI에게 줄 파일 저장` |
| 1단계 완료 | `검수diff(AI용).json을 내려받았습니다 — 수정 9건. 대화창에는 이 파일만 주세요.` |
| 검수 저장 2단계 단추 | `실명 검수본도 저장` |
| 2단계 완료 | `검수본(실명·보관용).json을 내려받았습니다.`<br>`방금 받은 두 파일 중 이름에 (실명·보관용)이 붙은 쪽이 더 최근입니다. 대화창에 넣을 것은 (AI용)이 붙은 쪽입니다.` |

**[변경 A13]** `✓ 저장됨`이라고 쓰지 않는다. `file://`에서는
`showSaveFilePicker`가 막혀 `<a download>` 폴백이 되고, 그 방식에는 **성공
콜백이 없다.** 근거가 "클릭했다"뿐인데 "저장됨"이라고 쓰면, 조용히 틀리지
않겠다는 도구가 하필 그 자리에서 조용히 틀린다. 상태를 하나 더 두는 값을 낸다.

**[변경 A4]** recency 함정을 문장으로 명시한다. 실제 첨부는 화면이 아니라
탐색기·크롬 다운로드 바에서 일어나고, 거기서는 **가장 최근 파일이 먼저**
보인다. 저장 순서는 AI용을 먼저 두되(1단계에서 멈춘 교사가 실명 파일만 갖게
되는 사고를 막는다), 이름과 문장으로 순서를 뒤집어 알린다.

### 6.6 검수 문구

| 자리 | 문구 |
|---|---|
| 검수 헤더 | `검수 · 48명 중 12명 마침 · 수정 9건` |
| 목록 제목 1 | `먼저 볼 12명` |
| 목록 제목 2 | `나머지 36명` |
| 우선 검토 사유(예) | `예외 학생` / `분량 상한 근접` / `분량 목표 미달` / `비고에 '확인' 포함` / `매핑에 토큰 없음` |
| 바이트 표시 | `758바이트 (NEIS 기준) · 상한 760 · 남은 2바이트` |
| 바이트 초과 | `상한 760을 18바이트 넘었습니다.` |
| 금지 낱말 | `금지 낱말이 있습니다 — 이는` |
| 마치고 다음 | `마치고 다음` (부기: `⌃⏎`) |
| 우선 목록 소진 | `먼저 볼 12명을 마치셨습니다. 나머지 36명으로 넘어갑니다.` |
| 전체 소진 | `48명을 모두 보셨습니다. 아래에서 저장하세요.` |
| 저장 전 확인 | `검수 완료 표시가 없는 학생이 12명 있습니다.`<br>단추: `그래도 저장` / `12명 보러 가기` |
| 메모 라벨 | `왜 고쳤는지 한 줄 (선택)` |
| 파일 읽기 실패 | `검수번들.json을 읽지 못했습니다. 대화에서 만든 파일이 맞는지 확인해 주세요.` (뒤에 원인 문자열) |

**바이트는 한 숫자만 쓴다.** 현재 코드는 `712 바이트(UTF-8)`와
`700 (2바이트 기준)`을 나란히 놓고 헤더에 `(기준 확정 전)`이라 적는다.
교사에게 이 괄호는 **"못 믿을 도구"**로 읽힌다. NEIS도 엑셀 수식도 2바이트
기준이므로 화면은 그것만 쓴다. UTF-8은 `verify_seteuk`의 내부 검사로만 남는다.

### 6.7 상단 상태 3종 (`aria-live="polite"`)

| 상태 | 문구 | 언제 |
|---|---|---|
| 1 | `이 화면에서 저장한 것 없음` | 초기, 그리고 이 세션에서 아직 저장 안 함 |
| 2 | `내려받기 폴더 확인` | 저장 동작을 마쳤으나 확인할 수 없음(폴백) |
| 2' | `저장함` | `showSaveFilePicker`로 실제 핸들을 받은 경우에만 |
| 3 | `저장한 뒤에 고치신 것이 있습니다` | 저장 이후 상태가 변경됨 |

`○` 같은 기호는 상태를 나르지 않는다 — 낱말이 나른다. 기호는 장식이므로
넣지 않는다.

---

## 7. 파일 6종을 화면이 설명하는 방식

### 7.1 현재의 문제

파일 이름이 나오는 곳은 저장 중 스치는 토스트와 **페이지 최하단 회색 단락**
뿐이다. 그 목록조차 `제출자.json`(학번 있음)이 빠지고 `점수원본.json`(이름
없음)은 "실명"으로 적혀 경계가 임의로 보인다. 교사가 실제로 만질 파일은
2종인데 6종을 동등하게 나열한다.

### 7.2 시각적 구분 규칙 — 상자 2개, 영원히 2개

```
┌ 이 컴퓨터에만 두는 것 ─────────┐ ┌ AI에게 주는 것 ──────────────┐
│ 경계: --stop 1px                │ │ 경계: --rule-strong 1px       │
│ 제목: --t-ui, 굵기 600, --ink   │ │ 제목: --t-ui, 굵기 600, --ink │
│ 파일명: --font-mono, --t-ui     │ │ 파일명: --font-mono, --t-ui   │
│ 부기: --t-meta, --ink-2         │ │ 부기: --t-meta, --ink-2       │
│ 배경: --surface                 │ │ 배경: --surface               │
└─────────────────────────────────┘ └───────────────────────────────┘
```

구분 규칙은 **넷**이다. 색 하나에 기대지 않는다.

1. **자리** — 왼쪽이 "두는 것", 오른쪽이 "주는 것". 좁은 창에서는 위/아래.
2. **제목 문장** — `이 컴퓨터에만 두는 것` / `AI에게 주는 것`. 아이콘 없음.
3. **경계색** — 왼쪽만 `--stop`. 오른쪽은 중립 `--rule-strong`.
   (색은 세 번째 신호이지 유일한 신호가 아니다.)
4. **파일 이름 자체** — `(실명·보관용)` / `(AI용)`이 이름에 박혀 있어
   **화면 밖에서도 살아남는다.** 탐색기에서, 6개월 뒤에.

**상자는 두 개뿐이고 파일 수가 늘어도 늘지 않는다.**
왼쪽 상자에는 **실명이 있는 것만** 들어간다.

### 7.3 6종의 처분

| # | 파일 | 실명 | 이 브리프에서 | 화면에 나오나 |
|---|---|---|---|---|
| 1 | `명렬.json` | **있음** | `{students, 학번목록, 항목, 인원, 인원출처}` — 아래 2·3을 흡수 | **왼쪽 상자에 이름과 인원** |
| 2 | `제출자.json` | 학번만 | **삭제.** `명렬.json`의 `학번목록` 키로 흡수 | 안 나옴 |
| 3 | `점수원본.json` | 없음 | **삭제.** `명렬.json`의 `항목` 키로 흡수 | 안 나옴 |
| 4 | `검수번들.json` | **있음** | 입력 파일. 대화/스크립트가 만든다 | **검수 구역 빈 상태 문장에** |
| 5 | `검수본(실명·보관용).json` | **있음** | 이름 변경(원래 `검수본.json`) | **왼쪽 상자** |
| 6 | `검수diff(AI용).json` | 없음 | 이름 변경(원래 `검수diff.json`) | **오른쪽 상자** |

교사가 눈으로 보는 파일 이름은 **4개**로 줄고, 그중 손으로 만지는 것은
`검수번들.json`(넣기)과 `검수diff(AI용).json`(주기) **2개**다.

파일을 하나로 합치는 이유는 정리벽이 아니다. `file://`에서 파일이 2~3개면
크롬이 "여러 파일 다운로드"를 **차단**하는데, 차단당해도 현재 화면은
`저장했습니다(내려받기 폴더) — 명렬 48명, 제출자 48명, 점수 30명`이라고
말한다. 파일이 1개면 배너 자체가 뜨지 않는다.

**전제 조건(코드)**: `verify_seteuk.load_submitted_ids`가
`학번목록 ∪ students ∪ map ∪ classes` **합집합**이라, 한 파일에 명렬 48명 +
제출자 45명이 들어가면 결과가 48이 되어 "미제출자에게 세특 생성" FAIL이
**경고 없이 영원히 안 뜬다.** §10-1을 반드시 함께 해야 한다.
**이 항목은 화면만 바꾸면 안 되는 유일한 것이다.**

### 7.4 CLI를 화면에서 지운다

교사가 직접 터미널에 쳐야 할 정당한 지점은 **하나도 없다.** 그런데
현재 `도구.html` 하단과 `사용안내.md`가 `destroy --dir <활동폴더> --yes`를
보여 준다. `<>` 치환을 모르고 `--yes`가 무섭기만 하니 **아무것도 안 하고**,
실명 중간 파일이 바탕화면에 영구 잔존한다 — 개인정보 설계가 마지막 걸음에서
무너진다.

화면에서 명령줄 문자열이 **0개**가 된다. 남는 것은 교사가 그대로 복사해
채팅에 칠 수 있는 한국어 한 줄(`중간 파일 파기해 줘`)이다.

---

## 8. 모션 사양

현재 코드는 `transition` / `animation` / `keyframes`가 **전부 0건**이다.
이건 장식을 배제한 것이 아니라 **상태 전달 수단이 없는 것**이다. 붙여넣기
직후 카드 3개가 한 프레임에 튀어 오른다.

원칙: 상태 변화에만. 150~250ms. 페이지 로드 연출 없음. 스태거 없음.
**모션이 어떤 정보의 유일한 전달자가 되지 않는다** — 모든 항목이 낱말 변화를
동반한다.

### 8.1 목록 (전부 7개)

| # | 무엇이 | 언제 | 어떻게 | 동반하는 낱말 |
|---|---|---|---|---|
| 1 | `확인` 구역 등장 | 붙여넣기 직후 | `opacity 0→1` + `translateY 4px→0`, **180ms** ease-out, **한 덩어리로** | `48명을 읽었습니다…` (`aria-live`) |
| 2 | 인원 불일치 줄 | 대조 실패 | 배경 `--stop-ground` **240ms** 페이드 인. **사라지지 않는다** | `적어 두신 48명과…` (`aria-live`) |
| 3 | 검수 항목 펼침 | `⏎` / 클릭 | `grid-template-rows: 0fr→1fr`, **200ms** ease-out | 항목이 `selected` 배경으로 |
| 4 | 단추 hover / active | 마우스 | `background-color` **120ms** | — |
| 5 | 목록 행 hover | 마우스 | `background-color` **120ms** | — |
| 6 | 색인 현재 위치 | 스크롤 | **transition 없음(0ms).** IntersectionObserver로 굵기만 교체 | `aria-current` |
| 7 | 저장 진행 | 클릭 | **스피너 없음.** 라벨 교체 `저장` → `저장 중…` → 결과 문장 | 결과 문장(`aria-live`) |

**[변경 A6]** 원안의 8번째 항목 "검수 항목이 `먼저 볼` → `마친` 목록으로
200ms 이동"을 **삭제한다.** 4스크린짜리 단일 문서에서 현재 위치보다 위의 행이
사라지면 문서 전체가 위로 밀리고, 그게 48회 반복된다. 게다가 roving tabindex의
현재 `li`가 DOM에서 제거되면 포커스가 `<body>`로 떨어져 `↑↓`가 죽는다.
마친 항목은 **제자리에 남고 `마침` 낱말과 `--ink-3` 글자색으로 바뀐다.**
재정렬은 파일을 다시 열 때만 일어난다. 좌표가 완전히 안정된다.

### 8.2 존재하지 않는 것

로드 애니메이션, 스켈레톤(로컬 파싱은 즉각이다), 스피너, 축하 효과,
숫자 카운트업, 스크롤 연동 페이드, hover 시 확대·그림자·`transform`,
페이지 전환 슬라이드, 스태거.

### 8.3 `prefers-reduced-motion: reduce`

```css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: .01ms !important;
    transition-duration: .01ms !important;
    scroll-behavior: auto !important;
  }
}
```

**대안은 문구다. 움직임이 전하던 정보를 낱말이 대신 전한다.**

- `확인` 구역은 즉시 나타나고 `aria-live` 영역이
  `48명을 읽었습니다. 적어 두신 48명과 같습니다.`를 읽는다.
- 펼침은 즉시 열리고, 열린 항목의 헤더 줄에 **밑줄 2초**가 남아
  "무엇이 방금 바뀌었는지"를 신호한다. 신호까지 지우지는 않는다.
- 자동 스크롤(`scrollIntoView`)은 `behavior: 'auto'`가 되며,
  **화면이 이미 목표를 담고 있으면 아예 스크롤하지 않는다**(`block: 'nearest'`).

### 8.4 `aria-live` 배치 (딱 3곳. 그 이상은 소음이다)

1. 인원 대조 줄 (`polite`)
2. 상단 저장 상태 (`polite`)
3. 검수 진행 카운트 `48명 중 12명 마침 · 수정 9건` (`polite`)

현재 코드는 0건이다.

---

## 9. 접근성 체크리스트

### 9.1 키보드 순서 (Tab)

```
상단 상태(포커스 안 받음)
 → 색인 링크 8개(≥1120px) / <summary> 하나(≤900px)
 → 인원 입력
 → 명단 textarea
 → [맞습니다] [열이 바뀌었습니다]
 → 정오표 복구 단추들
 → 점수 textarea
 → 제출 여부 라디오(그룹당 한 번, ←→로 선택)
 → [저장]  ← 열 확인 전에는 존재하지 않으므로 순서에도 없다
 → [화면에서 명단 지우기]
 → [검수번들.json 고르기]
 → 검수 목록(roving tabindex — 전체가 Tab 정지점 하나)
 → [AI에게 줄 파일 저장]
 → [실명 검수본도 저장]
```

**요구사항 충족**: 키보드만으로 `붙여넣기 → 확인 → 저장`이 되고,
검수 항목 사이를 이동할 수 있다.

### 9.2 검수 목록 키보드

| 키 | 동작 |
|---|---|
| `↑` `↓` / `K` `J` | 목록 이동 (roving tabindex) |
| `Home` `End` | 처음 / 마지막 항목 |
| `⏎` | 펼치기 / 접기 |
| `⌃⏎` | 마치고 다음 |
| `Tab` | 열린 항목 안에서 세특 → 메모 → [마치고 다음] |
| `Esc` | 세특 입력칸에서 **항목 헤더 줄로 포커스 이동**(접지 않음) |

- 목록은 `<ul>` + roving tabindex. **`role="listbox"` / `option`을 쓰지 않는다** —
  표준 어휘를 재발명하지 않는다.
- 현재 코드는 `role=` 0건, `tabindex` 0건, `role="tab"` 없는 `aria-selected`
  (스크린리더가 무시하는 **무효 ARIA**), 40개 항목 사이 이동 수단은 순차 Tab뿐이다.

### 9.3 포커스 관리 (A6 — 명시 규정)

| 사건 | 포커스가 가는 곳 |
|---|---|
| `⌃⏎`(마치고 다음) | **다음 항목의 `li`에 명시 이관**(`nextEl.focus()`). 현재 항목은 DOM에 그대로 있으므로 안전 |
| 우선 목록 소진 | `나머지 36명`의 첫 항목 `li` |
| 검수 파일 로드 완료 | 검수 헤더(`h2`, `tabindex="-1"`)로 이동 + `scrollIntoView({block:'nearest'})` |
| 정오표 복구 단추 실행 후 | 같은 자리에 남는 다음 문장 또는 정오표 제목(`tabindex="-1"`) |
| `[12명 보러 가기]` | 걸러진 목록의 첫 항목 |
| 인라인 오류 발생 | 포커스를 **뺏지 않는다.** `aria-live`로만 알린다 |

`Esc`에 "목록으로 돌아가기"라는 개념을 두지 않는다 — 단일 페이지에는 돌아갈
"목록 위치"가 따로 없다.

### 9.4 스크린리더 라벨

| 요소 | 사양 |
|---|---|
| 인원 입력 | `<label for>` = `이 활동 대상은 몇 명입니까`, `aria-describedby` = 설명 단락 |
| 명단 textarea | `<label>` = `명단 붙여넣기`, `aria-describedby` = 두 열 안내 |
| 열 확인 블록 | `<fieldset>` + `<legend>첫 줄을 이렇게 읽었습니다</legend>` |
| 명단 표 | `<caption>` = `읽어 들인 명단 48명`, `<th scope="col">` |
| 중복 행 | 마지막 `<td>`에 낱말 `중복` — **시각·청각 모두 같은 정보** |
| 역할 상자 | `<section aria-labelledby>` — 제목이 곧 라벨 |
| 검수 항목 | `li`에 `aria-expanded`, 헤더 텍스트에 학번·이름·사유·핵심소재가 전부 들어감 |
| 저장 상태 | `<output aria-live="polite">` |
| 색인 | `<nav aria-label="차례">`, 현재 항목에 `aria-current="true"` |
| 이중선 | `<hr>`가 아니라 `<section aria-labelledby>` — 제목 `여기서 대화로 돌아갑니다`가 읽힌다 |
| 아이콘 | **없음.** 따라서 `aria-hidden`도 alt도 필요 없다 |

### 9.5 색 외 상태 표시 (색만으로 전달하는 것 0건)

| 상태 | 색 | 색이 아닌 신호 |
|---|---|---|
| 중복 행 | `--stop` | 낱말 `중복` + 정오표에 행 번호 |
| 인원 불일치 | `--stop-ground` | 문장 전체 + `aria-live` |
| 우선 검토 | `--flag` | **제목 `먼저 볼 12명`** + 줄 맨 왼쪽 사유 낱말 |
| 마친 항목 | `--ink-3` | 낱말 `마침` |
| 수정된 항목 | — | 낱말 `수정됨` |
| 미제출 | — | 낱말 `미제출` |
| 바이트 초과 | `--stop` | 문장 `상한 760을 18바이트 넘었습니다` |
| 현재 색인 위치 | — | `font-weight: 600` + `aria-current` |
| 펼쳐진 검수 항목 | `--accent-quiet` | `aria-expanded="true"` + 내용이 실제로 보임 |
| 저장 상태 | — | 세 개의 서로 다른 **문장**(§6.7) |

### 9.6 그 밖

- 본문 대비 4.5:1 이상 — **전 조합 통과**(§3.5). 큰 글씨도 전부 4.5 이상.
- 비텍스트 대비 3:1 — 조작 경계 3.23~4.03, 이중선 4.80/6.90.
- 확대 200%에서 가로 스크롤 없음(`--measure`가 rem 기준이므로 자연히 성립).
- `prefers-reduced-motion` 존중(§8.3).
- 라이트·다크 모두 1급 시민. 층 구조가 두 테마에서 동일(§3.5).
- 인터넷 없이 동작. 외부 폰트·CDN·전송 0건.
- 클릭 표적 최소 32×32px(라디오 라벨 전체가 표적).
- `alert()` / `confirm()` 0건 — 네이티브 모달은 포커스를 가로채고 문구를
  바꿀 수 없다.

---

## 10. 워크플로 변경 권고 (화면 밖에서 손대야 하는 것)

각 항목에 **"이걸 안 하면 무엇이 남는가"**를 붙인다.
난이도 표기: ⬤ 반드시(화면 변경의 전제) / ◐ 권장 / ○ 선택.

### ⬤ 10-1. `scripts/verify_seteuk.py` — `load_submitted_ids` 합집합 제거 (4줄)

현재 이 함수는 `학번목록 ∪ students ∪ map ∪ classes`를 **합집합**으로 만든다.
`명렬.json`이 한 파일로 합쳐지면(§7.3) 명렬 48명 + 제출자 45명이 한 파일에
들어가 결과가 48이 된다.

- 고칠 것: `학번목록` 키가 있으면 **그것만 쓰고 즉시 반환**한다.
  더불어 docstring의 "파일 단위 독립성" 논증을 다시 쓰고, stdout에
  `명렬과 같은 파일의 체크 결과로 대조했습니다` 한 줄을 추가한다.
- 테스트: `scripts/tests/test_attribution_gate.py:182` 근처.
- **안 하면 남는 것**: 미제출자에게 세특이 생성돼도 FAIL이 **경고 없이 영원히
  안 뜬다.** 이 도구의 존재 이유("틀렸을 때 조용히 넘어가지 않게")가
  가장 위험한 방향으로 무너진다. **화면만 바꾸면 안 되는 유일한 항목이다.**

### ⬤ 10-2. `scripts/pseudonymize.py` — `SENSITIVE_GLOBS` 보강 (2줄)

- `검수본*.json` 추가 (현재 목록에 없어 **실명 파일 하나가 파기 대상에서
  통째로 빠져 있다**).
- 파일명이 `검수본(실명·보관용).json`으로 바뀌므로 글롭이 이를 잡는지 확인.
- 관찰 메모를 도입한다면 `관찰메모*.json` → `관찰메모*.*`.
- **안 하면 남는 것**: 교사가 `중간 파일 파기해 줘`라고 말하고 도구가
  "지웠습니다"라고 답한 뒤에도, 실명 검수본이 바탕화면에 남는다.
  파기 보고 자체가 거짓이 된다.

### ⬤ 10-3. `tools/도구.html` — 저장 파일 통합과 이름

- `명렬.json` = `{students, 학번목록, 항목, 인원, 인원출처}` 한 파일.
  `--roster`(추가 키 무시), `score --paste`(`obj.get("항목")`),
  `destroy` 글롭(`명렬*.json`) 모두 **변경 0**이다.
- `인원출처`는 `"직접입력"` 또는 `"명단확인후"` — §6.4의 탈출구를 쓴 경우
  후자가 기록되어, 게이트가 독립적으로 만족되지 않았음이 파일에 남는다.
- 검수 출력 파일명: `검수diff(AI용).json` / `검수본(실명·보관용).json`.
- **안 하면 남는 것**: 크롬의 다중 다운로드 차단이 그대로 남고, 차단당해도
  화면은 성공을 보고한다. 검수 쪽에서는 **AI에게 줄 유일한 파일이
  조용히 사라진다.**

### ◐ 10-4. `SKILL.md` — 인원을 다시 묻지 않는다

- `명렬.json`의 `인원` 키를 읽고, 값이 있으면 **대화에서 인원을 묻지 않는다.**
  `인원출처: "명단확인후"`이면 그 사실을 한 줄로 확인만 한다.
- **안 하면 남는 것**: 대화 왕복 1회가 그대로 남고, 더 나쁘게는 교사가 화면에서
  본 48을 그대로 답해 `--expected-count` 게이트가 계속 무력화된다.
  (SKILL.md 176행이 경고한 바로 그 일을 지금은 화면이 시키고 있다.)

### ◐ 10-5. `SKILL.md` — 브라우저를 에이전트가 연다 / 파일을 에이전트가 회수한다

- 에이전트가 `도구.html`을 직접 연다(교사가 경로를 읽고 더블클릭하는 단계 소멸).
- 저장 안내 직후 에이전트가 **내려받기 폴더와 활동 폴더를 확인하고 옮긴 뒤
  보고**한다. 파일 내용은 열지 않는다.
- **안 하면 남는 것**: "내려받기 폴더에서 옮겨 주세요"라는, 도구가 못 하는 일을
  사람에게 넘긴 문장이 남는다. 손으로 옮기는 파일이 5개 그대로다.

### ◐ 10-6. `사용안내.md` — CLI 문자열 제거

- `destroy --dir <활동폴더> --yes`를 `중간 파일 파기해 줘`로 교체.
  화면(§7.4)과 안내 문서의 문구를 일치시킨다.
- **안 하면 남는 것**: 화면에서 지워도 안내 문서가 같은 겁을 준다.
  교사는 아무것도 안 하고 실명 파일이 잔존한다.

### ○ 10-7. 바이트 기준 확정

- 화면은 이미 NEIS(2바이트) 기준 하나만 쓴다(§6.6). 하네스가 목표·상한을
  같은 기준으로 정의하고 `verify_seteuk`의 초과 판정도 여기에 맞춘다.
- **안 하면 남는 것**: 화면은 758이라 하고 검사는 UTF-8로 통과/실패를 갈라,
  교사가 화면을 믿고 고친 결과가 검사에서 뒤집힌다. 화면이 할 수 있는 기여는
  **둘을 보여 주기를 거부하는 것**까지이고, 나머지는 하네스의 일이다.

### ○ 10-8. 점수 열 모호성 질문을 대화에서 내린다

- "어느 열이 점수인가"는 **보기**이지 **판단**이 아니므로 화면의 일이다.
  대화는 붙여넣기만 안내한다. CLI는 헤드리스용으로 존치.
- **안 하면 남는 것**: 대화 왕복이 하나 더 남는다. 치명적이지는 않다.

### 파이썬 변경 총량

`verify_seteuk.py` 4줄 + docstring, `pseudonymize.py` 2줄, 테스트 1파일.
그 외는 전부 `도구.html`과 `SKILL.md`에서 끝난다.

---

## 11. 구현하지 말 것 (의도적 배제)

| # | 배제 | 이유 |
|---|---|---|
| 1 | **"처음이신가요?" 배너 / 단계 투어 / 첫실행 감지** | 제품 슬롭이고, 화면 자체가 순서인 구조에서 중복이다. 온보딩은 배치가 한다 |
| 2 | **localStorage / sessionStorage 자동 저장** | "아무것도 기억하지 않는다"는 약속을 깬다. 새로고침 손실은 §12-3에 정직하게 남긴다 |
| 3 | **인원 입력 전 붙여넣기 잠금** | disabled `textarea`는 붙여넣기·클릭·포커스를 전부 삼켜 **아무 반응도 없다**. placeholder를 안 읽은 교사에게는 "고장"이다. 잠금 대신 결과 표시 보류 + 탈출구(§6.4) |
| 4 | **자동 포커스** | 교사는 엑셀에서 두 열을 복사한 채로 이 창에 온다. 첫 필드에 포커스가 있으면 반사적 Ctrl+V가 인원 칸으로 들어간다 |
| 5 | **`alert()` / `confirm()`** | 포커스를 가로채고 문구를 바꿀 수 없다. 인라인 블록은 **세 번째 선택지**(`12명 보러 가기`)를 줄 수 있다 |
| 6 | **색인의 숫자·원·연결선·체크마크·진행 막대** | 붙이는 순간 스테퍼가 되고 금지 목록의 "01/02/03 번호 스캐폴딩"·SaaS 온보딩·장난스러운 교육앱이 된다 |
| 7 | **대문자 eyebrow / `letter-spacing` 제목 / `01·02·03` 번호** | 금지 목록 직격. 한글에 `uppercase`는 효과가 없어 실효는 제목을 12px 최저 대비로 만드는 것뿐이었다 |
| 8 | **성공 배지 / 초록 체크 / 축하 효과** | 설계 원칙 2 위반. "경고 0건"이 안전으로 읽혀서는 안 된다. 성공색 자체가 없다 |
| 9 | **`--flag` 배경 채움** | 라이트에서 노란 채움은 필연적으로 크림 금지 대역에 들어간다. 글자·선색으로만 존재 |
| 10 | **카드 격자 / 동일 카드 반복** | 반참조(화려한 SaaS·나이스). 구역은 가로선과 여백으로 나눈다. 상자는 역할 상자 2개뿐 |
| 11 | **아이콘 일체** | 나이스의 "의미 없는 아이콘"과 구별되지 않는다. 낱말이 더 정확하다 |
| 12 | **스켈레톤 / 스피너 / 로딩 연출** | 로컬 파싱은 즉각이다. 없는 대기를 연출하지 않는다 |
| 13 | **다반 필터 / 반 선택 드롭다운** | 놓을 자리가 레일뿐인데, 레일에 컨트롤이 들어가면 색인이 아니라 사이드바가 되고 #6의 규율이 무너진다. **반이 3개를 넘으면 다른 구조가 필요하다 — 이번 회차에서 풀지 않는다** |
| 14 | **구조 시연 / 파일럿 화면** | 파일 2개와 대화 왕복 1회가 새로 생겨, 다른 곳에서 줄인 것을 반납한다. 필요한 건 화면이 아니라 대화의 고정 제시 형식이다 |
| 15 | **메모 붙여넣기 상자** | 좋은 아이디어지만 이번 회차의 표면을 넓힌다. 도입 시 `pseudonymize.py` 글롭을 함께 고쳐야 한다(§10-2) |
| 16 | **커스텀 스크롤바 / 커스텀 드롭다운 / 커스텀 접힘 드로어** | 표준 어포던스 재발명 금지. 접힘은 네이티브 `<details>` |
| 17 | **유동 타이포(`clamp`)** | 사용자는 일정한 DPI로 본다. 반응형은 **구조적**으로만 한다 |
| 18 | **`검수본`과 `검수diff`의 통합** | 합치면 에이전트가 실명 파일을 열고 스스로 눈을 가려야 한다. 불변 원칙이 기계적 보장에서 자율 규제로 강등된다 |
| 19 | **탭 유지 옵션 / 두 구조 병행** | "같은 저장 단추가 두 곳에서 다르게 보이면 하나는 틀린 것"(product 금지) |
| 20 | **`Enter` 단독으로 마치고 다음** | 48회 연타 가능한 도장 키가 된다. `⌃⏎`로 한 손가락 더 요구한다 |

---

## 12. 이 브리프가 무너지는 지점 (구현자가 알아야 할 것)

정직하게 여섯 개. 대응책이 있는 것과 없는 것을 구분한다.

1. **명렬 실명과 검수 실명이 한 DOM에 공존한다.** 현재 `showTab()`은 `hidden`만
   토글하므로 실제 격리는 원래도 없었고 이 안이 실질을 나쁘게 만들지는 않는다.
   그러나 **의도의 표명은 사라진다.** 앞으로 어떤 기능(인쇄 스타일, 전역 검색,
   넓게 잡은 셀렉터)이 둘을 가로질러도 구조가 막아 주지 않는다.
   완화: 저장 직후 `[화면에서 명단 지우기]`(§6.5). **완화이지 해결이 아니다.**
2. **색인이 스테퍼로 미끄러지기 쉽다.** `글자 목록 + 굵기 하나`라는 제약을
   구현 내내 지켜야 하는데, 이건 설계가 아니라 규율의 문제이고 규율은 샌다.
   이 안에서 가장 실패 확률이 높은 단일 요소다. §11-6을 코드 리뷰 체크 항목으로.
3. **새로고침 한 번에 검수 48명이 날아간다.** 페이지가 길어 스크롤이 꼬였을 때
   F5 유혹이 크다. `beforeunload`는 문구를 못 바꾼다. 대응은 **경고를 소음이
   아니라 신호로 만드는 것**뿐이다 — `students.length`가 아니라 **저장 상태**
   (`아직` / `저장한 뒤 수정함`)에 건다(A9). 저장을 마친 교사는 경고를 보지 않고,
   보게 되면 그건 진짜다. **손실 자체는 막지 못한다.**
4. **열 확인이 반사적 클릭으로 전락할 수 있다.** `[맞습니다]`를 보여 주는 것은
   보장이 아니라 행동에 대한 내기다. 저장 단추를 확인 뒤로 미룬 것(A7)이
   최선이고, 값을 보여 주는 것 이상은 화면이 할 수 없다.
5. **한 번에 한 명 보기는 48편의 문체 반복을 못 잡는다.** 목록 둘째 줄의
   핵심소재(A10)가 "모닥불의 온기 12회"를 눈에 잡히게 하지만, **소재는 다른데
   문장 골격이 같은 경우**는 여전히 안 잡힌다. 대리 지표이지 실물이 아니다.
   ≤900px에서는 목록이 좁아져 이 훑기가 더 어려워진다.
6. **저장 성공을 끝내 확인할 수 없다.** `<a download>`에는 성공 콜백이 없다.
   §6.5는 그 한계를 문장으로 정직하게 말하지만, 교사 입장에서는 "확인해 주세요"가
   한 단계 더 남은 것이다. 이 값을 내는 것이 조용히 틀리는 것보다 낫다는 판단이다.

---

## 부록 A — 판정단 graft 반영표

| 출처 | 내용 | 반영 위치 |
|---|---|---|
| novice · two-doors §4-3 | 열 확인 전 저장 단추 미출현 | A7 · §2.2 · §5.2 |
| novice · document-desk §4.3 | 정오표에 복구 동작 단추 | A8 · §2.2③ · §6.4 |
| novice · document-desk §6.1④ | `beforeunload`를 파일 저장 상태에 | A9 · §12-3 |
| novice · two-doors §6-1 | 파일명에 용도(`검수본(실명·보관용)`) | A4 · §7.2-4 · §7.3 |
| novice · two-doors §5-1(2) | 목록 둘째 줄 = 핵심소재 | A10 · §2.3 |
| novice · document-desk §3 | 검수 빈 상태에 위치 + 없을 때 대처 | A11 · §6.2 |
| long-review · document-desk §5.3 | 두 면 행간·기준선 일치 | A15 · §2.3 |
| long-review · two-doors §5-2(칸 높이) | 칸 높이 고정 `max(원문,세특,14rem)` | A15 · §2.3 |
| long-review · one-page-flow §5.4-3 | ⌃⏎가 우선 목록을 먼저 소진 + 전환 고지 | §6.6 · §9.3 |
| long-review · document-desk §11-6 | `저장됨` 대신 `내려받기 폴더 확인` | A13 · §6.5 · §6.7 |
| long-review · document-desk §9 | 세특 입력 300ms 디바운스 + blur/paste 즉시 | A15 · §5.4 |
| long-review · one-page-flow §7.5 | 미달 조합 명시 규율 | §3.5 — **미달 조합 자체를 없앴다** |
| slop · two-doors §5-2 | 원문 inset / 세특 raised 표면 어휘 | A12 · §3.1 · §2.3 |
| slop · two-doors §7-2 | `--focus` 독립 토큰(청) | A18 · §3.2 · §5.1 |
| slop · document-desk §7.3 | 패널 전용 중립 `--panel` | A12 · §3.2 |
| slop · document-desk §2-C | ≤900px 접힘은 네이티브 `<details>` | A17 · §5.6 · §11-16 |
| slop · document-desk §1.3 | 색인에 `세특 ― 대화에서 만듭니다` 상설 | A16 · §2.1 · §5.6 |

## 부록 B — 적대적 검증 대응표

| # | 지적 | 대응 | 완전 해소? |
|---|---|---|---|
| 1 | 자동 포커스 + 클립보드 충돌 | 자동 포커스 제거, `type=text`+`inputmode`, 인원 칸 붙여넣기 감지 후 명단으로 이관 + 문장 | ✅ |
| 2 | disabled 잠금이 침묵으로 실패 / 닭-달걀 | 잠금 폐기. 상자는 늘 편집 가능, 결과 표시만 보류, 탈출구 단추 + `인원출처` 기록 | ✅ |
| 3 | 2회차에 역할 상자가 거짓 주장 | 초기 문구를 `이 화면에서는 아직 만들지 않았습니다 / 화면은 폴더를 볼 수 없습니다`로. 상단 상태도 `이 화면에서 저장한 것 없음` | ✅ |
| 4 | 다운로드 폴더 recency가 순서를 뒤집음 | 두 파일 모두 이름에 용도. 저장 순서는 AI용 우선 유지(1단계 중단 시 안전), recency 함정을 문장으로 고지 | ✅ |
| 5 | `--ink-3`/`--accent-quiet` 4.26, hover 배경과 봉인 규칙 충돌, 이중선 토큰 미지정 | `--ink-3` 재계산(라이트 5.17~5.92 / 다크 5.47~7.01, 전 표면 통과) → 봉인 규칙 자체가 불필요. `--rule-double` 신설(4.80/6.90). 전 조합 직접 계산해 표로 | ✅ |
| 6 | 마친 항목 이동으로 스크롤·포커스 붕괴 | 이동 삭제(A6). 제자리 + `마침` 낱말. 포커스 명시 이관 규정(§9.3). `Esc` 의미 재정의 | ✅ |
| — | (검증이 통하지 않았다고 인정한 것) | §4.4 제출 3회 판단 · §6.5 CLI 삭제 · §9.5 세 번째 선택지 | 원안 유지 |

## 부록 C — 만드는 순서 (구현 권장)

1. 팔레트·타이포 스케일·8px 눈금 교체 (§3 §4) — 나머지 전부의 토대
2. 단일 세로 흐름 골격 + 역할 상자 2개 + 이중선 (§2.1 §7.2)
3. 인원 먼저 + 붙여넣기 잠금 없음 + 탈출구 (§6.4)
4. 첫 행 실제 값 확인 + 저장 단추를 확인 뒤로 (§2.2 A7)
5. 정오표(복구 동작 포함) + 칩 폐기 + 0을 초록으로 안 그리기 (§2.2 §3.6)
6. 저장 1파일화 + 파일명 + 정직한 저장 상태 (§7 §6.5) — **§10-1을 함께**
7. 검수 한 명 + 핵심소재 둘째 줄 + 키보드/포커스 (§2.3 §9.2 §9.3)
8. 모션 7종 + `aria-live` 3곳 + reduced-motion 문구 대안 (§8)

**1~5만으로 "교사가 틀린 줄 모르고 NEIS에 넣는" 실패의 앞단이 대부분 막힌다.**

---

## 요약표

| | 현재 코드 | 이 브리프 |
|---|---|---|
| 상자(카드) | 44 | 2 |
| 활자 크기 | 12종 | 5종 |
| 간격 값 | 15종 | 8종 |
| 저장 대화상자(명단) | 2~3 (차단됨) | 1 |
| 교사가 보는 파일 이름 | 6 | 4 (만지는 것 2) |
| 제출 여부 판단 | 48회 | 3회 |
| 동시에 펼쳐진 세특 | 40 | 1 |
| 화면의 CLI 문자열 | 1 | 0 |
| 최저 본문 대비(라이트) | 1.129 (`tr.dup`) | **5.17** |
| 최저 본문 대비(다크) | **1.006** (`tr.dup`) | **5.47** |
| 주 단추 대비(다크) | 2.26 | **8.85** |
| 크림 금지 대역 토큰 | 1 (`--warnbg`) | **0** |
| `:focus-visible` | 0건 | 전 인터랙티브 |
| `aria-live` | 0건 | 3곳 |
| `role` / `tabindex` | 0건 | roving tabindex + 정확한 시맨틱 |
| 모션 | 0건 | 7종(전부 낱말 동반) |
| 첫 페인트에 보이는 개인정보 경계 | 없음 | 빈 상자 2개 |





