# B · 문서 작업대 (Document Desk)

> 방향 B의 설계안. 이 회차는 문서만 만든다 — 코드는 수정하지 않았다.
> 등장하는 학생 이름·학번은 전부 가상이다(김하늘·이서준·정하윤 등, 30101~30131).
> 경로 주의: 과제문의 `undefined/`는 치환 실패로, 실제 대상은 `C:\Users\admin\.claude\skills\seteuk-harness\`다.

---

## 0. 한 문장

**빠진 것은 화면이 아니라 물건이다.** 교사가 30분 동안 계속 묻는 질문은 "이 화면에서 뭘 하지"가 아니라 **"내가 지금 뭘 가지고 있지"** 이고, 그 질문에 답하는 상시 목록이 이 도구에는 없다. 문서 작업대는 그 목록 — **색인(索引)** — 을 화면의 1급 객체로 승격시키고, 나머지를 그 옆에 붙인다.

---

## 1. 왜 색인인가 — 이 방향의 변호

### 1.1 진단의 머리말이 곧 이 방향의 설계 요구서다

first-run 진단의 첫 줄은 이렇다.

> 이 도구는 "무엇을 하는가"는 설계돼 있는데 **"지금 내가 어디에 있는가"가 설계돼 있지 않다.**

"어디에 있는가"는 단계 표시기로 답할 수 없다. 단계 표시기는 진행률이지 소지품이 아니다. 교사가 실제로 알아야 하는 건 **명단이 있는지, 점수가 있는지, 세특이 아직 없는지, 저장된 파일이 몇 개이고 그중 AI에게 줄 게 뭔지, 지금 걸려 있는 문제가 몇 개인지** — 전부 "가진 것"의 목록이다. 목록은 어딘가에 늘 놓여 있어야 목록이다. 스크롤하면 사라지는 카드에는 담을 수 없다.

### 1.2 PRODUCT.md 설계 원칙 5개 중 3개가 같은 요구를 한다

| 원칙 | 요구 | 색인이 답하는 방식 |
|---|---|---|
| **2 · 조용한 실패를 시각화한다.** "경고 0건"을 안전으로 읽히게 두지 않는다 | 문제가 화면에서 사라지지 않아야 한다 | `지금 걸린 것`은 상시 블록이고, **0일 때는 숫자를 쓰지 않고 아무것도 쓰지 않는다.** 초록 0 배지가 존재할 자리가 구조적으로 없다 |
| **3 · 실명은 브라우저 밖으로 나가지 않는다. 이 경계가 UI에서 보여야 한다** | 파일 구분이 상시 노출돼야 한다 | `파일` 블록의 두 제목 — `이 컴퓨터에만 둘 것` / `AI에게 줄 것` — 이 항상 화면에 있다. 지금은 이 정보가 **페이지 최하단 회색 단락**에만 있다(F12) |
| **5 · 처음 여는 사람이 설명 없이 다음 칸을 안다. 화면 자체가 순서** | 순서가 구조로 보여야 한다 | 색인 항목은 순서대로 놓이고 **아무것도 숨기지 않는다.** 지금은 4단계 중 3단계가 `hidden`이라 순서가 DOM에 없다(ui-critique P0 #8) |

한 요소가 세 원칙을 동시에 만족시킨다. 이건 우연이 아니라, 세 원칙이 전부 "지속성"을 요구하고 있었기 때문이다.

### 1.3 그리고 이것이 반참조를 막는 유일한 자리다

사용자가 지목한 다섯 번째 반참조는 **"웹 세특 생성 서비스의 결과 화면"** 이다. 이걸 피하는 방법은 결과를 예쁘게 안 만드는 게 아니라, **화면이 자기가 안 하는 일을 명시하는 것**이다. 그 문장이 놓일 자리가 있어야 한다.

색인에는 처음부터 이 줄이 있다.

```
 세특             ―
   대화에서 만듭니다
```

이 두 줄이 하는 일:
- F14(30분 뒤 세특 0문장인데 아무도 예고 안 함)를 **첫 화면에서** 해소한다.
- "붙여넣고 버튼 누르면 쏟아지는 화면"이라는 기대를 **버튼을 누르기 전에** 꺾는다.
- 원칙 4("화면은 눈으로 봐야 하는 것만 맡는다")를 화면이 스스로 말하게 한다.

다른 배치에서는 이 고백을 어디에 둘지 애매하다. 안내문에 넣으면 안 읽히고, 토스트에 넣으면 사라지고, 배너에 넣으면 "처음이신가요?" 온보딩 슬롭이 된다. 색인에서는 **상태 항목 하나**로 자연스럽다. 값이 `―`일 뿐이다.

### 1.4 "이건 관리자 대시보드 사이드바 아닌가?" — 아니다, 그리고 그 차이가 전부다

| 사이드 내비게이션 | 이 색인 |
|---|---|
| 누르면 오른쪽이 바뀐다 | **누르는 곳이 거의 없다.** 오른쪽을 바꾸지 않는다 |
| 아이콘 + 라벨 | **아이콘 0개.** 활자와 숫자만 |
| 접기 토글, 로고 블록, 사용자 아바타 | 없음 |
| 현재 위치를 강조 | 현재 위치가 아니라 **가진 것의 수량**을 적는다 |
| 카드·박스·배경 대비로 구획 | **괘선 한 줄과 여백**으로 구획. 박스 0개 |

이건 내비게이션이 아니라 **명세서(manifest)** 다. 문서로 치면 판권면·정오표·목차가 한 데 모인 앞장. 나이스와 닮을 위험은 실재하지만(§11-2), 그 위험은 폭 때문이 아니라 조판이 흐트러질 때 생긴다.

---

## 2. 화면 구조

격자: `grid-template-columns: 17rem minmax(0, 1fr)`; 페이지 최대폭 명단 탭 `82rem`, 검수 탭 `90rem`(읽는 면이라 넓힌다). 색인은 `position: sticky; top: 0; height: 100dvh; overflow-y: auto`.
색인 폭은 **두 탭에서 동일하다**(17rem = 272px). 폭이 바뀌면 "같은 물건"이라는 감각이 깨진다.

### 2-0. 1280px · 첫 화면 (아무것도 안 한 상태)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ 세특 작업대 — 2학년 문학 · 만복사저포기 감상문                              │
│ ┃명단과 점수┃   검수                                                        │
├──────────────────────────┬──────────────────────────────────────────────────┤
│ 이 활동                  │  대상 인원                                       │
│ ────────────────────     │  ──────────────────────────────────────────────  │
│ 대상 인원        ―       │  이 활동을 하는 학생이 몇 명입니까?              │
│   아직 안 적으셨습니다   │                                                  │
│                          │      [        ] 명                               │
│ 명단             ―       │                                                  │
│ 점수             ―       │  명단을 보고 세지 마십시오. 명단이 틀렸을 가능성 │
│ 세특             ―       │  을 명단으로 검증할 수는 없습니다. 정원에서 전출 │
│   대화에서 만듭니다      │  ·자퇴를 뺀, 아시는 수를 적어 주세요.            │
│                          │                                                  │
│ 파일                     │                                                  │
│ ────────────────────     │  명단 붙여넣기                                   │
│ 이 컴퓨터에만 둘 것      │  ──────────────────────────────────────────────  │
│   아직 없습니다          │  ┌────────────────────────────────────────────┐  │
│ AI에게 줄 것             │  │                                            │  │
│   아직 없습니다          │  │   위에 인원을 적으시면 여기가 열립니다.    │  │
│   검수를 마치면          │  │                                            │  │
│   검수diff.json          │  └────────────────────────────────────────────┘  │
│   하나가 생깁니다        │                                                  │
│                          │  점수 붙여넣기                                   │
│ 지금 걸린 것      ―      │  ──────────────────────────────────────────────  │
│                          │  명단을 붙여넣으면 열립니다. (선택)              │
│ ────────────────────     │                                                  │
│ 검수를 마치기 전에는     │  저장                                            │
│ NEIS에 입력하지          │  ──────────────────────────────────────────────  │
│ 마십시오.                │  파일 하나를 이 폴더에 저장합니다.               │
└──────────────────────────┴──────────────────────────────────────────────────┘
```

### 2-A. 1280px · 명단과 점수 · 붙여넣은 직후

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ 세특 작업대 — 2학년 문학 · 만복사저포기 감상문                              │
│ ┃명단과 점수┃   검수                                                        │
├──────────────────────────┬──────────────────────────────────────────────────┤
│ 이 활동                  │  명단 붙여넣기                       고쳐 붙이기 │
│ ────────────────────     │  ──────────────────────────────────────────────  │
│ 대상 인원      48명      │  ┌────────────────────────────────────────────┐  │
│   내가 적어 둔 수        │  │ 30101  김하늘                              │  │
│                          │  │ 30102  이서준                              │  │
│ 명단           48줄      │  │ 30103  박도윤                              │  │
│   제출 45 · 미제출 3     │  │ …                                          │  │
│                          │  └────────────────────────────────────────────┘  │
│ 점수           30명      │                                                  │
│   15점 ~ 4점             │  확인                                            │
│                          │  ──────────────────────────────────────────────  │
│ 세특           아직      │  첫 줄을 이렇게 읽었습니다.                      │
│   대화에서 만듭니다      │                                                  │
│                          │      학번   30101                                │
│ 파일                     │      이름   김하늘                               │
│ ────────────────────     │                                                  │
│ 이 컴퓨터에만 둘 것      │   [ 학번과 이름 바꿔 읽기 ]   [ 다시 붙여넣기 ]  │
│   명렬.json    저장됨    │                                                  │
│   점수원본.json  아직    │  48줄을 읽었습니다. 적어 두신 48명과 같습니다.   │
│ AI에게 줄 것             │                                                  │
│   아직 없습니다          │  겹치는 학번 1건                                 │
│                          │  ──────────────────────────────────────────────  │
│ 지금 걸린 것      2      │  30112 이 12행과 34행에 두 번 있습니다.          │
│   겹치는 학번 1건        │  여러 반을 함께 붙여넣었거나, 출석번호를 학번으로│
│   점수 없는 학생 15      │  쓴 표일 수 있습니다.                            │
│                          │   [ 뒤엣것 지우기 ]   [ 둘 다 두기 ]             │
│ ────────────────────     │                                                  │
│ 검수를 마치기 전에는     │  순번  학번    이름     제출                     │
│ NEIS에 입력하지          │   1   30101   김하늘    ☑                        │
│ 마십시오.                │   2   30102   이서준    ☑                        │
│                          │   3   30103   박도윤    ☑                     ⋮  │
└──────────────────────────┴──────────────────────────────────────────────────┘
```

### 2-B. 1280px · 검수 — 한 번에 한 명, 왼쪽 색인이 곧 명부

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ 세특 작업대 — 2학년 문학 · 만복사저포기 감상문                              │
│ 명단과 점수   ┃검수┃                                                        │
├──────────────────────────┬──────────────────────────────────────────────────┤
│ 검수  12 / 48            │  30112  정하윤                   12번째 · 48명 중 │
│ ────────────────────     │  ──────────────────────────────────────────────  │
│ 먼저 볼 것      9명      │  원문에서 근거를 찾지 못한 서술이 있습니다.      │
│  30112 정하윤  검토      │  「토론에서 주도적으로」— 원문에 토론 언급 없음  │
│  30119 최서아  검토      │                                                  │
│  30124 한지우  검토      │                                                  │
│  30131 오시현  검토      │  ┌─ 학생 원문 ─────┐ ┌─ 세특 ──────────────┐     │
│  …                       │  │ 만복사저포기를  │ │ 만복사저포기의 환상 │     │
│                          │  │ 읽으면서 가장   │ │ 구조를 산 자와 죽은 │     │
│ 나머지         39명      │  │ 마음에 걸린 것은│ │ 자의 경계로 읽어 내 │     │
│  30101 김하늘   ✓        │  │ 양생이 여인을   │ │ 고, 양생의 마지막   │     │
│  30102 이서준   ✓        │  │ 떠나보낸 뒤의   │ │ 선택을 애도의 완성으│     │
│  30103 박도윤            │  │ 삶이었다. 나는  │ │ 로 해석하였음.      │     │
│  30104 서예은            │  │ 그것이 …        │ │                     │     │
│  30105 남주호            │  └─────────────────┘ └─────────────────────┘     │
│  …                       │                       612 바이트 · 상한 700      │
│                          │                                                  │
│ ────────────────────     │  검수 메모 ───────────────────────────────       │
│ 파일                     │  [ 왜 고쳤는지 한 줄 — 다음 회차 규칙이 됩니다 ] │
│ ────────────────────     │  [ ✓ 검수 마침 · 다음 사람 ]        ↑↓ 로도 이동 │
│ 이 컴퓨터에만 둘 것      │                                                  │
│   검수본.json   아직     │  ──────────────────────────────────────────────  │
│ AI에게 줄 것             │  [ 검수 마치고 저장 ]                            │
│   검수diff.json 아직     │  두 파일이 생깁니다. AI에게 줄 것은 diff 하나.   │
└──────────────────────────┴──────────────────────────────────────────────────┘
```

### 2-C. 900px 이하 — 색인은 sticky 한 줄로 접힌다

```
┌────────────────────────────────────────────────────────────┐
│ 세특 작업대 — 만복사저포기 감상문                          │
│ ┃명단과 점수┃   검수                                       │
├────────────────────────────────────────────────────────────┤
│ 명단 48 · 점수 30 · 세특 아직 · 걸린 것 2      펼치기 ▾    │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  명단 붙여넣기                        고쳐 붙이기          │
│  ────────────────────────────────────────────              │
│  ┌──────────────────────────────────────────┐              │
│  │ 30101  김하늘                            │              │
│  │ …                                        │              │
│  └──────────────────────────────────────────┘              │
│                                                            │
│  확인                                                      │
│  ────────────────────────────────────────────              │
│  첫 줄을 이렇게 읽었습니다.                                │
│      학번   30101                                          │
│      이름   김하늘                                         │
│   [ 바꿔 읽기 ]   [ 다시 붙여넣기 ]                        │
│                                                            │
│  겹치는 학번 1건                                           │
│  ────────────────────────────────────────────              │
│  30112 이 12행과 34행에 두 번 있습니다.  …                 │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

펼친 상태 — 네이티브 `<details>`/`<summary>`. 커스텀 드로어를 만들지 않는다(product 금지: 표준 어포던스 재발명).

```
┌────────────────────────────────────────────────────────────┐
│ 명단 48 · 점수 30 · 세특 아직 · 걸린 것 2       접기 ▴     │
├────────────────────────────────────────────────────────────┤
│ 이 활동                                                    │
│   대상 인원   48명      명단   48줄                        │
│   점수        30명      세특   아직                        │
│ 파일                                                       │
│   이 컴퓨터에만 둘 것   명렬.json 저장됨                   │
│   AI에게 줄 것          아직 없습니다                      │
│ 지금 걸린 것 2                                             │
│   겹치는 학번 1건 · 점수 없는 학생 15명                    │
└────────────────────────────────────────────────────────────┘
```

**한 줄 요약에서도 `걸린 것 2`는 절대 생략되지 않는다.** 접힌 상태에서 숫자가 0이면 그 조각만 사라진다(`걸린 것 0`을 쓰지 않는다).

---

## 3. 첫 화면 — 무엇을 가르치는가

교사가 도구를 처음 열었을 때 **오른쪽에서 손댈 수 있는 것은 딱 하나**, 인원 입력칸이다. 붙여넣기 상자는 `disabled`이고 안쪽에 이유가 적혀 있다("위에 인원을 적으시면 여기가 열립니다"). 배너도, 투어도, "처음이신가요?"도 없다.

이 배치가 가르치는 네 가지:

**① 이 화면은 세특을 만들지 않는다.** 색인의 `세특 ― / 대화에서 만듭니다`. 30분 뒤에 대한 예고가 0초에 있다.

**② 순서.** 색인 항목이 위에서 아래로 `대상 인원 → 명단 → 점수 → 세특`. 오른쪽 작업 영역도 같은 순서이고, **비어 있는 구획도 제목과 함께 전부 보인다.** 지금처럼 `hidden`으로 지우지 않는다.

**③ 인원을 명단보다 먼저 적는다.** workflow-surgery §2의 발견 — `--expected-count` 게이트는 이미 무력화되어 있다. 교사가 화면에서 "48명"을 본 뒤 30분 뒤 대화에서 인원을 물으면 본 대로 48을 답하기 때문이다. 색인의 **첫 항목**이 `대상 인원`이고 그 값이 `―`인 한 아래가 열리지 않으므로, 순서가 배치로 강제된다. 이건 안내 문구가 아니라 구조다.
마이크로카피:
> 이 활동을 하는 학생이 몇 명입니까?
> 명단을 보고 세지 마십시오. 명단이 틀렸을 가능성을 명단으로 검증할 수는 없습니다. 정원에서 전출·자퇴를 뺀, 아시는 수를 적어 주세요.

**④ 나중에 파일이 두 종류로 갈린다.** `파일` 블록이 처음부터 두 제목을 갖고 비어 있다. 그리고 미리 말한다:
> AI에게 줄 것
>   아직 없습니다
>   검수를 마치면 검수diff.json 하나가 생깁니다

F12의 진짜 문제는 "6종을 안 알려 준다"가 아니라 "6종을 동등하게 나열한다"였다. 색인은 **역할로 묶고, 교사가 실제로 만질 하나를 이름으로 예고**한다.

### 두 번째 방문(검수)의 첫 화면

색인은 비어 있다. 이걸 숨기지 않는다.

```
 검수  아직
 ────────────────────
 불러온 것이 없습니다.

 활동 폴더의
 검수번들.json 을
 여기에 끌어다 놓거나
 [ 파일 고르기 ]

 파일이 아직 없다면
 대화로 돌아가
 전체 생성을 마치세요.
```

F16(검수번들을 어떻게 얻어 어디서 여는지가 대화에만 있다) 해소. 빈 상태가 "nothing here"가 아니라 **파일명·위치·없을 때의 대처**를 말한다(product 규칙: "Empty states that teach the interface").

---

## 4. 붙여넣기 직후

### 4.1 무엇이 먼저 눈에 들어오는가

붙여넣는 순간 세 곳이 동시에 갱신된다. 순서는 **색인 → 확인 → 표**.

색인이 먼저인 이유: 교사의 눈은 붙여넣기 상자(오른쪽 위)에 있고, 색인 `명단 ―`이 `48줄`로 바뀌는 것은 주변시에서 잡힌다. "받았다"는 신호를 결과 스크롤 없이 준다.

### 4.2 F8 — 열 판별을 **열 번호가 아니라 첫 행의 값으로** 확인시킨다

지금 화면은 이렇게 말한다.

> 열 판별: 헤더 — 2번째 열을 학번, 4번째 열을 이름으로 읽었습니다.

교사는 "2번째 열"이 무엇인지 검증할 수 없다. 나이스 명렬(`반│번호│학번│성명`)에서 출석번호를 학번으로 잡아도 이 문장은 참으로 읽힌다. 바꾼다.

```
확인
──────────────────────────────────────────
첫 줄을 이렇게 읽었습니다.

    학번   30101
    이름   김하늘

 [ 학번과 이름 바꿔 읽기 ]   [ 다시 붙여넣기 ]
```

- `30101`은 교사가 아는 값이다. `2번째 열`은 아니다.
- 판별이 틀렸을 때의 **복구 동작이 같은 자리에** 있다(원칙 1: 판단이 필요한 곳에서 멈춘다).
- 값이 1~2자리면 `via`와 무관하게 한 줄이 더 붙는다:
  > 학번이 `7`처럼 짧습니다. 출석번호를 읽었을 수 있습니다. 학번 열이 따로 있다면 그 열까지 함께 긁어 주세요.

### 4.3 조용한 실패 — 세 가지

**칩(chip)을 전부 폐기한다.** `48명 · 중복 0 · 빈칸 0`은 세 개의 안심 배지이고, 그중 둘은 0인데 초록으로 렌더된다(ui-critique P0 #3). 대신:

| 지금 | 문서 작업대 |
|---|---|
| `중복 0` 초록 칩 | **아무것도 표시하지 않음** |
| `중복 1` 빨강 칩 + `tr.dup` 배경 1.129:1 | 표 **위쪽에 정오표 블록**으로 분리 + 색인 `지금 걸린 것`에 한 줄 |

**(a) 중복 학번** — 색이 아니라 **자리**로 드러낸다. 겹치는 행은 표에서 찾게 하지 않고, 표 앞에 뽑아 낸다.

```
겹치는 학번 1건
──────────────────────────────────────────
30112 이 12행과 34행에 두 번 있습니다.
여러 반을 함께 붙여넣었거나, 출석번호를 학번으로
쓴 표일 수 있습니다.
 [ 뒤엣것 지우기 ]   [ 둘 다 두기 ]
```

원인 가설을 주는 이 문구는 first-run이 "도구 전체의 모범"이라 부른 기존 경고를 그대로 계승한 것이다. 여기에 **행 번호**와 **동작 두 개**를 더했다. `tr.dup { background }`는 삭제한다 — 색 단독 전달(1.129:1)이 사라지고, P0 #2가 구조적으로 소멸한다.

**(b) 빈칸 / 건너뛴 행** — 몇 개가 아니라 **어느 행이 왜**.
> 7행과 22행을 건너뛰었습니다 — 이름 칸이 비어 있습니다. 엑셀에서 병합된 셀이면 이렇게 보입니다.

**(c) 시트 여럿 / 표 두 개가 붙음** — 지금은 아무도 안 잡는다. 머리글로 보이는 줄이 본문 중간에 다시 나오거나 열 개수가 급변하면:
> 27행에서 머리글이 다시 나옵니다. 표 두 개를 이어 붙여 놓으신 것 같습니다.
> [ 27행부터 잘라내기 ]   [ 그대로 두기 ]

**(d) 인원 대조** — 색인 첫 항목과 붙여넣은 줄 수를 즉시 맞댄다.
> 적어 두신 **48명**과 붙여넣은 **47줄**이 다릅니다. 마지막 줄을 함께 긁지 않으셨을 수 있습니다.

이 한 줄이 죽어 있던 게이트를 살린다(workflow-surgery §2). 그리고 이 값은 `명렬.json`의 `인원` 키로 저장되어, 30분 뒤 대화가 다시 묻지 않는다.

### 4.4 F9 — 미제출 체크는 점수 뒤로 간다

지금은 답안 파싱 전에 묻고 기본값이 전원 제출이다. 기본값이 곧 오답이다.
문서 작업대에서 `제출` 열은 **점수를 붙여넣기 전까지 비활성**이고, 점수를 붙이면 이렇게 바뀐다.

> 점수가 없는 학생이 15명입니다. 미제출입니까?
> [ 15명 모두 미제출로 ]   [ 하나씩 고르겠습니다 ]

기본값을 주지 않고, 근거(점수 없음)를 먼저 보인 뒤 묻는다.

### 4.5 F10 — `누락 18`을 없앤다

칩이 사라졌으므로 그 자리에 문장이 온다.
> 제출로 표시된 45명 중 **18명**의 점수가 붙여넣기에 없습니다. 채점표에서 아래쪽 행을 함께 긁으셨는지 확인해 주세요.

---

## 5. 검수 화면

### 5.1 40~50명의 피로 — 한 번에 한 명, 색인이 명부

지금은 48개 카드가 세로로 쌓인다. 결과:
- 내가 몇 번째인지 모른다.
- 항목 간 이동 수단이 순차 Tab뿐이다(P0 #7, 명시 요구사항 미충족).
- 그리고 이 형태가 곧 **"결과가 쏟아지는 화면"** — 다섯 번째 반참조 그 자체다.

문서 작업대는 색인을 명부로 바꾼다. 오른쪽에는 **한 명만** 놓는다.

- **위치**: 색인 머리 `검수 12 / 48`, 작업면 오른쪽 위 `12번째 · 48명 중`.
- **이동**: 색인 항목 클릭 / `↑` `↓` / `Enter`(= 검수 마침 + 다음 사람). 색인은 `role="listbox"`, 항목은 `role="option"` + roving `tabindex`, 현재 항목에 `aria-selected="true"`.
- **끝난 사람**: 이름 앞 `✓` + `--ink-quiet`. **`opacity`를 쓰지 않는다** — 지금 `.card.done { opacity:.55 }`는 본문을 3.90:1로 떨어뜨리면서 textarea는 여전히 편집 가능하다(P1).
- 스크롤 대상이 48개 카드에서 **48줄짜리 목록**으로 줄어든다. 목록은 한 화면에 30줄 이상 들어간다.

### 5.2 우선 검토를 어떻게 표시하는가

ui-critique의 지적: 탭 B가 반참조에 근접하는 걸 막는 **유일한 장치인 `우선검토` 태그가 화면에서 가장 안 보이는 요소**다(4.35:1 / 12.16px).

태그를 없애고 **문장으로 승격**한다. 세 자리에 동시에.

1. **색인의 그룹 제목** — 정렬 순서를 숨기지 않고 구조로 보인다.
   ```
   먼저 볼 것      9명
    30112 정하윤  검토
    ...
   나머지         39명
   ```
   (지금은 `data.students.sort(...)`로 조용히 섞을 뿐, 왜 이 순서인지 화면이 말하지 않는다.)
2. **작업면 맨 위, 본문 크기(17px)·`--ink` 대비의 한 문장** — 배지가 아니라 문장이다.
   > 원문에서 근거를 찾지 못한 서술이 있습니다.
   > 「토론에서 주도적으로」 — 원문에 토론 언급이 없습니다.
3. **색인 항목의 `검토`** — 15px, `--flag`(라이트 5.41:1 / 다크 11.13:1). 색 없이 글자만으로도 읽힌다.

### 5.3 원문과 세특의 배치

```
90rem(1440) 최대폭 − 색인 17rem(272) − 여백 2.5rem(40) = 약 1128px
  ┌ 학생 원문 528px ┐  ┌ 세특 568px ┐        (간격 32px)
```

- 한글 본문 17px 기준 **원문 약 31자 / 세특 약 33자.** 한글 산문 권장 30~45자 안에 든다.
- **세특이 더 넓다.** 고쳐 쓰는 대상이고 편집 중 줄바꿈이 흔들리면 안 되기 때문. 원문은 참조이므로 조금 좁아도 된다 — 다만 **크기는 같은 17px**이다(P0 #4: 지금 원문 13.92px < 탭 라벨 14.88px).
- 두 면의 **행간·기준선이 일치**한다(둘 다 `line-height: 1.75`, 같은 `padding-top`). 두 글을 나란히 오래 읽는 화면에서 이게 종이 느낌의 실체다.
- 원문은 길면 자체 스크롤. 세특은 내용만큼 자란다(`field-sizing: content` 미지원 시 스크립트로 auto-grow).

### 5.4 F15 — 바이트는 하나만 보여 준다

지금: `712 바이트(UTF-8) / 700 (2바이트 기준)` + 헤더에 `(기준 확정 전)`. 교사에게 "못 믿을 도구"로 읽힌다.

**NEIS·엑셀 수식이 쓰는 2바이트 기준 하나만 본문에 쓴다.**
```
612 바이트 · 상한 700
```
초과 시:
```
712 바이트 · 상한 700 — 12바이트 넘습니다
```
UTF-8 값은 `<details>` 각주 한 줄로 내린다("다른 기준으로 세면 738바이트입니다"). 화면에서 두 숫자가 동시에 경쟁하지 않는다. **초과 판정 기준도 같은 값으로 통일해야 한다**(현재 코드는 UTF-8로만 판정 — 이 문서는 요구만 적고 코드는 건드리지 않는다).

### 5.5 `alert` / `confirm` 제거

`검수번들.json`을 못 읽었을 때, 검수 미완료 상태로 저장할 때 — 지금은 네이티브 모달이다(product 금지: "Modal as first thought"). 대체:
- 읽기 실패 → 빈 상태 자리에 문장. `검수번들.json을 읽지 못했습니다 — {오류}. 활동 폴더의 파일이 맞는지 확인해 주세요.`
- 미완료 저장 → 저장 버튼 **옆에** 인라인 확인 한 줄. `검수 마침 표시가 없는 학생이 36명 있습니다. [ 그래도 저장 ]`

---

## 6. 파일의 정체를 화면이 어떻게 설명하는가

이 도구의 존재 이유 중 하나이므로, **상시 블록**으로 둔다. 토스트도, 하단 단락도 아니다.

```
 파일
 ────────────────────
 이 컴퓨터에만 둘 것
   명렬.json    저장됨
   점수원본.json  아직
 AI에게 줄 것
   아직 없습니다
   검수를 마치면
   검수diff.json
   하나가 생깁니다
```

### 6.1 설계 규칙 네 가지

**① 역할이 파일명보다 먼저다.** 제목 `이 컴퓨터에만 둘 것` / `AI에게 줄 것`은 15px `--ink`, 파일명은 13px `--ink-quiet`. 교사가 기억할 것은 두 문장이지 여섯 개 이름이 아니다.

**② 두 무리는 절대 섞이지 않는다.** workflow-surgery §4의 결론 — 검수본과 diff를 합치면 실명 경계가 기계적 보장에서 자율 규제로 강등된다. 화면도 같은 결론을 지지한다: **한 줄로 합칠 수 없는 두 제목이 있으면, 그 아래 파일도 합칠 수 없다는 게 눈으로 읽힌다.** 여기서 `AI에게 줄 것`을 두 항목으로 만드는 설계는 화면이 먼저 거부한다.

**③ 4행이 6행보다 읽힌다.** workflow-surgery §3이 제안한 `명렬.json` 1파일화(`{students, 학번목록, 항목, 인원}`)를 이 방향은 **전제로 요구한다.** 그러면 명단 탭의 `이 컴퓨터에만 둘 것`은 한 줄이 되고, 저장 대화상자도 3→1로 줄어 F11/F17(다운로드 차단 배너)의 원인이 사라진다. 색인은 그 결과가 눈에 보이는 자리다.
> ⚠ 단, workflow-surgery §3의 대가를 함께 갚아야 한다 — `verify_seteuk.load_submitted_ids`가 합집합이라, 한 파일에 명렬 48 + 제출자 45가 들어가면 "미제출자에게 세특 생성" FAIL이 영원히 안 뜬다. 화면 설계가 파이썬 4줄을 강제한다는 사실을 여기 적어 둔다.

**④ 상태는 셋뿐: `아직` / `저장됨` / `다시 저장 필요`.** 마지막 상태는 저장 후 명단을 고쳤을 때 뜬다. 이게 F18(저장했는데도 창 닫을 때 경고)의 해법이다 — `beforeunload`를 "students.length > 0"이 아니라 **색인의 파일 상태**에 건다. 색인이 저장 상태의 단일 출처가 되면, 화면과 경고가 어긋날 수 없다.

### 6.2 저장의 마이크로카피

버튼: `활동 폴더에 저장`. `내려받기 폴더에서 옮겨 주세요`는 삭제한다 — 도구가 못 하는 일을 사람에게 넘기는 문장이고, workflow-surgery §5에 따라 에이전트가 회수한다.
저장 뒤 색인 파일 행이 `아직 → 저장됨`으로 바뀌고, 작업면에는:
> 명렬.json 하나를 저장했습니다. 이 파일에는 실명이 들어 있습니다 — AI에게 주지 마십시오.
> 대화로 돌아가시면 됩니다.

### 6.3 파기(destroy) 문구

지금 화면과 안내에 있는 `pseudonymize.py destroy --dir <활동폴더> --yes`는 삭제한다. 교사가 직접 쳐야 할 정당한 CLI 지점은 하나도 없다(first-run). 색인 하단:
> 검수가 끝나면 실명이 든 파일을 지웁니다. 대화에서 **"중간 파일 지워 줘"** 라고 하시면 됩니다.

---

## 7. 색 전략

### 7.1 이 팔레트의 결정 하나

**의미를 나르는 배경 틴트를 전부 없앤다.** 경고·오류·성공에 색면(色面)을 깔지 않는다. 상태는 **활자(굵기·크기)와 괘선과 자리**로 나르고, 색은 잉크에만 쓴다.

이 결정 하나가 해소하는 것:
- `--warnbg #fdf6e3` — 팔레트에서 **유일한 크림 표면**(OKLCH C 0.026 / H 90). 금지 요구를 정면으로 어기던 값이 사라진다.
- `--warn` on `--warnbg` = 4.35:1(라이트 미달) — 배경이 없으니 대비 문제도 없다.
- `tr.dup { background: var(--badbg) }` = 라이트 1.129:1 / 다크 1.006:1 — 색 단독 전달이 사라진다.
- "색만으로 상태를 전달하지 않는다"(PRODUCT 접근성)를 예외 없이 만족한다.

남는 유색 표면은 **주 버튼 하나**(`--mark`)와 **중립 2층인 색인 배경**(`--index`)뿐이다. 후자는 의미가 아니라 구조다(product 규칙: "A second neutral layer for sidebars, toolbars, and panels").

### 7.2 토큰

문서 어휘로 짓는다. `--paper/--cream/--sand/--ivory` 류는 쓰지 않는다.
`--surface`(책상) · `--sheet`(놓인 종이) · `--index`(색인 면) · `--ink` · `--ink-quiet` · `--rule`(괘선) · `--rule-strong`(조작 가능한 경계) · `--mark`(표시=강조) · `--mark-ink` · `--flag`(확인 요망) · `--stop`(막힘).

### 7.3 라이트

| 토큰 | OKLCH | hex |
|---|---|---|
| `--surface` | `oklch(0.945 0.003 240)` | `#ebedef` |
| `--sheet` | `oklch(1.000 0.000 240)` | `#ffffff` |
| `--index` | `oklch(0.912 0.004 240)` | `#e0e2e4` |
| `--ink` | `oklch(0.255 0.006 240)` | `#202325` |
| `--ink-quiet` | `oklch(0.492 0.008 240)` | `#5d6265` |
| `--rule` | `oklch(0.870 0.0045 240)` | `#d2d5d7` |
| `--rule-strong` | `oklch(0.600 0.007 240)` | `#7d8184` |
| `--mark` | `oklch(0.455 0.085 158)` | `#256544` |
| `--mark-ink` | `oklch(1.000 0.000 240)` | `#ffffff` |
| `--flag` | `oklch(0.470 0.105 65)` | `#824c01` |
| `--stop` | `oklch(0.450 0.160 25)` | `#9b1e22` |

대비(계산값):

| 조합 | 비 | 요구 |
|---|---|---|
| `--ink` / `--sheet` | **15.76:1** | 본문 ≥4.5 ✓ |
| `--ink` / `--index` | **12.14:1** | ✓ |
| `--ink-quiet` / `--sheet` | **6.20:1** | ✓ |
| `--ink-quiet` / `--index` | **4.78:1** | 가장 빡빡한 지점, 통과 ✓ |
| `--mark-ink` / `--mark` (주 버튼) | **6.97:1** | ✓ |
| `--flag` / `--sheet` | **7.02:1** | ✓ |
| `--flag` / `--index` | **5.41:1** | ✓ |
| `--stop` / `--sheet` | **8.09:1** | ✓ |
| `--rule-strong` / `--sheet` | **3.94:1** | 조작 경계 ≥3 ✓ |
| `--rule-strong` / `--index` | **3.04:1** | ✓ |
| `--sheet` : `--surface` (층) | 1.17:1 | 층이 실재함 |

### 7.4 다크

| 토큰 | OKLCH | hex |
|---|---|---|
| `--surface` | `oklch(0.190 0.005 240)` | `#121416` |
| `--sheet` | `oklch(0.248 0.006 240)` | `#1f2224` |
| `--index` | `oklch(0.152 0.005 240)` | `#0a0c0d` |
| `--ink` | `oklch(0.930 0.004 240)` | `#e5e8ea` |
| `--ink-quiet` | `oklch(0.740 0.006 240)` | `#a7abae` |
| `--rule` | `oklch(0.335 0.007 240)` | `#34373a` |
| `--rule-strong` | `oklch(0.545 0.009 240)` | `#6c7175` |
| `--mark` | `oklch(0.760 0.100 158)` | `#77c599` |
| `--mark-ink` | `oklch(0.190 0.025 158)` | `#0a170f` |
| `--flag` | `oklch(0.820 0.100 78)` | `#e8bc78` |
| `--stop` | `oklch(0.760 0.115 25)` | `#f1938c` |

| 조합 | 비 |
|---|---|
| `--ink` / `--sheet` | **13.09:1** |
| `--ink` / `--index` | **15.97:1** |
| `--ink-quiet` / `--sheet` | **6.98:1** |
| `--ink-quiet` / `--index` | **8.51:1** |
| `--mark-ink` / `--mark` (주 버튼) | **8.92:1** ← 지금 2.26:1 |
| `--flag` / `--sheet` | **9.12:1** |
| `--stop` / `--sheet` | **7.14:1** |
| `--rule-strong` / `--sheet` | **3.25:1** |
| `--sheet` : `--surface` (층) | 1.15:1 |

**다크 주 버튼이 P0 #1의 정확한 수정이다.** 지금은 `#fff` on `#7fb99a` = 2.26:1 — 그 요소가 `#save`와 `#rv-save`, 파이프라인의 두 종결 동작 전부다. 밝은 초록 위에 **어두운 잉크**를 얹어 8.92:1로 올린다.

### 7.5 크림 금지 요구를 어떻게 통과하는가

금지대역은 warm-neutral near-white — **L 0.84–0.97 / C < 0.06 / hue 40–100**.
이 팔레트의 밝은 면은 `--surface` L 0.945 · `--index` L 0.912로 **명도는 대역 안**이지만 **hue 240 · C 0.003–0.004**다. 색상각이 금지대역(40–100)에서 140° 이상 떨어진 **차가운 중립**이고, 채도는 대역 상한의 1/15이다. 눈에는 크림이 아니라 **레이저 출력물 위의 회색 책상**으로 읽힌다.
그리고 다크 중립은 hue 240이다 — 기존 다크가 H≈271(보라 기울기, P2)이던 것을 파랑 쪽으로 옮겨 AI 챗봇 반참조의 잔향을 지운다. C는 0.004~0.006이라 색으로 지각되지 않는다.

**두 테마의 층 구조가 같다**: `sheet:surface`가 라이트 1.17 / 다크 1.15. 지금은 라이트 1.035 / 다크 1.089로 두 테마가 다른 구조로 읽혔다(P1).

### 7.6 상태 어휘

| 상태 | 표현 |
|---|---|
| hover | 버튼 `--rule-strong` 경계 + 배경 `--surface`. 링크 밑줄 굵기 1→2px |
| focus-visible | `outline: 2px solid var(--ink); outline-offset: 2px`. **잉크색 링** — 새 색상을 추가하지 않고 두 테마 모두 13:1 이상 |
| active | 이동 없음. 배경 한 단 더 |
| disabled | `opacity` 금지. `--ink-quiet` + `--rule` 경계 + `cursor: default` + **비활성 이유 문장** |
| selected (검수 현재 항목) | `--mark` 잉크 + 좌측 여백 안의 `▸` 글리프. 배경 채우지 않음 |
| error / warn | 잉크 색 + 문장. 면 없음 |

`opacity`로 상태를 만드는 곳은 전부 없앤다(P1: `.card.done` .55 → 본문 3.90:1, `tr.absent` .45 → 2.89:1).

---

## 8. 타이포

### 8.1 스택 (외부 폰트 로드 불가)

```css
font-family:
  -apple-system, BlinkMacSystemFont,
  "Segoe UI",                    /* 라틴·숫자 — 한글 글리프 없음 */
  "Pretendard Variable", Pretendard,
  "Apple SD Gothic Neo",
  "Malgun Gothic", "맑은 고딕",   /* 한글 폴백 */
  system-ui, sans-serif;
font-variant-numeric: tabular-nums;
```

- `Segoe UI`를 `Malgun Gothic` **앞에** 둔다. Segoe UI에는 한글이 없어 윈도우가 글리프 단위로 맑은 고딕에 폴백하므로, **숫자·영문은 Segoe UI, 한글은 맑은 고딕**이 된다. 학번(`30101`)과 바이트 수가 맑은 고딕 숫자보다 정확히 읽힌다.
- `tabular-nums`는 색인의 세로 숫자 정렬에 필수다. 색인 숫자는 우측 정렬 — 이게 "명세서"의 감각이다.
- 본문(세특·원문)에는 `word-break: keep-all`(한글 어절 보존) + `overflow-wrap: break-word`.

### 8.2 고정 rem 스케일 — 5단계뿐

| 단계 | 크기 | 행간 | 쓰이는 곳 |
|---|---|---|---|
| `--t-doc` | `1.625rem` / 26px | 1.3 | 활동명(페이지에 하나) |
| `--t-head` | `1.25rem` / 20px | 1.35 | 작업면 구획 제목, 학생 이름 |
| `--t-body` | `1.0625rem` / 17px | **1.75** | **세특·원문·안내 문장·textarea** |
| `--t-ui` | `0.9375rem` / 15px | 1.45 | 색인 항목, 버튼, 표 셀, 라벨, 탭 |
| `--t-note` | `0.8125rem` / 13px | 1.5 | 파일명, 바이트, 각주 |

인접 비율 1.13~1.30. 지금은 0.19rem 안에 9단계, 인접비 1.01~1.03이라 **스케일이 아니었다**(P1).

**깨뜨릴 수 없는 규칙 하나:** `--t-ui`(15px) < `--t-body`(17px). UI 라벨은 본문보다 반드시 작다. 지금은 원문 13.92px < 탭 라벨 14.88px로 뒤집혀 있다(P0 #4). PRODUCT.md의 "본문 크기·행간을 UI 라벨보다 우선한다"는 취향이 아니라 순서 규칙이다.

### 8.3 한글 조판

- **행간 1.75.** 한글은 자소가 자간을 꽉 채워 라틴보다 넉넉한 행간이 필요하다. 세특·원문 두 면에 같은 값을 써서 **기준선이 맞물리게** 한다.
- **자간 0.** 한글 본문에 `letter-spacing`을 주지 않는다. 20px 이상 제목에만 `-0.01em`.
- **줄 길이 30~35자**(≈ 34ch). 지금 산문은 약 83자이고, 하필 가장 긴 줄이 실명 경고문이다(P1). 작업면 산문은 `max-width: 34em`로 묶는다.
- **`text-transform: uppercase` 전면 금지.** 지금 `h2`/`h3`에 걸린 uppercase는 한글에 아무 효과가 없고, 실효는 구획 제목을 12px 최저대비로 만든 것뿐이다(P0 #5). 구획 제목은 `--t-head` 20px `--ink` + 아래 `--rule` 한 줄로 바꾼다. 소문자 eyebrow는 사라진다.
- **`01/02/03` 번호도 없앤다.** 지금 `1 · 붙여넣기 / 2 · 확인 / 3 · 점수 / 4 · 저장`은 금지 목록의 번호 스캐폴딩이고, 순서는 이미 색인이 표현한다. 중복이다.

### 8.4 세로 리듬 — 종이 느낌의 실체

기본 행 = 17px × 1.75 ≈ **30px**. 간격은 이 배수만 쓴다.

```
--s1: 4px   (인라인)
--s2: 8px
--s3: 12px
--s4: 20px
--s5: 30px  (= 1행, 문단)
--s6: 60px  (= 2행, 구획)
```

간격 6종. 지금은 15종이고 공배수가 없다(P1). 크림 배경 없이 "문서"를 만드는 방법이 정확히 이것 — **모든 구획 제목의 기준선이 30px 격자 위에 앉고, 색인과 작업면의 첫 줄이 같은 높이에서 시작한다.** 스크롤해도 두 기둥의 행이 어긋나지 않는다.

---

## 9. 모션

**상태 변화에만.** 지금은 `transition`·`animation`·`keyframes`가 **0건**인데, 이건 장식 배제가 아니라 상태 전달 수단의 부재다(P1).

| 무엇이 | 언제 | 어떻게 |
|---|---|---|
| 색인 값 (`― → 48줄`) | 파싱 성공 시 | 140ms `opacity` + `translateY(2px→0)`. **바뀐 행만.** 색인 전체가 움직이면 안 된다 |
| 파일 행 (`아직 → 저장됨`) | 저장 성공 시 | 180ms 같은 방식. 저장은 되돌리기 어려운 동작이라 유일하게 조금 길다 |
| `지금 걸린 것` 항목 | 생길 때 / 해소될 때 | 160ms `height` + `opacity`. **해소될 때도 애니메이션한다** — 사라지는 걸 봐야 고쳐진 걸 안다 |
| `확인` 구획 등장 | 붙여넣기 직후 | **단일** 160ms 페이드. 구획 3개를 stagger로 띄우지 않는다(지금은 카드 3개가 한 프레임에 튄다) |
| 검수 학생 전환 | `↑`/`↓`/클릭 | 100ms 크로스페이드만. **슬라이드 금지** — 좌우 이동은 공간적 위치를 암시하는데 그건 거짓이다 |
| 버튼 | hover/active | 120ms `background`·`border-color`. `transform` 없음 |
| textarea 재파싱 | 입력 중 | 애니메이션이 아니라 **300ms 디바운스 + `blur`/`paste` 즉시**. 지금은 매 타건 재파싱이라 타이핑 중 오류가 번쩍인다(P1) |

전부 120~180ms. product 규칙(150~250ms)의 아래쪽 — 교사는 흐름 안에 있고 마감이 있다.
**페이지 로드 연출 없음.** 첫 화면은 그냥 거기 있다.

`aria-live="polite"`를 두 곳에 건다: `지금 걸린 것` 블록, 저장 결과 문장. 검수 학생 전환은 `aria-live` 대신 학생 이름을 `<h2>`로 두고 포커스를 옮긴다.

### reduced-motion

```css
@media (prefers-reduced-motion: reduce) {
  * { transition-duration: 1ms !important; animation: none !important; }
}
```
움직임을 지우되 **신호까지 지우지 않는다.** 대안: 값이 바뀐 색인 행에 `--mark` 밑줄이 **2초간 남았다가 없어진다**(`transition` 대신 클래스 토글 + `setTimeout`). 밑줄은 문서의 어휘이고, 움직이지 않으면서 "여기가 방금 바뀌었다"를 말한다.

---

## 10. 진단 마찰 대응

### 완전히 해결 — 11개

| # | 마찰 | 어디서 |
|---|---|---|
| **F7** | 페이지가 지금 어느 단계인지 말하지 않는다 | 색인 전체 (§1, §2) |
| **F8** | 헤더 경로에서 출석번호를 학번으로 읽고 경고 없음 | 첫 행 실제 값 확인 + `바꿔 읽기` (§4.2) |
| **F9** | 미제출자를 아직 모르는 시점에 미제출을 묻는다 | `제출` 열을 점수 뒤로, 기본값 제거 (§4.4) |
| **F10** | `누락 18`이 무엇인지 알 수 없다 | 칩 폐기 → 문장 (§4.5) |
| **F12** | 파일 6종을 화면이 구분해 주지 않는다 | 색인 `파일` 블록, 역할 우선 (§6) |
| **F14** | 30분 뒤 세특이 0문장이라는 걸 아무도 예고하지 않는다 | 색인 `세특 ― / 대화에서 만듭니다` (§1.3, §3) |
| **F15** | 바이트가 두 개 뜨고 어느 쪽이 기준인지 모른다 | 2바이트 기준 하나, UTF-8은 각주 (§5.4) |
| **F16** | 검수번들을 어떻게 얻어 어디서 여는지가 대화에만 있다 | 검수 탭 빈 상태가 파일명·위치·대처를 말함 (§3) |
| **F18** | 저장했는데도 창 닫을 때 경고가 뜬다 | `beforeunload`를 색인 파일 상태에 건다 (§6.1 ④) |
| **F19** | 명렬을 다시 불러올 방법이 없다 | 색인 각 파일 행에서 불러오기 (§11-1) |
| **F20** | `안내` 시트·"검수 전 NEIS 금지"가 화면에 없다 | 색인 하단 고정 두 줄 (§2) |

### 부분 해결 — 2개

| # | 마찰 | 이 방향이 하는 일 / 남는 것 |
|---|---|---|
| **F11 / F17** | 저장이 브라우저 다운로드 차단에 걸리는데 화면은 성공을 말한다 | 성공 주장이 **사라지는 토스트가 아니라 지속 목록**이 되어, 교사가 나중에라도 `아직`을 발견할 수 있다. + 이 방향은 workflow-surgery §3의 **1파일화를 전제로 요구**하므로 차단의 원인(다중 다운로드)이 사라진다. **남는 것**: `<a download>` 경로에는 성공 콜백이 없어 `저장됨`이 여전히 추정이다 → §11-6 |

### 이 방향으로 해결되지 않는 것 — 7개

**F1**(스킬을 무슨 말로 부르는지) **F2·F3**(파이썬 설치·PATH) **F4**(재시작 초기화) **F5**(파일 위치 전달) **F6**(도구.html을 여는 책임) **F13**(파일 이동 확인).
전부 브라우저 창 밖의 일이다. 색인은 여기에 아무 도움이 안 된다. 이건 SKILL.md와 에이전트 동작의 문제이고, 그렇게 정직하게 남겨 둔다.

### ui-critique P0 — 8개 전부

| P0 | 해소 |
|---|---|
| 1 · 다크 주 버튼 2.26:1 | `--mark-ink` on `--mark` = **8.92:1** (§7.4) |
| 2 · `tr.dup` 1.129:1 / 1.006:1, 색 단독 | 행 틴트 삭제, 정오표 블록으로 분리 (§4.3a) |
| 3 · `중복 0`을 초록으로 렌더 | **칩 폐기.** 0이 표시될 자리가 없다 (§4.3) |
| 4 · 본문(13.92px) < UI 라벨(14.88px) | 17px vs 15px, 규칙으로 고정 (§8.2) |
| 5 · uppercase eyebrow 전 구획 | uppercase·letter-spacing 삭제, 20px `--ink` 제목 (§8.3) |
| 6 · hover/active/focus-visible 0건 | 상태 어휘 표 (§7.6) |
| 7 · `role=` 0건, 검수 항목 간 이동 수단 없음 | `listbox`/`option`, roving tabindex, `↑↓`/`Enter` (§5.1) |
| 8 · 4단계 중 3단계가 `hidden` | **아무것도 숨기지 않는다.** 비어 있는 구획도 제목과 이유가 보인다 (§2-0) |

주요 P1: `--warnbg` 크림 표면 삭제(§7.1) · `--warn` 4.35:1 소멸(§7.1) · 라이트 카드 층 1.035→1.17(§7.3) · 조작 경계 1.25→3.04 이상(§7.3) · `opacity` 상태 붕괴 제거(§7.6) · 타입 12종→5종(§8.2) · 간격 15종→6종(§8.4) · 줄 길이 83자→34자(§8.3) · `aria-live` 도입(§9) · `alert`/`confirm` 제거(§5.5) · 모션 0건→상태 전달용 7종(§9) · 매 타건 재파싱→디바운스(§9).

### workflow-surgery 채택

§1(브라우저 2회 유지) · §2(인원을 명단 위로 — **색인 첫 항목으로 구조화**) · §3(JSON 4종 — 파일 블록이 이걸 요구) · §4(검수본/diff 분리 유지 — 두 제목이 이걸 표현) · §5(손 이동 0 — `내려받기 폴더에서 옮겨 주세요` 삭제) · §8(온보딩 배너 없음, 배치가 곧 안내) · §9(번호 스캐폴딩·eyebrow 제거).

---

## 11. 이 안이 무너지는 지점

정직하게, 여섯 개다.

**11-1. 두 번째로 열면 색인은 비어 있다.**
"이 활동의 상태가 늘 보인다"는 약속의 절반은 거짓이다. 검수하러 다시 열 때 색인은 아무것도 모른다. 채우려면 파일을 불러와야 하고, 그러면 실명이 다시 브라우저에 들어온다. `localStorage`로 메우고 싶어지지만 그건 "이 도구는 아무것도 기억하지 않는다"는 약속을 깬다.
지금의 답 — 비어 있는 채로 두고, 빈 행이 **무엇을 불러오면 채워지는지**를 말하게 한다. F19가 여기서 해결되는 건 사실이지만, 그건 문제를 해결한 게 아니라 **약점을 어포던스로 바꾼 것**에 가깝다. 색인이 "늘 아는 물건"이라는 이 방향의 서사는 여기서 한 번 접힌다.

**11-2. 왼쪽 기둥은 나이스와 가장 닮은 형상이다.**
아이콘·로고·접기 토글을 다 빼도, 폭 272px의 고정 좌측 패널은 첫인상에서 관공서 시스템으로 읽힐 위험이 남는다. ui-critique도 "관공서가 가장 위험하나 색이 아니라 경계 4겹·소형 활자 12종·`nowrap` 표 때문"이라고 했다 — 즉 이 위험을 막는 건 전부 **조판**이다(박스 0개, 배경 계조 1단, 활자 2종, 30px 격자, 숫자 우측 정렬). 조판이 조금만 흐트러지면 즉시 무너진다.
**이 방향은 다른 안보다 실행 난이도가 높다.** 잘 만들면 가장 좋지만, 대충 만들면 가장 나쁘다.

**11-3. 화면 지분 21%를 먹는데, 첫 10분간 대부분 `―`다.**
비어 있음이 순서를 가르친다는 게 내 주장이지만, 반대로 **"아무것도 안 적힌 쓸모없는 기둥"** 으로 읽힐 수도 있다. 특히 회차가 쌓여 도구에 익숙해진 교사에게는 색인이 알려 주는 게 없어지고 폭만 남는다. 3회차 이후 사용자에게 이 방향의 가치는 감소한다.

**11-4. 한 번에 한 명은 전체 훑기를 없앤다.**
48명의 문체가 서로 비슷비슷한지, 같은 표현이 반복되는지는 **나란히 스크롤해야** 보인다. 색인에 미리보기를 넣으면 "균등하게 대충 보기"로 되돌아가고, 안 넣으면 문체 일관성 점검 수단을 잃는다.
지금 생각하는 완화책은 `전체 훑어보기` 보조 화면인데, 그러면 화면이 둘이 되어 이 방향이 자랑하는 단순함이 깎인다. **이 트레이드오프는 아직 해결하지 못했다.**

**11-5. 900px 이하에서는 이 방향이 다른 안과 구별되지 않는다.**
색인이 한 줄로 접히면 남는 건 요약 스트립 하나다. 노트북을 반으로 쪼개 쓰는 교사(한쪽엔 클로드 코드, 한쪽엔 도구)에게는 이 안의 핵심이 거의 도달하지 않는다. 그리고 그건 드문 사용 방식이 아니다.

**11-6. `저장됨`은 완전히 검증할 수 없다.**
`showSaveFilePicker`가 막힌 `file://` 경로에서는 `<a download>`에 성공 콜백이 없다. 색인이 `저장됨`이라고 쓸 근거가 사실은 "클릭했다"뿐이다. 이 도구의 존재 이유가 **조용히 틀리지 않기**인데, 하필 그 약속을 대표하는 요소가 그 자리에서 조용히 틀릴 수 있다.
완화: 폴백 경로에서는 `저장됨`을 쓰지 않고 **`내려받기 폴더 확인`** 이라는 별도 상태를 쓴다. 정직하지만, 상태가 하나 늘고 문구가 길어진다 — §6.1의 "상태는 셋뿐"이 넷이 된다. 단순함을 정직함에 내주는 자리다.

---

## 12. 만들 때의 순서 (다음 회차 입력)

1. **색인 골격 + `파일` 블록** — F7·F12·F14·F20을 한 번에 연다. 나머지가 붙을 자리가 먼저 생겨야 한다.
2. **인원 입력을 색인 첫 항목으로, 붙여넣기 잠금** — 죽어 있던 게이트를 살린다. HTML 20줄, 파이썬 0.
3. **`확인` 구획 — 첫 행 값 대조 + `바꿔 읽기`** — F8. 실측 63% 무성 실패의 핵심.
4. **칩·행 틴트 폐기, 정오표 블록** — P0 #2·#3 동시 소멸.
5. **팔레트·스케일·격자 교체** — P0 #1·#4·#5 + P1 다수.
6. **검수 한 명 화면 + 색인 명부 + 키보드** — P0 #7, 반참조 방어.
7. **상태·모션·`aria-live`** — P0 #6, P1.

1~4까지만 해도 이 도구가 실패라고 정의한 것("교사가 틀린 줄 모르고 NEIS에 넣는 것")의 앞단이 대부분 막힌다.
