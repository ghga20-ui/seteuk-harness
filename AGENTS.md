# AGENTS.md — 타 코딩 에이전트(Codex·Antigravity 등)용 진입점

이 저장소는 **Claude Code 스킬(seteuk-harness)**입니다. 수행평가 학생 원본을 입력으로 과세특(세부능력 및 특기사항) 초안을 만드는 자기 성장형 하네스입니다. Claude Code 밖의 에이전트(Codex, Antigravity 등)에서 이 폴더를 열었다면 **이 환경 지원은 실험적**입니다 — 절차 설계와 개인정보 규율은 Claude Code에서만 검증되었고, 다른 런타임에서의 동작·규율 준수는 검증되지 않았습니다. 아래 "미검증 고지"를 반드시 읽으십시오.

## 1. 정본은 SKILL.md다

절차의 정본은 이 폴더의 **`SKILL.md`**입니다. 이 AGENTS.md는 그 정본을 대체하지 않습니다 — 다른 환경에서 빠지는 안전장치를 사람(에이전트)이 대신 지키도록 안내하는 얇은 포인터일 뿐입니다.

**먼저 `SKILL.md` 전체를 읽고, 파이프라인(§1~§9)을 순서대로 따르십시오.** 순서를 바꾸지 마십시오. 특히 위키 선독(매 실행 시작), 실행 전 환경 점검, 저장 게이트는 생략 불가입니다.

## 2. 이 환경에서 빠지는 안전장치의 대체 규율

Claude Code에서는 하네스가 자동으로 지켜 주던 것을, 이 환경에서는 **에이전트가 규율로 대신 지켜야** 합니다.

- **명단은 어떤 경우에도 채팅으로 받지 않습니다.** 사용자가 명단(학번+이름 목록)이나 채점표 내용을 채팅에 붙여넣으려 하면 즉시 중단시키고 파일로 받으십시오. 이미 붙여넣어진 실명·학번은 전송이 끝난 것이라 복구 불가입니다. 문구 예: "그 목록은 여기 붙여넣지 마세요. 학생 실명이 대화에 들어갑니다. 활동 폴더에 파일로 저장하고 파일 경로만 알려 주세요."
- **자동 발동(트리거)이 없음.** 이 환경에서는 스킬이 자동으로 켜지지 않습니다. **사용자가 세특 작성을 요청하면, 먼저 이 AGENTS.md와 `SKILL.md`를 읽는 것으로 시작**하고, 그다음 SKILL.md의 "위키 선독"과 "실행 전 점검"부터 밟으십시오.
- **구조화 질문 UI가 없음.** 인터뷰 UI가 없으므로 **질문은 한 번에 하나씩, 번호 선택지로** 제시하고 답을 기다리십시오(예: "① …채점표.xlsx  ② 백업.xlsx — 어느 파일인가요?"). 여러 질문을 한꺼번에 쏟지 마십시오. 파일 후보가 2개 이상이면 자동 선택하지 말고 반드시 확정받으십시오.

## 3. 개인정보 계약(타 모델용 재확인)

이것은 코드가 아니라 **프롬프트 규율**이므로 이 환경에서 특히 명시적으로 지켜야 합니다.

- **실명·학번이 든 파일을 에이전트가 직접 열지 마십시오.** 다음 파일은 절대 Read/열람 금지입니다: `명렬.json`, `매핑.json`, 채점표 원본(xlsx 등), `검수번들.json`, `검수본(실명·보관용).json`, 학생 답안 원본, `확인.html`, `검수.html`. 대신 **파이썬 CLI의 stdout 요약(인원수·건수·분포)만 읽으십시오.**
- 오케스트레이터 자체가 클라우드 LLM이므로 **대화창에 들어오는 순간 이미 전송**입니다. 관찰 메모·점수는 대화로 받지 말고 파일로 받아 CLI로 토큰화한 뒤에만 결과를 보십시오.
- 에이전트가 열어도 되는 것: **토큰만 든 산출물** — `토큰본.json`, `관찰메모.json`, `점수.json`, `검수diff(AI용).json`(이름·학번 없음).

**파이썬 CLI 목록(`scripts/pseudonymize.py <명령>`) — 각각의 용도 한 줄:**
- `detect` — 활동 폴더의 명단/채점표 후보 파일명·수정시각·시트 수만 출력(내용 안 엶).
- `roster` — 명단 파일 → `명렬.json` 생성. stdout은 "명렬 인식: N명"만.
- `confirm-html` — 실명 눈대조용 `확인.html` 생성(사용자가 브라우저로 확인, 에이전트는 안 엶).
- `issue` — 제출자 학번으로 토큰 발급 → `매핑.json`. stdout은 "제출자 N명, 미발급 M명".
- `mask` — 학생 텍스트의 실명·학번 치환 → `토큰본.json`(학번 유출 시 exit 1 차단).
- `memo` — 관찰 메모(파일) 토큰화 → `관찰메모.json`.
- `score` — 채점표(파일)에서 토큰별 점수 추출 → `점수.json`. 점수 열 애매하면 exit 1로 물음. 분포만 stdout.
- `finalize` — 토큰 초안 → 실명 `초안.json` 재결합(복원 실패 시 exit 1).
- `review-bundle` — 검수용 번들 생성(실명 산출물, 로컬 전용).
- `destroy` — 매핑·명렬·토큰본 등 중간 산출물 파기(먼저 목록 확인 후 `--yes`).

저장/검증은 `scripts/verify_seteuk.py <초안.json> --profile … --roster … --mapping … --save …`(FAIL이면 저장 차단). 검증기는 초안의 학생 키(`토큰`/`학번`)로 단계를 판별한다 — 토큰 단계(finalize 전)는 텍스트 검사만 하며 `--roster`·`--save`를 거부하고, 실명 단계의 stdout은 학번 대신 토큰(매핑 제공 시) 또는 코드별 집계만 표시한다. 손글씨 이미지 마스킹만 예외적으로 `mask_images.py` 함수 직접 호출이며, 실적용을 검증하는 기계 게이트가 없어 전송 직전 교사 육안 확인이 필수입니다.

## 4. 미검증 고지(정직하게)

- 이 환경(Claude Code 밖)에서의 **규율 준수 자체가 미검증**입니다 — 위 §2·§3은 사람이 지키는 프롬프트 규율이라, UI 강제가 없는 여기서는 보증 수준이 Claude Code보다 낮습니다.
- **손글씨 전사 품질**과 마스킹 실적용은 어느 환경에서도 기계 게이트가 없어 육안 확인이 필수입니다.
- 확신이 없으면 진행하지 말고 멈춰서 사용자에게 물으십시오. 조용히 틀리는 것보다 멈추는 것이 이 도구의 설계 원칙입니다.

모든 응답은 한국어로. 문구는 동료 말투로, 행동을 먼저 말하고, 격언·디자인 내부어를 쓰지 않습니다.
